
window.allDBsData = {
   
"zaj": [
 //video
{ "id": 1, "type": "video", "title": "LoveHerFeet - Creepy Stepdad Stepdaughter", "image": "https://up6.cc/2026/05/177876650039191.jpg", "contentURL": "https://drive.google.com/file/d/1JIapsW3ubHNY-AimREIX_K_dzKWyfMtZ/view?usp=drivesdk", "labels": "Feet, milf", "duration": "11:30" },
{ "id": 2, "type": "video", "title": "Busy Mercedes Carrera takes time off work for a good fuck", "image": "https://a.top4top.io/p_37535ta9f0.jpg", "contentURL": "https://drive.google.com/file/d/1Ira7gOoh78MxUlYApl3Rsf_BpUbe_V0H/view?usp=drivesdk", "labels": "hardcore, blowjob, creampie, milf", "duration": "00:00" },
{ "id": 3, "type": "video", "title": "Perfect pink pussy babe gets an intense orgasm and unforgettable CREAMPIE", "image": "https://files.catbox.moe/jxijpj.jpg", "contentURL": "https://drive.google.com/file/d/1FF6hq54P1xsK4FXu1ZucQ3enAWAQ5Wkd/view?usp=drivesdk", "labels": "Pink Pussy, blowjob, creampie, milf", "duration": "00:00" },
{ "id": 4, "type": "video", "title": "Slut Teenie Step Sis Makes A Move On her Older Stepbro", "image": "https://f.top4top.io/p_3755ozfb71.jpg", "contentURL": "https://drive.google.com/file/d/1IbfZ0GQlkjcsu5LAyWFCAreJ46ngowXo/view?usp=drivesdk", "labels": "Stepbrohardcore, blowjob, creampie, milf", "duration": "00:00" },
{ "id": 6, "type": "video", "title": "She_s going on a Date on Valentine_s Day. what could go Wrong", "image": "https://up6.cc/2026/05/177876642332791.png", "contentURL": "https://drive.google.com/file/d/1iMFSYhVyBh-cO7qn2r9WrFgKwxaKZTi8/view?usp=drivesdk", "labels": "blowjob, creampie, milf", "duration": "00:00" },
{ "id": 5, "type": "video", "title": "Horny Teen Eats Creampie from her Asian Girlfriend_s Pussy after Hard and Rough Sex with Sugar Daddy", "image": "https://i.top4top.io/p_37897jkwi1.jpg ", "contentURL": "https://drive.google.com/file/d/1KLnk9AQ3nC1JHaA2nTC1-wC6FlH8pfou/view?usp=sharing ", "labels": "", "duration": "00:00" },
{ "id": 7, "type": "video", "title": "Genuine Home Fucking Video", "image": "https://b.top4top.io/p_37890950m1.jpg", "contentURL": "https://drive.google.com/file/d/1GHB4iPeXQFi6ATsJ8trGRm2VfB9u9iCu/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 8, "type": "video", "title": "Sunny Shine Enjoys Unforgettable First anal Sex ", "image": "https://b.top4top.io/p_37897fhpa1.png", "contentURL": "https://drive.google.com/file/d/1IzO67I4rOeulqdMJJrUjJzlXfhZdMxxJ/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 9, "type": "video", "title": "Amateur Latina Squirts Hard while Straining", "image": "https://j.top4top.io/p_3789pljv32.jpg", "contentURL": "https://drive.google.com/file/d/1X6yeQPWBrkhDV9a5F2dHZUOVNys2cZMS/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 10, "type": "video", "title": "I made him Fuck me in the Shower", "image": "https://k.top4top.io/p_3789htbp91.png", "contentURL": "https://drive.google.com/file/d/103kyExFwCG9aZPk2s6uNqAXgbZ4TGSvs/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 11, "type": "video", "title": "Lucky Neighbor-When the neighbor knocked on the door, they were already hot and craving sex", "image": "https://l.top4top.io/p_3789mx9tb1.png", "contentURL": "https://drive.google.com/file/d/1NEl-elR6W43fQemm3ww3IND3daPYsA7b/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 12, "type": "video", "title": "My Birthday Party went not as Expected", "image": "https://k.top4top.io/p_3789rlfhw1.png", "contentURL": "https://drive.google.com/file/d/1K-7ja4Vp0IR38MRgJd66aa4-ZZtmCLan/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 14, "type": "video", "title": "Petite Babe Freya Von Doom Rides BBC after Blowjob", "image": "https://a.top4top.io/p_3789whdxd2.png", "contentURL": "https://drive.google.com/file/d/1ldz82d_Q7gIG_MwR610R3BPmUpM3PKqE/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 15, "type": "video", "title": "POV Sloppy Blowjob & Titfuck & Hard Doggyfuck with Creampie ", "image": "https://l.top4top.io/p_378929nfm1.jpg", "contentURL": "https://drive.google.com/file/d/1skThZvvLKvQMxtqF4t0RwUlQ-zp4Z9S8/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 16, "type": "video", "title": "PURE TABOO 18yo Step-Sister Longs 2 try ANAL with Bro", "image": "https://j.top4top.io/p_37895o52c1.jpg", "contentURL": "https://drive.google.com/file/d/1NIT3LaJkIKlaF0raYCUMTsXbvIZ1gb7x/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 17, "type": "video", "title": "PURE TABOO Gia Derza Secretly Loses her Anal Virginity to her Priest", "image": "https://k.top4top.io/p_37891xhso2.jpg", "contentURL": "https://drive.google.com/file/d/1Inp_SQpbnl-yvhJCb-1YxRG7BXhp_Mou/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 18, "type": "video", "title": "PURE TABOO Innocent Church Girl tries Anal for the first Time with Perverted Priest", "image": "https://j.top4top.io/p_3789o48gc1.jpg", "contentURL": "https://drive.google.com/file/d/1NIT3LaJkIKlaF0raYCUMTsXbvIZ1gb7x/view?usp=sharing", "labels": "", "duration": "00:00" },
{ "id": 36, "type": "video", "title": "PURE TABOO Shocked MILF Dana Vespoli Discovers Husband Swapped Stepdaughter For Petite Coco Lovelock ", "image": "https://i.top4top.io/p_37905f2tf1.jpg", "contentURL": "https://drive.google.com/file/d/1J6fl4O0YKSa5pQDf8YxhVbltxdnQtsQx/view?usp=sharing", "labels": " Pornstars,Coco Lovelock,Dana Vespoli " },
{ "id": 40, "type": "video", "title": "Sugar Daddy Fucks His Young Fresh Asian Wife Hard And Cums In her Pretty  ", "image": "https://j.top4top.io/p_37905n4ei2.jpg", "contentURL": "https://drive.google.com/file/d/1R-gNBXltITRYJELiyhukg8rK8wdk1q5c/view?usp=sharing", "labels": " Pornstars, Denis Marti و Fanky Town و Kvini Kim" },
{ "id": 41, "type": "video","title": "She Stole From Her Foster Parents So She Pay With Her Body", "image": "https://k.top4top.io/p_37902fvip3.jpg", "contentURL": "https://drive.google.com/file/d/1nX7EdN4VolqAdjPIGNK6t8Jcy4dpyAvV/view?usp=sharing", "labels": " Pornstars,Mr. Pete و Phoebe Kalib و Kelly Caprice" },
{ "id": 39, "type": "video", "title": "The toughest ANAL fucking in the shower RE UPLOAD ", "image": "https://c.top4top.io/p_3790iilnl1.jpg", "contentURL": "https://drive.google.com/file/d/1NCFBKbFc0riYLtke8cxWWrc4bNbqHW_w/view?usp=sharing", "labels": " Angelica Heaven,pornstars " },
{ "id": 40, "type": "video", "title": "They Taught Their Virgin Stepdaughter How to Get Her Perfect Ass Fucked ", "image": "https://g.top4top.io/p_3790mxvrw1.jpg", "contentURL": "https://drive.google.com/file/d/1tsFuspW70IKTwY93rvDLz6GTOffg1bjq/view?usp=sharing", "labels": "pornstars, Christie Stevens , Filthy Rich   " },
{ "id": 42, "type": "video", "title": "Threesome With Two Teen Step Sisters! Pulls Panties To The Side And Fucked 4K ", "image": "https://b.top4top.io/p_3790weybc1.jpg", "contentURL": "https://drive.google.com/file/d/1MbJdmeAfTveigGiFmxjpWCE2g628q4_-/view?usp=sharing", "labels": "pornstars, 18-25, Amateur, Babe, Brunette HD Porn , Hardcore, POV ,Step Fantasy, Threesome ,Verified Amateurs " },
{ "id": 43, "type": "video", "title": "Tinder Guy Used All My Holes For His Pleasure While Parents Were Away ", "image": "https://b.top4top.io/p_3790n1hu71.jpg", "contentURL": "https://drive.google.com/file/d/1_XyHfKKtaREvfTa-h3sXkA16oJT5_QIH/view?usp=sharing", "labels": "pornstars,18-25, 6 Amateur, Anal, Big Dick, Cumshot, HD Porn, POV, Verified Amateurs ,Verified Couples   " },
{ "id": 44, "type": "video", "title": "nesa video 42", "image": "", "contentURL": "", "labels": "pornstars, " }, 
{ "id": 44, "type": "video", "title": "nesa video 42", "image": "", "contentURL": "", "labels": "pornstars, " },  
{ "id": 44, "type": "video", "title": "nesa video 42", "image": "", "contentURL": "", "labels": "pornstars, " },  



 



//movie
{ "id": 36, "type": "movie", "title": "PURE TABOO Innocent Church Girl tries Anal for the first Time with Perverted Priest", "image": "https://j.top4top.io/p_3789o48gc1.jpg", "contentURL": "https://drive.google.com/file/d/1NIT3LaJkIKlaF0raYCUMTsXbvIZ1gb7x/view?usp=sharing", "labels": "", "duration": "00:00" },



//gif
{ "id": 24, "type": "gif", "title": "VR Porn Amazing Big Tit Teen Nekane Fucks POV in 3D on BaDoinkVR.com", "image": "https://c.top4top.io/p_3789t6ipb1.gif", "contentURL": "https://c.top4top.io/p_3789t6ipb1.gif", "labels": "gif", "duration": "00:00" },

//image
{ "id": 19, "type": "image", "title": "zaj video 19", "image": " ", "contentURL": "", "labels": "", "duration": "00:00" },
{ "id": 20, "type": "image", "title": "VR Porn Amazing Big Tit Teen Nekane Fucks POV in 3D on BaDoinkVR.com", "image": "https://up6.cc/2026/05/177902698111341.jpg", "contentURL": "", "labels": "badoinkvr, spanish, natural tits, virtual, reality, point of view, big boobs, young, brunette, vr samsung, playstation, 3d,  nekane ,vr porn step sister, doggy style", "duration": "00:00" },
{ "id": 21, "type": "image", "title": "zaj video 21", "image": "https://l.top4top.io/p_3789cjdgx1.jpg", "contentURL": "https://files.catbox.moe/9rjfd5.jpg","redirectUrl": "https://www.playvids.com/0fzcTfKE7ZA/vf/balls-and-wrist-deep-anal-threeway-with-gia-derza-and-paige-owens", "labels": "badoinkvr, spanish, natural tits, virtual, reality, point of view, big boobs, young, brunette, vr samsung, playstation, 3d,  nekane ,vr porn step sister, doggy style", "duration": "00:00" },
{ "id": 22, "type": "image", "title": "Petite Girls Lilu Moon Mina Cougar Or Kitten Moms Bang Teens Hairy Pussy Fucking Girl With Big Ass Hole  Lilu Moon-Mina Moon-Jordi El Nino Polla", "image": "https://up6.cc/2026/05/177902798820221.jpg", "contentURL": "https://files.catbox.moe/8saj01.jpg", "redirectUrl": "https://www.peekvids.com/v/petite-girls-lilu-moon-mina-cougar-or-kitten-moms-bang-teens-hairy-pussy-fucking-girl-with-big-ass-hole/44PAH0Vgg3R", "labels": "  blowjobs , natural tits, doggystyle, brunette, cowgirl, cumshot, small tits, threesome, anal, big ass, interview   ", "duration": "00:00" },
{ "id": 23, "type": "image", "title": "Paige Owens Gia Derza Ricky Johnson", "image": "https://files.catbox.moe/es69j7.jpg", "contentURL": "https://files.catbox.moe/es69j7.jpg", "redirectUrl": "https://www.playvids.com/0fzcTfKE7ZA/vf/balls-and-wrist-deep-anal-threeway-with-gia-derza-and-paige-owens", "labels": " blowjobs, natural tits, doggystyle, brunette, cowgirl, cumshot, small tits, threesome, anal, big ass, interview", "duration": "00:00" }, 
{ "id": 24, "type": "image", "title": "nesa video 5", "image": "https://h.top4top.io/p_3790ira7n10.jpg", "contentURL": "", "labels": "", "duration": "00:00" },
{ "id": 25, "type": "image", "title": "nesa video 5", "image": "https://f.top4top.io/p_37903ri1s8.gif", "contentURL": "https://f.top4top.io/p_37903ri1s8.gif", "labels": "", "duration": "00:00" },
{ "id": 26, "type": "image", "title": "nesa video 5", "image": "https://d.top4top.io/p_3790j1beu7.jpg", "contentURL": "", "labels": "", "duration": "00:00" },
{ "id": 27, "type": "image", "title": "nesa video 5", "image": "https://c.top4top.io/p_379066knm6.png", "contentURL": "", "labels": "", "duration": "00:00" },
{ "id": 28, "type": "image", "title": "nesa video 28", "image": "https://b.top4top.io/p_3790ggis35.png", "contentURL": "", "labels": "" },
{ "id": 29, "type": "image", "title": "nesa video 29", "image": "https://a.top4top.io/p_3790twn314.png", "contentURL": "", "labels": "" },
{ "id": 30, "type": "image", "title": "nesa video 30", "image": "https://j.top4top.io/p_3790h567m1.jpg", "contentURL": "", "labels": "" },
{ "id": 31, "type": "image", "title": "nesa video 31", "image": "https://d.top4top.io/p_3790oa8f91.png", "contentURL": "", "labels": "" },
{ "id": 32, "type": "image", "title": "nesa video 32", "image": "https://b.top4top.io/p_3790d0ky21.png", "contentURL": "", "labels": "" }
//{ "id": 33, "type": "image", "title": "nesa video 33", "image": "", "contentURL": "", "labels": "" },
//{ "id": 34, "type": "image", "title": "nesa video 34", "image": "", "contentURL": "", "labels": "" },
//{ "id": 35, "type": "image", "title": "nesa video 35", "image": "", "contentURL": "", "labels": "" }



],



  "nesa": [
    { "id": 2, "type": "video", "title": "nesa video 2", "image": " ", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 3, "type": "video", "title": "nesa video 3", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 4, "type": "video", "title": "nesa video 4", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 5, "type": "video", "title": "nesa video 5", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 6, "type": "video", "title": "nesa video 6", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 7, "type": "video", "title": "nesa video 7", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 8, "type": "video", "title": "nesa video 8", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 9, "type": "video", "title": "nesa video 9", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 10, "type": "video", "title": "nesa video 10", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 11, "type": "video", "title": "nesa video 11", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 12, "type": "video", "title": "nesa video 12", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 13, "type": "video", "title": "Kenzie Taylor  Kit Mercer Lana Sharapova are having a lesbian   ", "image": "https://up6.cc/2026/05/17790269811612.jpg", "contentURL": " https://drive.google.com/file/d/1_jN-2NkK14-Ix_RGkiA4hfT3U28gmaln/view?usp=sharing", "labels": "big boobs, big tits stepmom, hairy pussy, lesbian facesitting, lesbian threesome, lesbian tribbing, milf big tits, milf threesome, small boobs, stepdaughter, stepmom, big tits, cunnilingus, eating pussy, facesitting, lesbian, milf, old & young, threesome", "duration": "13:21" },
    { "id": 14, "type": "video", "title": "nesa video 14", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 15, "type": "video", "title": "nesa video 15", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 16, "type": "video", "title": "nesa video 16", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 17, "type": "video", "title": "nesa video 17", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 18, "type": "video", "title": "nesa video 18", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 19, "type": "video", "title": "nesa video 19", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 20, "type": "video", "title": "nesa video 20", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 21, "type": "video", "title": "nesa video 21", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 1, "type": "video", "title": "Epic Public Flashing Compilation ", "image": "https://c.top4top.io/p_3746q2xr80.jpg", "contentURL": "https://drive.google.com/file/d/1KECVpAXrNn21o01g88VQlZWokUn5wfuN/view?usp=drivesdk", "labels": "Compilation", "duration": "00:00" },


    { "id": 22, "type": "image", "title": "nesa video 22", "image": "https://f.top4top.io/p_3754qh5mz0.jpg", "contentURL": "https://f.top4top.io/p_3754qh5mz0.jpg",    "labels": "naked, pussy, ass, butt"},
    { "id": 23, "type": "image", "title": "nesa video 23", "image": "https://e.top4top.io/p_3753j5f462.jpg", "contentURL": "https://e.top4top.io/p_3753j5f462.jpg",    "labels": "naked, pussy, ass, butt" },
    { "id": 24, "type": "image", "title": "nesa video 24", "image": "https://f.top4top.io/p_3754zu7n06.jpeg", "contentURL": "https://f.top4top.io/p_3754zu7n06.jpeg",    "labels": "naked, pussy, ass, butt" },
    { "id": 26, "type": "image", "title": "nesa video 25", "image": "https://up6.cc/2026/05/17790269811612.jpg", "contentURL": "https://up6.cc/2026/05/17790269811612.jpg","redirectUrl": "https://www.tnaflix.com/amateur-porn/Kenzie-Taylor%2C-Kit-Mercer%2C-Lana-Sharapova-Good-Mom-Bad-Mother-%2805.12.2020%29/video6570900",   "labels": "naked, pussy, ass, butt"},

    { "id": 25, "type": "image", "title": "nesa video 25", "image": "https://l.top4top.io/p_3754oc8jl0.png", "contentURL": "https://l.top4top.io/p_3754oc8jl0.png",    "labels": "naked, pussy, ass, butt"},


  ],
  "haywan": [
    { "id": 1, "type": "video", "title": "haywan video 1", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 2, "type": "video", "title": "haywan video 2", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 3, "type": "video", "title": "haywan video 3", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 4, "type": "video", "title": "haywan video 4", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 5, "type": "video", "title": "haywan video 5", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 6, "type": "video", "title": "haywan video 6", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 7, "type": "video", "title": "haywan video 7", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 8, "type": "video", "title": "haywan video 8", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 9, "type": "video", "title": "haywan video 9", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 10, "type": "video", "title": "haywan video 10", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 11, "type": "video", "title": "haywan video 11", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 12, "type": "video", "title": "haywan video 12", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 13, "type": "video", "title": "haywan video 13", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 14, "type": "video", "title": "haywan video 14", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 15, "type": "video", "title": "haywan video 15", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 16, "type": "video", "title": "haywan video 16", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 17, "type": "video", "title": "haywan video 17", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 18, "type": "video", "title": "haywan video 18", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 19, "type": "video", "title": "haywan video 19", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 20, "type": "video", "title": "haywan video 20", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 21, "type": "video", "title": "haywan video 21", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 22, "type": "video", "title": "haywan video 22", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 23, "type": "video", "title": "haywan video 23", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 24, "type": "video", "title": "haywan video 24", "image": "", "contentURL": "", "labels": "", "duration": "00:00" },
    { "id": 25, "type": "video", "title": "haywan video 25", "image": "", "contentURL": "", "labels": "", "duration": "00:00" }
  ],
  "ime": [
    { "id": 1, "type": "video", "title": "ime anime 1", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 2, "type": "video", "title": "ime anime 2", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 3, "type": "video", "title": "ime anime 3", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 4, "type": "video", "title": "ime anime 4", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 5, "type": "video", "title": "ime anime 5", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 6, "type": "video", "title": "ime anime 6", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 7, "type": "video", "title": "ime anime 7", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 8, "type": "video", "title": "ime anime 8", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 9, "type": "video", "title": "ime anime 9", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 10, "type": "video", "title": "ime anime 10", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 11, "type": "video", "title": "ime anime 11", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 12, "type": "video", "title": "ime anime 12", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 13, "type": "video", "title": "ime anime 13", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 14, "type": "video", "title": "ime anime 14", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 15, "type": "video", "title": "ime anime 15", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 16, "type": "video", "title": "ime anime 16", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 17, "type": "video", "title": "ime anime 17", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 18, "type": "video", "title": "ime anime 18", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 19, "type": "video", "title": "ime anime 19", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 20, "type": "video", "title": "ime anime 20", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" },
    { "id": 21, "type": "image", "title": "ime anime 21", "image": "https://d.top4top.io/p_3754m7gav4.jpeg", "contentURL": "https://d.top4top.io/p_3754m7gav4.jpeg", "labels": "anime" },
    { "id": 22, "type": "image", "title": "ime anime 22", "image": "https://b.top4top.io/p_3754pp9ki2.jpeg", "contentURL": "https://b.top4top.io/p_3754pp9ki2.jpeg", "labels": "anime"},
    { "id": 23, "type": "image", "title": "ime anime 23", "image": "https://c.top4top.io/p_37548lmfr3.jpeg", "contentURL": "https://c.top4top.io/p_37548lmfr3.jpeg", "labels": "anime"},
    { "id": 24, "type": "image", "title": "ime anime 24", "image": "https://g.top4top.io/p_37541rhbo2.jpeg", "contentURL": "https://g.top4top.io/p_37541rhbo2.jpeg", "labels": "anime" },
    { "id": 25, "type": "image", "title": "ime anime 25", "image": "", "contentURL": "", "labels": "anime", "duration": "00:00" }
  ],
  "liveGirls": [
    { "id": 1, "type": "live", "title": "Live Channel 1", "image": "", "contentURL": "", "labels": "live, channel", "duration": "Live" },
    { "id": 2, "type": "live", "title": "Live Channel 2", "image": "", "contentURL": "", "labels": "live, channel", "duration": "Live" },
    { "id": 3, "type": "live", "title": "Live Channel 3", "image": "", "contentURL": "", "labels": "live, channel", "duration": "Live" }
  ],
  "liveChannels": [
    { "id": 1, "type": "live", "title": "ОХ-АХ HD", "image": "https://k.top4top.io/p_37567rat30.jpg", "contentURL": "http://oxax.tv/oh-ah.html", "labels": "live, channel", "duration": "Live" },
        { "id": 2, "type": "live", "title": "fap-tv-3", "image": "https://c.top4top.io/p_376232e0q0.jpg", "contentURL": "http://oxax.tv/fap-tv-3.html", "labels": "live, channel", "duration": "Live" },
    { "id": 3, "type": "live", "title": "brazzers-tv-online", "image":"https://j.top4top.io/p_3756oefsw0.png", "contentUR ": "https://adult-tv-channels.com/brazzers-tv-online/", "labels": "live, channel", "duration": "Live" },
        { "id": 4, "type": "live", "title": "fap-tv-4", "image":"https://up6.cc/2026/05/177885612466951.jpg", "contentUR ": "http://oxax.tv/fap-tv-4.html", "labels": "live, channel", "duration": "Live" },
    { "id": 5, "type": "live", "title": "fap-tv-2", "image": "https://f.top4top.io/p_3762tq3iq0.jpg", "contentURL": "http://oxax.tv/fap-tv-2.html", "labels": "live, channel", "duration": "Live" },
        { "id": 5, "type": "live", "title": "superone", "image": "https://up6.cc/2026/05/177895327895371.png", "contentURL": "http://oxax.tv/superone-hd.html", "labels": "live, channel", "duration": "Live" }

  ],
  
  "life": [
    {
      "id": 1,
      "type": "article",
      "title": "وضعية إندراني",
      "image": "https://l.top4top.io/p_3702uwnur1.png",
      "labels": "تعليمي",
      "blocks": [
        {
          "type": "text",
          "content": "<div style=\"direction: rtl; text-align: right; line-height: 1.8;\"><h2 style=\"color: #fb7185; margin-bottom: 15px;\">وضعية إندراني</h2><p style=\"margin-bottom: 15px;\">بواسطة رايتشل فوستر (9 فبراير 2022). يحب الأزواج وضعية إندراني لأنها مريحة وتسمح باختراق عميق جداً.</p><div style=\"margin: 20px 0;\"><img src=\"https://l.top4top.io/p_3702uwnur1.png\" style=\"width: 100%; max-width: 500px; border-radius: 15px;\" alt=\"صورة توضيحية\"></div><video controls style=\"width: 100%; max-width: 600px; border-radius: 15px;\"><source src=\"https://d.top4top.io/m_36805y6jr1.mp4\" type=\"video/mp4\"></video></div>"
        }
      ]
    },
    {
      "id": 2,
      "type": "article",
      "title": "نصائح للحياة الزوجية",
      "image": "https://l.top4top.io/p_3702uwnur1.png",
      "labels": "نصائح",
      "blocks": [
        {
          "type": "text",
          "content": "<div style=\"direction: rtl; text-align: right; line-height: 1.8;\"><h2 style=\"color: #fb7185; margin-bottom: 15px;\">نصائح ذهبية للحياة الزوجية</h2><ul style=\"margin-right: 20px; margin-bottom: 20px;\"><li>التواصل المستمر هو أساس العلاقة الناجحة</li><li>احترام المساحة الشخصية للطرف الآخر</li><li>المفاجآت الرومانسية تضفي جواً من السعادة</li></ul><div style=\"margin: 20px 0;\"><img src=\"https://l.top4top.io/p_3702uwnur1.png\" style=\"width: 100%; max-width: 500px; border-radius: 15px;\" alt=\"صورة نصائح\"></div></div>"
        }
      ]
    },
    {
      "id": 3,
      "type": "article",
      "title": "أفضل 10 وضعيات",
      "image": "https://l.top4top.io/p_3702uwnur1.png",
      "labels": "توب 10",
      "blocks": [
        {
          "type": "text",
          "content": "<div style=\"direction: rtl; text-align: right; line-height: 1.8;\"><h2 style=\"color: #fb7185; margin-bottom: 15px;\">أفضل 10 وضعيات للأزواج</h2><ol style=\"margin-right: 20px; margin-bottom: 20px;\"><li>وضعية إندراني</li><li>وضعية الملعقة</li><li>الوضعية التبشيرية</li><li>وضعية الفارسة</li><li>وضعية الكلبية</li></ol><video controls style=\"width: 100%; max-width: 600px; border-radius: 15px;\"><source src=\"https://d.top4top.io/m_36805y6jr1.mp4\" type=\"video/mp4\"></video></div>"
        }
      ]
    },
    {
      "id": 4,
      "type": "article",
      "title": "أسرار العلاقة الحميمة",
      "image": "https://l.top4top.io/p_3702uwnur1.png",
      "labels": "أسرار",
      "blocks": [
        {
          "type": "text",
          "content": "<div style=\"direction: rtl; text-align: right; line-height: 1.8;\"><h2 style=\"color: #fb7185; margin-bottom: 15px;\">أسرار العلاقة الحميمة</h2><p>اكتشف أسرار العلاقة الحميمة الناجحة...</p><img src=\"https://l.top4top.io/p_3702uwnur1.png\" style=\"width:100%; max-width:500px;\"></div>"
        }
      ]
    },
    {
      "id": 5,
      "type": "article",
      "title": "الرومانسية في الزواج",
      "image": "https://l.top4top.io/p_3702uwnur1.png",
      "labels": "رومانسية",
      "blocks": [
        {
          "type": "text",
          "content": "<div style=\"direction: rtl; text-align: right; line-height: 1.8;\"><h2 style=\"color: #fb7185; margin-bottom: 15px;\">كيف تحافظ على الرومانسية</h2><p>نصائح للحفاظ على الرومانسية في العلاقة الزوجية...</p><img src=\"https://l.top4top.io/p_3702uwnur1.png\" style=\"width:100%; max-width:500px;\"></div>"
        }
      ]
    }
  ]
}


// دالة لاستخراج الرابط الحقيقي من أي نص (مثل BBCode)
function extractRealUrl(raw) {
    if (!raw || typeof raw !== 'string') return raw;
    // أولاً: البحث عن [img] داخل النص (حتى لو كان داخل [url])
    let match = raw.match(/\[img\](.*?)\[\/img\]/i);
    if (match) return match[1];
    // ثانياً: البحث عن [url=...]
    match = raw.match(/\[url=(.+?)\](.*?)\[\/url\]/i);
    if (match) return match[1];
    // ثالثاً: البحث عن رابط مباشر
    match = raw.match(/https?:\/\/[^\s"']+/i);
    if (match) return match[0];
    return raw;
}
// دالة لتحويل رابط Google Drive إلى رابط تضمين صالح (preview)
function getGoogleDriveEmbedLink(url) {
    if (!url || typeof url !== 'string') return url;
    // استخراج معرف الملف من الرابط
    let match = url.match(/\/d\/(.+?)\/view/);
    if (match) {
        const fileId = match[1];
        return `https://drive.google.com/file/d/${fileId}/preview`;
    }
    match = url.match(/id=(.+?)(&|$)/);
    if (match) {
        const fileId = match[1];
        return `https://drive.google.com/file/d/${fileId}/preview`;
    }
    if (url.includes('drive.google.com/uc')) {
        match = url.match(/id=(.+?)(&|$)/);
        if (match) {
            return `https://drive.google.com/file/d/${match[1]}/preview`;
        }
    }
    return url;
}

// دالة لتحويل رابط Google Drive إلى رابط مباشر قابل للتضمين
function getGoogleDriveDirectLink(url) {
    if (!url || typeof url !== 'string') return url;
    const match = url.match(/\/d\/(.+?)\/view/);
    if (match) {
        return `https://drive.google.com/uc?export=download&id=${match[1]}`;
    }
    if (url.includes('drive.google.com/embed')) return url;
    if (url.includes('export=download')) return url;
    return url;
}

// دالة لتنظيف جميع روابط العنصر (الصورة والمحتوى)
function sanitizeItemUrls(item) {
    if (item.image) {
        item.image = extractRealUrl(item.image);
        if (item.image && item.image.includes('drive.google.com')) {
            item.image = getGoogleDriveEmbedLink(item.image);
        }
    }
    if (item.contentURL) {
        item.contentURL = extractRealUrl(item.contentURL);
        if (item.contentURL && item.contentURL.includes('drive.google.com')) {
            // 🔥 تحويل رابط Google Drive إلى رابط مباشر قابل للتشغيل
            const match = item.contentURL.match(/\/d\/(.+?)\/view/);
            if (match) {
                const fileId = match[1];
                item.contentURL = `https://drive.google.com/uc?export=download&id=${fileId}`;
            } else {
                item.contentURL = getGoogleDriveEmbedLink(item.contentURL);
            }
        }
    }
    return item;
}
const allCategories = [
    "Lesbian", "Asian", "MILF", "Doggystyle", "Ebony", "Gay", "Teen", "Repairman", 
    "Pizza Delivery", "Amateur", "Big Cock", "Softcore", "Nurse", "Handjob", 
    "Blowjob", "Stepsis", "Latina", "Redhead", "Anal", "Threesome", "Twink", 
    "Cheerleader", "Camgirl", "Porn Parody", "Virgin Girl", "Hentai", "Casting", 
    "Old+Young", "Cosplay", "Teacher", "Ladyboy", "Stepmom", "Interracial", "69", 
    "Gangbang", "Bisexual", "BBC", "Outdoor", "Stepbro", "Fingering", "Maid", 
    "Tattoo", "Feet", "Monster Cock", "Stepdad", "Orgy", "Femdom", "BBW", "Rimjob", 
    "Titty Fuck", "Rule 34", "Trans Woman", "Caught", "Twins", "Retro", "Free Use", 
    "Toys", "Creampie", "Secretary", "Gloryhole", "Reverse Gangbang", "Massage", 
    "Schoolgirl", "Public", "Lezdom", "Kissing", "Job Interview", "Doctor", "Nudism", 
    "DP", "Crossdressing", "Hitch Hiker", "Cheating", "Swimsuit", "Bear", "Stranger", 
    "Strap-On", "Machine", "Medical", "Fisting", "Masturbation", "Tomboy", "Cuckold", 
    "Tied Up", "Furry", "Indian", "Nun", "Sauna", "Glasses", "African", "Voyeur", 
    "Prison", "Babysitter", "Money", "Underwater", "Gaydom", "Piss", "Fondling", 
    "Vintage", "Granny", "Virgin Boy", "Bukkake", "Trans Man", "JOI", "Hairy", 
    "Bareback", "Spanking", "Reluctant", "Pregnant", "CBT", "Deepthroat", "Bull Dyke", 
    "SPH", "Ballbusting", "Shower", "Japanese Game Show", "Prostate Milking", "Slave", 
    "Edging", "ASMR", "Cruising", "Hotwife", "Catgirl", "Swingers", "Pegging", "Hijab", 
    "DILF", "Military", "Snowballing", "Arab", "Scat", "Feminization", "Hypnotized", 
    "Santa", "Loli", "Prostitute", "Drunk", "Premature Ejaculation", "Chastity", 
    "Sleeping", "Futanari", "Incest", "Homeless", "Triple Penetration", "Snuff Fantasy", 
    "Blood", "Chloroform", "Rape Fantasy (CNC)", "Crackhead", "Enema", "Torture", 
    "Twincest", "Gore", "Stepbro", "Trans",  "naked", "Shemale", "Compilation", "hardcore","blowjob"  , "creampie", "milf", "Pink Pussy"
];
// متغير لحفظ التصنيف المختار حالياً
let currentFilterTag = null;

// دالة لاستخراج التصنيفات من العنصر
function getItemTags(item) {
    if (item.labels) {
        if (typeof item.labels === 'string') {
            return item.labels.split(',').map(t => t.trim());
        }
        return [item.labels];
    }
    return [];
}

// استبدل دالة filterByTag بهذه النسخة
function filterByTag(tagName) {
    if (window.closePlayer) window.closePlayer();
    if (window.closeArticle) window.closeArticle();
    
    // تعيين التصنيف الحالي
    currentFilterTag = tagName;
    
    // إعادة تعيين وضع الملف الشخصي (إذا كان نشطاً)
    profileMode = null;
    
    // تطبيق الفلتر
    doSearch('');
    
    // تحديث عنوان الصفحة
    const catTitle = document.getElementById('catTitle');
    if (catTitle) catTitle.innerText = `#${tagName}`;
    
    // رفع الصفحة للأعلى
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

// تأكد من أن هذه الدالة موجودة وأنها تستخدم allCategories الصحيح
function renderCategoryBar() {
    const container = document.getElementById('topCategoryBar');
    if (!container) return;
    const randomTags = [...allCategories].sort(() => 0.5 - Math.random()).slice(0, 10);
    container.innerHTML = randomTags.map(tag => `
        <button onclick="filterByTag('${tag.replace(/'/g, "\\'")}')" class="bg-zinc-900 hover:bg-pink-600 text-white text-[11px] font-bold py-2 px-5 rounded-full border border-zinc-800 transition-all whitespace-nowrap shadow-lg">
            ${tag}
        </button>
    `).join('');
}
window.addEventListener('DOMContentLoaded', renderCategoryBar);
// حفظ موضع التمرير الحالي
function saveCurrentScrollPosition() {
    sessionStorage.setItem('savedScrollY', window.pageYOffset || document.documentElement.scrollTop);
}

// استعادة موضع التمرير
function restoreScrollPosition() {
    const savedY = sessionStorage.getItem('savedScrollY');
    if (savedY && !document.getElementById('playWindow').classList.contains('hidden')) {
        // إذا كان المشغل مفتوحاً لا نستعيد التمرير الآن
        return;
    }
    if (savedY) {
        window.scrollTo({ top: parseInt(savedY), behavior: 'auto' });
        sessionStorage.removeItem('savedScrollY');
    }
}

// توسيع saveState لحفظ currentPage و profileMode
const originalSaveState = saveState;
saveState = function() {
    sessionStorage.setItem('selectedType', currentType);
    sessionStorage.setItem('selectedCat', currentCat);
    sessionStorage.setItem('activeParent', activeParent || "");
    sessionStorage.setItem('currentPage', currentPage);
    if (profileMode) sessionStorage.setItem('profileMode', profileMode);
    else sessionStorage.removeItem('profileMode');
};
// بيانات أولية سريعة (تظهر فوراً)
let allDBs = {
  zaj: [],
  nesa: [], haywan: [], ime: [], liveGirls: [], liveChannels: [], life: []
};

// دالة لتحميل البيانات مع التخزين المؤقت
function loadData() {
  return new Promise((resolve, reject) => {
    // استخدام البيانات من data.js مباشرة
    if (window.allDBsData) {
      for (let key in window.allDBsData) {
        if (allDBs[key]) allDBs[key] = window.allDBsData[key];
        else allDBs[key] = window.allDBsData[key];
      }
      // تحديث الواجهة
      if (currentSourceKey && allDBs[currentSourceKey]) {
        dummyData = shuffleArray(allDBs[currentSourceKey]);
        renderAll();
        updateFooterStats();
      }
      renderMenu();
      renderCategoryBar();
      resolve();
    } else {
      // fallback: محاولة fetch إذا لم توجد البيانات (للخادم)
      fetch('data.json')
        .then(response => response.json())
        .then(data => {
          for (let key in data) allDBs[key] = data[key];
          if (currentSourceKey && allDBs[currentSourceKey]) {
            dummyData = shuffleArray(allDBs[currentSourceKey]);
            renderAll();
            updateFooterStats();
          }
          renderMenu();
          renderCategoryBar();
          resolve();
        })
        .catch(reject);
    }
  });
}
// ================ الإعلانات ================
const siteAds = {
    networkAds: [ 
        { type: 'video', url: "https://e.top4top.io/m_3790fvvfu1.mp4 ", link: "https://example.com", title: "إعلان فيديو 1", description: "شاهد العرض الآن" },
        { type: 'video', url: "https://l.top4top.io/m_379011vop1.mp4 ", link: "https://example2.com", title: "إعلان GIF", description: "عرض محدود" },
        { type: 'video', url: "https://i.top4top.io/m_37903nnp21.mp4", link: "https://example3.com", title: "إعلان صورة 1", description: "خصم 50%" }
    ],
    sidebarAds: [
        { url: "https://i.postimg.cc/zfSyrPnH/hq720.jpg", link: "https://example1.com", type: "image" },
        { url: "https://d.top4top.io/m_36805y6jr1.mp4", link: "https://example2.com", type: "video" }
    ],
    paginationLargeAds: [
        { image: "https://i.postimg.cc/zfSyrPnH/hq720.jpg", link: "https://example1.com", title: "عرض خاص", isPortrait: false },
        { image: "https://b.top4top.io/p_3680ejx641.jpg", link: "https://example2.com", title: "عروض حصرية", isPortrait: false }
    ],
    
    paginationSquareAds: [
        { image: "https://h.top4top.io/p_3753dyy8t2.gif", link: "https://example1.com", title: "إعلان 1", isPortrait: false },
         { video: "https://e.top4top.io/m_37536b5gy0.mp4", link: "https://example1.com", title: "إعلان 2", isPortrait: false },
                  { image: "https://k.top4top.io/p_3762b5g6a0.gif", link: "https://example1.com", title: "إعلان 2", isPortrait: false },
        { video: "https://k.top4top.io/m_3747dk0rw0.mp4", link: "https://example2.com", title: "إعلان 2", isPortrait: false }
    ],
    
 // أضف هذا الكود الجديد للإعلانات المستطيلة الرفيعة
    horizontalRectAds: [
    { video: "https://k.top4top.io/m_3747dk0rw0.mp4", link: "https://example1.com", title: "عرض حصري 1" },
    { video: "https://g.top4top.io/m_3753hq7ei1.mp4", link: "https://example2.com", title: "عرض حصري 2" },
    { video: "https://g.top4top.io/m_3753hq7ei1.mp4", link: "https://example2.com", title: "عرض حصري 2" },
        { image: "https://f.top4top.io/p_3762zgfvm0.png", link: "https://example2.com", title: "عرض حصري 2" },
        { video: "https://d.top4top.io/m_37548wm9l0.mp4", link: "https://example2.com", title: "عرض حصري 2" },
                { image: "https://g.top4top.io/p_3790ytbej1.png", link: "https://example2.com", title: "عرض حصري 2" },
    { video: "https://b.top4top.io/m_3754wr5fy0.mp4", link: "https://example3.com", title: "عرض حصري 3" }
]


};

// Cache لتثبيت الإعلانات لكل قسم (لا تتغير أثناء نفس الجلسة)
let adsCache = {
    networkAd: null,           // إعلان الشبكة الواحد المضمن بين البطاقات
    sidebarAds: [],            // مصفوفة الإعلانات الجانبية (4 إعلانات)
    paginationLargeAds: [],    // إعلانات الترقيم الكبيرة (غير مستخدمة حالياً)
    paginationSquareAds: [],   // إعلانات المربعات تحت الترقيم (3 إعلانات)
    horizontalRectAd: null     // الإعلان الأفقي للهواتف
};

// دالة لإعادة تعيين الـ Cache عند تغيير القسم
function resetAdsCache() {
    adsCache = {
        networkAd: null,
        sidebarAds: [],
        paginationLargeAds: [],
        paginationSquareAds: [],
        horizontalRectAd: null
    };
}
// دالة لتهيئة الإعلانات الجانبية للقسم الحالي (4 إعلانات ثابتة)
function initSidebarAdsForCurrentSection() {
    if (adsCache.sidebarAds.length === 0) {
        // نختار 4 إعلانات عشوائية من siteAds.sidebarAds
        const shuffled = [...siteAds.sidebarAds].sort(() => 0.5 - Math.random());
        adsCache.sidebarAds = shuffled.slice(0, 4);
    }
    return adsCache.sidebarAds;
}
// ================ المتغيرات العامة ================
var tempRegistration = null;
var generatedVerificationCode = null;
var lastActivityTime = new Date().getTime();
var userIdle = false;
var pageVisible = true;
var ageVerificationEnabled = true;
var favorites = JSON.parse(localStorage.getItem('myFavs')) || [];
var likes = JSON.parse(localStorage.getItem('myLikes')) || [];
var dislikes = JSON.parse(localStorage.getItem('myDislikes')) || [];
var watchLater = JSON.parse(localStorage.getItem('myWatchLater')) || [];
var likesCount = JSON.parse(localStorage.getItem('likesCount')) || {};
var currentSourceKey = localStorage.getItem('selectedSource') || 'zaj';
var currentSourceName = localStorage.getItem('selectedSourceName') || 'الزواج';
var currentType = localStorage.getItem('selectedType') || 'video';
var currentCat = localStorage.getItem('selectedCat') || 'all content';
var savedParent = localStorage.getItem('activeParent');
var activeParent = (savedParent === "null" || savedParent === null || savedParent === undefined) ? null : savedParent;
var currentPlayingId = localStorage.getItem('currentPlayingId') || null;
var dummyData = (typeof allDBs !== 'undefined' && allDBs[currentSourceKey]) ? [...allDBs[currentSourceKey]] : [];
var currentUser = JSON.parse(localStorage.getItem('currentUser')) || null;
var usersDB = JSON.parse(localStorage.getItem('usersDB')) || [];
var currentLang = localStorage.getItem('siteLang') || 'en';
var itemsPerPage = 27;
var currentPage = 1;
var profileMode = null;
var scrollPosition = 0;
var isLoginMode = true;
window.filterByTag = filterByTag;


// ================ Firebase (معطل مؤقتاً) ================
const firebaseConfig = {
    apiKey: "AIzaSyBk2NxXkXkXkXkXkXkXkXkXkXkXkXk",
    authDomain: "my-platform-2026.firebaseapp.com",
    projectId: "my-platform-2026",
    storageBucket: "my-platform-2026.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abc123def4567890"
};

try {
    if (typeof firebase !== 'undefined' && !firebase.apps.length) {
        firebase.initializeApp(firebaseConfig);
    }
} catch (e) {
    console.log("Firebase غير متاح");
}

const auth = typeof firebase !== 'undefined' ? firebase.auth() : null;
const provider = auth ? new firebase.auth.GoogleAuthProvider() : null;

// ================ دوال جلب الإعلانات ================
function getRandomNetworkAd() {
    // إذا كان هناك إعلان مخزن للقسم الحالي، نعيده
    if (adsCache.networkAd) return adsCache.networkAd;
    
    // وإلا نختار واحداً عشوائياً ونخزنه
    const ad = siteAds.networkAds[Math.floor(Math.random() * siteAds.networkAds.length)];
    adsCache.networkAd = ad;
    return ad;
}

function getRandomSidebarAd() {
    return siteAds.sidebarAds[Math.floor(Math.random() * siteAds.sidebarAds.length)];
}

function getRandomPaginationLargeAd() {
    return siteAds.paginationLargeAds[Math.floor(Math.random() * siteAds.paginationLargeAds.length)];
}

function getRandomPaginationSquareAds(count = 3) {
    // إذا كانت الإعلانات مخزنة مسبقاً للقسم الحالي، نعيدها
    if (adsCache.paginationSquareAds.length > 0) {
        return adsCache.paginationSquareAds.slice(0, count);
    }
    
    // وإلا نختار عشوائياً عدداً من الإعلانات (عادة 3) ونخزنها
    const shuffled = [...siteAds.paginationSquareAds].sort(() => 0.5 - Math.random());
    const selected = shuffled.slice(0, count);
    adsCache.paginationSquareAds = selected;
    return selected;
}

function getRandomAd() {
    return getRandomNetworkAd();
}

// ================ الترجمة ================
const translations = {
    en: {
        sectionNames: {
            zaj: "Straight",
            nesa: "Lesbian",
            haywan: "Trans",
            liveGirls: "Live Girls",
            liveChannels: "Live Channels Porn",
            life: "Sex Positions",
            movies: "Movies"
        },
        sectionTitles: {
            zaj: "Hot porn videos",
            nesa: "Lesbian Porn Videos",
            haywan: "Transgender Porn Videos",
            liveGirls: "Live Girls - Streaming Now",
            liveChannels: "Live Porn Channels",
            life: "Sex Positions - Educational Guides",
            movies: "Full Movies"
        },
        subTitles: {
            zaj: "Watch the hottest straight porn videos in HD quality",
            nesa: "Best videos - Girl having sex with another woman",
            haywan: "Amazing transgender porn videos - TS performers",
            liveGirls: "Watch live girls streaming in real time",
            liveChannels: "24/7 live porn channels",
            life: "Learn new sex positions with our educational guides",
            movies: "Watch full-length movies in high quality"
        },
        searchPlaceholder: "Search for video...",
        profileSaved: "My Saved Profile:",
        btnFav: "❤ Favorites",
        btnLike: "👍 Likes",
        btnWatchLater: "⏱️ Watch Later",
        videosCount: "📹 0 Videos",
        imagesCount: "🖼️ 0 Images",
        articlesCount: "📄 0 Articles",
        videosMenu: "Videos",
        staticImages: "Image",
        animatedImages: "Gif",
        allContentMenu: "All Content",
        close: "Close ✕",
        saved: "Saved",
        addToFav: "Add to Favorites",
        removeFromFav: "Remove from Favorites",
        addToWatchLater: "Watch Later",
        removeFromWatchLater: "Remove from Watch Later",
        like: "Like",
        liked: "Liked",
        dislike: "Dislike",
        disliked: "Disliked",
        share: "Share",
        copied: "Copied",
        backToContent: "← Back to Content",
        relatedTopics: "Related Topics",
        welcome: "Welcome",
        logout: "Logout",
        loginButton: "🔐 Login",
        leftColumnTitle: "Videos, images and articles are shared on this site via the links below",
        link1: "YouTube",
        link2: "Facebook",
        link3: "Twitter",
        link4: "Instagram",
        link5: "TikTok",
        aiHelpText: "This website was created with the help of",
        quickLinks: "Quick Links",
        support: "Support",
        videos: "Videos",
        images: "Images",
        articles: "Articles",
        popular: "Popular",
        privacyPolicy: "Privacy Policy",
        contactUs: "Contact Us",
        aboutUs: "About Us",
        termsOfService: "Terms of Service",
        footerRights: "All rights reserved © 2024 porniq.com",
        footerDesc: "Porniq provides you with unlimited free porn videos with the hottest pornstars. Enjoy the largest amateur porn community on the net.",
        favTitle: "Favorites",
        likeTitle: "Liked Content",
        watchLaterTitle: "Watch Later"
    },
    ar: {
        sectionNames: {
            zaj: "Straight",
            nesa: "Lesbian",
            haywan: "Trans",
            liveGirls: "Live Girls",
            liveChannels: "Live Channels Porn",
            life: "Sex Positions",
            movies: "Movies"
        },
        sectionTitles: {
            zaj: "Hot porn videos",
            nesa: "Lesbian Porn Videos",
            haywan: "Transgender Porn Videos",
            liveGirls: "Live Girls - Streaming Now",
            liveChannels: "Live Porn Channels",
            life: "Sex Positions - Educational Guides",
            movies: "Full Movies"
        },
        subTitles: {
            zaj: "Watch the hottest straight porn videos in HD quality",
            nesa: "Best videos - Girl having sex with another woman",
            haywan: "Amazing transgender porn videos - TS performers",
            liveGirls: "Watch live girls streaming in real time",
            liveChannels: "24/7 live porn channels",
            life: "Learn new sex positions with our educational guides",
            movies: "Watch full-length movies in high quality"
        },
        searchPlaceholder: "Search for video...",
        profileSaved: "My Saved Profile:",
        btnFav: "❤ Favorites",
        btnLike: "👍 Likes",
        btnWatchLater: "⏱️ Watch Later",
        videosCount: "📹 0 Videos",
        imagesCount: "🖼️ 0 Images",
        articlesCount: "📄 0 Articles",
        videosMenu: "Videos",
        staticImages: "Image",
        animatedImages: "Gif",
        allContentMenu: "All Content",
        close: "Close ✕",
        saved: "Saved",
        addToFav: "Add to Favorites",
        removeFromFav: "Remove from Favorites",
        addToWatchLater: "Watch Later",
        removeFromWatchLater: "Remove from Watch Later",
        like: "Like",
        liked: "Liked",
        dislike: "Dislike",
        disliked: "Disliked",
        share: "Share",
        copied: "Copied",
        backToContent: "← Back to Content",
        relatedTopics: "Related Topics",
        welcome: "Welcome",
        logout: "Logout",
        loginButton: "🔐 Login",
        leftColumnTitle: "Videos, images and articles are shared on this site via the links below",
        link1: "YouTube",
        link2: "Facebook",
        link3: "Twitter",
        link4: "Instagram",
        link5: "TikTok",
        aiHelpText: "This website was created with the help of",
        quickLinks: "Quick Links",
        support: "Support",
        videos: "Videos",
        images: "Images",
        articles: "Articles",
        popular: "Popular",
        privacyPolicy: "Privacy Policy",
        contactUs: "Contact Us",
        aboutUs: "About Us",
        termsOfService: "Terms of Service",
        footerRights: "All rights reserved © 2024 porniq.com",
        footerDesc: "Porniq provides you with unlimited free porn videos with the hottest pornstars. Enjoy the largest amateur porn community on the net.",
        favTitle: "Favorites",
        likeTitle: "Liked Content",
        watchLaterTitle: "Watch Later"
    }
};

function t(key) {
    return translations[currentLang] && translations[currentLang][key] ? translations[currentLang][key] : key;
}

// ================ دوال الترجمة واللغة ================
function toggleLanguage() {
    currentLang = currentLang === 'en' ? 'ar' : 'en';
    localStorage.setItem('siteLang', currentLang);
    document.documentElement.dir = currentLang === 'ar' ? 'rtl' : 'ltr';
    document.documentElement.lang = currentLang;
    document.getElementById('langBtn').innerHTML = currentLang === 'ar' ? '🌐 AR' : '🌐 EN';
    updateAllTexts();
    renderMenu();
    updateUserUI();
    updateSubTitle();
}

function updateAllTexts() {
    const sectionHeaders = document.querySelectorAll('.main-section-header');
    const sectionNames = {
        zaj: currentLang === 'ar' ? 'مستقيم ⚤' : 'Straight',
        nesa: currentLang === 'ar' ? 'سحاقيات ⚢' : 'Lesbian',
        haywan: currentLang === 'ar' ? 'متحولين جنسياً ⚧' : 'Trans',
        life: currentLang === 'ar' ? 'وضعيات جنسية' : 'Sex Positions'
    };
    sectionHeaders.forEach(header => {
        const leftContent = header.querySelector('div:first-child');
        if (leftContent) {
            const span = leftContent.querySelector('span:last-child');
            if (span) {
                const parentContainer = header.closest('.main-section-container');
                if (parentContainer) {
                    const submenuDiv = parentContainer.querySelector('[class^="section-submenu-"]');
                    if (submenuDiv) {
                        const className = submenuDiv.className;
                        if (className.includes('zaj')) span.innerText = sectionNames.zaj;
                        else if (className.includes('nesa')) span.innerText = sectionNames.nesa;
                        else if (className.includes('haywan')) span.innerText = sectionNames.haywan;
                        else if (className.includes('life')) span.innerText = sectionNames.life;
                    }
                }
            }
        }
    });
    const searchBox = document.getElementById('searchBox');
    if (searchBox) searchBox.placeholder = t('searchPlaceholder');
    const profileTitle = document.querySelector('.profile-card .text-\\[10px\\]');
    if (profileTitle) profileTitle.innerText = t('profileSaved');
    const btnFav = document.getElementById('btnFav');
    if (btnFav) btnFav.innerText = t('btnFav');
    const btnLike = document.getElementById('btnLike');
    if (btnLike) btnLike.innerText = t('btnLike');
    const btnWatchLater = document.getElementById('btnWatchLater');
    if (btnWatchLater) btnWatchLater.innerText = t('btnWatchLater');
    updateStats();
    const catTitle = document.getElementById('catTitle');
    if (catTitle) {
        if (profileMode) {
            if (profileMode === 'fav') catTitle.innerText = t('favTitle');
            else if (profileMode === 'like') catTitle.innerText = t('likeTitle');
            else if (profileMode === 'watchlater') catTitle.innerText = t('watchLaterTitle');
        } else {
            catTitle.innerText = t('sectionTitles')[currentSourceKey] || t('sectionNames')[currentSourceKey];
        }
    }
    const activeName = document.getElementById('activeName');
    if (activeName && currentSourceKey) {
        activeName.innerHTML = t('sectionNames')[currentSourceKey];
    }
}

function updateSubTitle() {
    const subTitle = document.getElementById('sectionSubtitle');
    if (!subTitle) return;
    if (currentSourceKey && translations[currentLang] && translations[currentLang].subTitles) {
        subTitle.innerText = translations[currentLang].subTitles[currentSourceKey] || '';
    } else {
        subTitle.innerText = '';
    }
}

// ================ دوال المستخدم والتسجيل ================
function updateUserUI() {
    const nav = document.querySelector('nav .flex.items-center.gap-4');
    if (!nav) return;
    const existingUserSection = document.getElementById('userSection');
    if (existingUserSection) existingUserSection.remove();
    const userSection = document.createElement('div');
    userSection.id = 'userSection';
    userSection.className = 'flex items-center';
    if (currentUser) {
        let avatarHtml = '';
        if (currentUser.photoURL) {
            avatarHtml = `<img src="${currentUser.photoURL}" class="w-8 h-8 rounded-full border-2 border-pink-500">`;
        } else {
            const firstLetter = currentUser.username ? currentUser.username.charAt(0).toUpperCase() : 'U';
            avatarHtml = `<div class="w-8 h-8 rounded-full bg-pink-600 flex items-center justify-center text-white font-bold">${firstLetter}</div>`;
        }
        userSection.innerHTML = `
            <div class="flex items-center gap-3 bg-gradient-to-r from-zinc-800 to-zinc-900 px-4 py-1.5 rounded-full border border-pink-500/30 shadow-lg">
                ${avatarHtml}
                <div class="flex flex-col">
                    <span class="text-xs text-white/60">Welcome</span>
                    <span class="text-sm font-bold text-pink-500">${currentUser.username || 'User'}</span>
                </div>
                <button onclick="logout()" class="text-xs bg-red-600/80 hover:bg-red-600 px-3 py-1.5 rounded-full text-white font-bold transition-all mr-2">
                    Logout
                </button>
            </div>
        `;
        ageVerificationEnabled = false;
        const ageModal = document.getElementById('ageVerifyModal');
        if (ageModal) ageModal.style.display = 'none';
    } else {
        userSection.innerHTML = `
            <button onclick="showAuthModal()" class="bg-gradient-to-r from-pink-600 to-purple-600 hover:from-pink-500 hover:to-purple-500 text-white px-3 py-1.5 rounded-full text-xs font-bold transition-all flex items-center gap-1.5 shadow-md shadow-pink-600/30">
                🔐 Login
            </button>
        `;
        ageVerificationEnabled = true;
    }
    nav.appendChild(userSection);
}

function showAuthModal() {
    const existingModal = document.getElementById('authModal');
    if (existingModal) existingModal.remove();
    const modal = document.createElement('div');
    modal.id = 'authModal';
    modal.style.cssText = `
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.96);
        z-index: 100000000;
        display: flex;
        align-items: center;
        justify-content: center;
    `;
    modal.style.direction = currentLang === 'ar' ? 'rtl' : 'ltr';
    modal.innerHTML = `
        <div style="width: 260px; max-width: 85%; background: #0a0a0a; border-radius: 20px; border: 1px solid #be185d; padding: 16px 14px; box-shadow: 0 20px 40px rgba(0,0,0,0.5);">
            <div style="text-align: center; margin-bottom: 10px;">
                <h2 id="authTitle" style="font-size: 18px; font-weight: bold; color: white; margin: 0;">Login</h2>
                <div style="width: 35px; height: 2px; background: #be185d; margin: 6px auto 0;"></div>
            </div>
            <div id="authError" style="background: rgba(220,38,38,0.2); border: 1px solid #ef4444; color: #fecaca; padding: 6px; border-radius: 8px; font-size: 10px; margin-bottom: 10px; display: none;"></div>
            <div id="loginFields">
                <div style="margin-bottom: 10px;">
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">👤 Username</label>
                    <input type="text" id="loginUsername" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; font-size: 12px; box-sizing: border-box;" placeholder="Username">
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">🔑 Password</label>
                    <input type="password" id="loginPassword" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; font-size: 12px; box-sizing: border-box;" placeholder="********">
                </div>
            </div>
            <div id="registerFields" style="display: none;">
                <div style="margin-bottom: 10px;">
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">👤 Username</label>
                    <input type="text" id="regUsername" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; font-size: 12px; box-sizing: border-box;" placeholder="Username">
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">📧 Email</label>
                    <input type="email" id="regEmail" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; font-size: 12px; box-sizing: border-box;" placeholder="Email">
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">🔑 Password</label>
                    <input type="password" id="regPassword" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; font-size: 12px; box-sizing: border-box;" placeholder="Password">
                </div>
                <div style="margin-bottom: 10px;">
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">✅ Confirm</label>
                    <input type="password" id="regConfirmPassword" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; font-size: 12px; box-sizing: border-box;" placeholder="Confirm">
                </div>
            </div>
            <div id="verificationFields" style="display: none;">
                <div style="text-align: center; color: #aaa; font-size: 11px; margin-bottom: 10px;">Verification code sent to your email</div>
                <div>
                    <label style="display: block; color: #aaa; font-size: 10px; margin-bottom: 4px;">🔢 4-digit code</label>
                    <input type="text" id="verificationCode" maxlength="4" style="width: 100%; background: #1a1a1a; border: 1px solid #333; border-radius: 10px; padding: 8px 10px; color: white; text-align: center; font-size: 16px; letter-spacing: 4px; box-sizing: border-box;" placeholder="____">
                </div>
            </div>
            <div id="loginActions" style="display: flex; gap: 8px; margin-top: 12px;">
                <button onclick="handleLogin()" style="flex: 1; background: #be185d; color: white; border: none; border-radius: 10px; padding: 8px; font-size: 12px; font-weight: bold; cursor: pointer;">Login</button>
                <button onclick="closeAuthModal()" style="flex: 1; background: #2a2a2a; color: white; border: none; border-radius: 10px; padding: 8px; font-size: 12px; font-weight: bold; cursor: pointer;">Cancel</button>
            </div>
            <div id="registerActions" style="display: none; gap: 8px; margin-top: 12px;">
                <button onclick="handleRegister()" style="flex: 1; background: #be185d; color: white; border: none; border-radius: 10px; padding: 8px; font-size: 12px; font-weight: bold; cursor: pointer;">Send Code</button>
                <button onclick="closeAuthModal()" style="flex: 1; background: #2a2a2a; color: white; border: none; border-radius: 10px; padding: 8px; font-size: 12px; font-weight: bold; cursor: pointer;">Cancel</button>
            </div>
            <div id="verificationActions" style="display: none; gap: 8px; margin-top: 12px;">
                <button onclick="verifyCode()" style="flex: 1; background: #22c55e; color: white; border: none; border-radius: 10px; padding: 8px; font-size: 12px; font-weight: bold; cursor: pointer;">Verify</button>
                <button onclick="resetToRegister()" style="flex: 1; background: #2a2a2a; color: white; border: none; border-radius: 10px; padding: 8px; font-size: 12px; font-weight: bold; cursor: pointer;">Back</button>
            </div>
            ${auth ? `
            <div style="position: relative; margin: 12px 0;">
                <div style="position: absolute; inset: 0; display: flex; align-items: center;"><div style="width: 100%; border-top: 1px solid #333;"></div></div>
                <div style="position: relative; display: flex; justify-content: center;"><span style="background: #0a0a0a; padding: 0 8px; color: #666; font-size: 9px;">OR</span></div>
            </div>
            <button onclick="googleSignIn()" style="width: 100%; background: white; color: #333; border: none; border-radius: 10px; padding: 8px; font-size: 11px; font-weight: bold; cursor: pointer; display: flex; align-items: center; justify-content: center; gap: 6px;">
                <svg style="width: 14px; height: 14px;" viewBox="0 0 24 24"><path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/><path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/><path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/><path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/></svg>
                Google
            </button>
            ` : ''}
            <div style="text-align: center; margin-top: 12px;">
                <span id="toggleAuthText" style="color: #666; font-size: 9px;">Don't have an account?</span>
                <button onclick="toggleAuthMode()" id="toggleAuthBtn" style="background: none; border: none; color: #ec489a; font-size: 9px; font-weight: bold; cursor: pointer; display: inline; padding: 0;"> Sign up</button>
            </div>
            <div style="text-align: center; margin-top: 8px; font-size: 7px; color: #444;">By registering, you agree to our Terms</div>
        </div>
    `;
    document.body.appendChild(modal);
    document.body.style.overflow = 'hidden';
}

function closeAuthModal() {
    const modal = document.getElementById('authModal');
    if (modal) modal.remove();
    tempRegistration = null;
    generatedVerificationCode = null;
    document.body.style.overflow = '';
}

function toggleAuthMode() {
    isLoginMode = !isLoginMode;
    const authTitle = document.getElementById('authTitle');
    const toggleBtn = document.getElementById('toggleAuthBtn');
    const toggleText = document.getElementById('toggleAuthText');
    const loginFields = document.getElementById('loginFields');
    const loginActions = document.getElementById('loginActions');
    const registerFields = document.getElementById('registerFields');
    const registerActions = document.getElementById('registerActions');
    const verificationFields = document.getElementById('verificationFields');
    const verificationActions = document.getElementById('verificationActions');
    if (authTitle) authTitle.innerText = isLoginMode ? 'Login' : 'Create Account';
    if (toggleBtn) toggleBtn.innerText = isLoginMode ? 'Sign up' : 'Login';
    if (toggleText) toggleText.innerText = isLoginMode ? "Don't have an account?" : 'Already have an account?';
    if (loginFields) loginFields.style.display = isLoginMode ? 'block' : 'none';
    if (loginActions) loginActions.style.display = isLoginMode ? 'flex' : 'none';
    if (registerFields) registerFields.style.display = isLoginMode ? 'none' : 'block';
    if (registerActions) registerActions.style.display = isLoginMode ? 'none' : 'flex';
    if (verificationFields) verificationFields.style.display = 'none';
    if (verificationActions) verificationActions.style.display = 'none';
}

function handleLogin() {
    const username = document.getElementById('loginUsername').value.trim();
    const password = document.getElementById('loginPassword').value.trim();
    const errorDiv = document.getElementById('authError');
    errorDiv.classList.add('hidden');
    if (!username || !password) {
        showAuthError('❌ جميع الحقول مطلوبة');
        return;
    }
    const user = usersDB.find(u => u.username === username && u.password === password);
    if (user) {
        currentUser = { username: user.username, email: user.email };
        localStorage.setItem('currentUser', JSON.stringify(currentUser));
        closeAuthModal();
        updateUserUI();
        alert(`👋 مرحباً بعودتك ${username}!`);
    } else {
        showAuthError('❌ اسم المستخدم أو كلمة المرور غير صحيحة');
    }
}

function handleRegister() {
    const username = document.getElementById('regUsername').value.trim();
    const email = document.getElementById('regEmail').value.trim();
    const password = document.getElementById('regPassword').value.trim();
    const confirm = document.getElementById('regConfirmPassword').value.trim();
    const errorDiv = document.getElementById('authError');
    errorDiv.classList.add('hidden');
    if (!username || !email || !password || !confirm) {
        showAuthError('❌ جميع الحقول مطلوبة');
        return;
    }
    if (!email.includes('@') || !email.includes('.')) {
        showAuthError('❌ البريد الإلكتروني غير صحيح');
        return;
    }
    if (password !== confirm) {
        showAuthError('❌ كلمة المرور غير متطابقة');
        return;
    }
    if (password.length < 6) {
        showAuthError('❌ كلمة المرور يجب أن تكون 6 أحرف على الأقل');
        return;
    }
    if (usersDB.find(u => u.username === username)) {
        showAuthError('❌ اسم المستخدم موجود بالفعل');
        return;
    }
    if (usersDB.find(u => u.email === email)) {
        showAuthError('❌ البريد الإلكتروني مستخدم بالفعل');
        return;
    }
    tempRegistration = { username, email, password };
    generatedVerificationCode = generateVerificationCode();
    alert(`🔐 رمز التحقق الخاص بك هو: ${generatedVerificationCode}`);
    document.getElementById('registerFields').classList.add('hidden');
    document.getElementById('registerActions').classList.add('hidden');
    document.getElementById('verificationFields').classList.remove('hidden');
    document.getElementById('verificationActions').classList.remove('hidden');
    document.getElementById('authTitle').innerText = 'تأكيد البريد الإلكتروني';
}

function verifyCode() {
    const code = document.getElementById('verificationCode').value.trim();
    const errorDiv = document.getElementById('authError');
    errorDiv.classList.add('hidden');
    if (!code || code.length !== 4 || isNaN(code)) {
        showAuthError('❌ يرجى إدخال رمز تحقق صحيح مكون من 4 أرقام');
        return;
    }
    if (code !== generatedVerificationCode) {
        showAuthError('❌ رمز التحقق غير صحيح');
        return;
    }
    if (!tempRegistration) {
        showAuthError('❌ انتهت صلاحية الجلسة، يرجى المحاولة مجدداً');
        resetToRegister();
        return;
    }
    usersDB.push({
        username: tempRegistration.username,
        email: tempRegistration.email,
        password: tempRegistration.password
    });
    localStorage.setItem('usersDB', JSON.stringify(usersDB));
    currentUser = { username: tempRegistration.username, email: tempRegistration.email };
    localStorage.setItem('currentUser', JSON.stringify(currentUser));
    tempRegistration = null;
    generatedVerificationCode = null;
    closeAuthModal();
    updateUserUI();
    alert(`🎉 مرحباً بك ${currentUser.username}، تم إنشاء حسابك بنجاح!`);
}

function resetToRegister() {
    document.getElementById('verificationFields').classList.add('hidden');
    document.getElementById('verificationActions').classList.add('hidden');
    document.getElementById('registerFields').classList.remove('hidden');
    document.getElementById('registerActions').classList.remove('hidden');
    document.getElementById('authTitle').innerText = 'إنشاء حساب جديد';
    tempRegistration = null;
    generatedVerificationCode = null;
}

function showAuthError(msg) {
    const err = document.getElementById('authError');
    err.innerText = msg;
    err.classList.remove('hidden');
}

function logout() {
    currentUser = null;
    localStorage.removeItem('currentUser');
    updateUserUI();
    alert('👋 تم تسجيل الخروج بنجاح');
    location.reload();
}

function googleSignIn() {
    if (!auth || !provider) {
        alert('خدمة Google غير متاحة حالياً');
        return;
    }
    auth.signInWithPopup(provider)
        .then((result) => {
            const user = result.user;
            let existingUser = usersDB.find(u => u.email === user.email);
            if (!existingUser) {
                const newUser = {
                    username: user.displayName || user.email.split('@')[0],
                    email: user.email,
                    password: 'google-auth-' + Math.random().toString(36),
                    provider: 'google',
                    photoURL: user.photoURL
                };
                usersDB.push(newUser);
                localStorage.setItem('usersDB', JSON.stringify(usersDB));
                existingUser = newUser;
            }
            currentUser = {
                username: existingUser.username,
                email: existingUser.email,
                photoURL: user.photoURL || existingUser.photoURL,
                provider: 'google'
            };
            localStorage.setItem('currentUser', JSON.stringify(currentUser));
            closeAuthModal();
            updateUserUI();
            ageVerificationEnabled = false;
            const ageModal = document.getElementById('ageVerifyModal');
            if (ageModal) ageModal.style.display = 'none';
            alert(`👋 مرحباً ${currentUser.username}!`);
        })
        .catch((error) => {
            console.error("خطأ في تسجيل الدخول:", error);
            showAuthError('❌ حدث خطأ في تسجيل الدخول');
        });
}

function generateVerificationCode() {
    return Math.floor(1000 + Math.random() * 9000).toString();
}

// ================ دوال التحقق من العمر ================
function verifyAge() { 
    localStorage.setItem('ageVerified', 'true');
    localStorage.setItem('ageVerifyTimestamp', new Date().getTime());
    document.getElementById('ageVerifyModal').style.display = 'none'; 
}

function handleVisibilityChange() {
    if (document.hidden) {
        pageVisible = false;
        document.getElementById('ageVerifyModal').style.display = 'none';
    } else {
        pageVisible = true;
        lastActivityTime = new Date().getTime();
        setTimeout(() => {
            checkAgeVerification();
        }, 500);
    }
}

function updateUserActivity() {
    lastActivityTime = new Date().getTime();
    userIdle = false;
}

function checkUserActivity() {
    const now = new Date().getTime();
    if (now - lastActivityTime > 120000) {
        userIdle = true;
        document.getElementById('ageVerifyModal').style.display = 'none';
    } else {
        userIdle = false;
    }
}

function checkAgeVerification() {
    if (currentUser) return;
    if (!ageVerificationEnabled) return;
    if (!pageVisible) return;
    checkUserActivity();
    if (userIdle) return;
    const playWindow = document.getElementById('playWindow');
    if (playWindow && !playWindow.classList.contains('hidden')) return;
    const isVerified = localStorage.getItem('ageVerified');
    const lastVerifyTime = localStorage.getItem('ageVerifyTimestamp');
    const now = new Date().getTime();
    if (!isVerified || !lastVerifyTime) {
        document.getElementById('ageVerifyModal').style.display = 'flex';
        return;
    }
    const tenMinutes = 10 * 60 * 1000;
    if (now - parseInt(lastVerifyTime) > tenMinutes) {
        document.getElementById('ageVerifyModal').style.display = 'flex';
    }
}

document.addEventListener('mousemove', updateUserActivity);
document.addEventListener('keydown', updateUserActivity);
document.addEventListener('click', updateUserActivity);
document.addEventListener('scroll', updateUserActivity);
document.addEventListener('touchstart', updateUserActivity);
document.addEventListener('visibilitychange', handleVisibilityChange);

// ================ دوال القوائم والتبديل ================
function toggleDBList() { 
    const el = document.getElementById('dbList'); 
    if (el) {
        el.style.display = el.style.display === 'block' ? 'none' : 'block';
    }
}

function toggleSidebar() {
    const sidebar = document.getElementById('mainSidebar');
    const overlay = document.getElementById('sidebarOverlay');
    if (!sidebar || !overlay) return;

    if (!sidebar.classList.contains('translate-x-full')) {
        // إغلاق
        sidebar.classList.add('translate-x-full');
        sidebar.classList.remove('translate-x-0');
        overlay.classList.add('hidden');
        sidebar.style.top = '';
        sidebar.style.right = '';
    } else {
        // فتح
        sidebar.classList.remove('translate-x-full');
        sidebar.classList.add('translate-x-0');
        overlay.classList.remove('hidden');
        // لا نحتاج لضبط top/right لأن القائمة ثابتة في الأعلى
    }
}
function toggleSubMenu(id, btn) {
    const subMenu = document.getElementById(id);
    const arrow = btn?.querySelector('span:last-child');
    if (!subMenu) return;
    document.querySelectorAll('[id^="sub-"]').forEach(el => {
        if (el.id !== id) el.classList.add('hidden');
    });
    subMenu.classList.toggle('hidden');
    if (arrow) {
        arrow.style.transform = subMenu.classList.contains('hidden') ? "rotate(0deg)" : "rotate(180deg)";
    }
}

function updateSidebarStats() {
    if (!dummyData) return;
    const videos = dummyData.filter(item => item.type === 'video').length;
    const images = dummyData.filter(item => item.type === 'image').length;
    const articles = dummyData.filter(item => item.type === 'article').length;
    const statsDivs = document.querySelectorAll('#mainSidebar .grid-cols-3 .bg-zinc-900\\/50');
    if (statsDivs.length >= 3) {
        statsDivs[0].querySelector('.text-pink-500').innerText = videos;
        statsDivs[1].querySelector('.text-pink-500').innerText = images;
        statsDivs[2].querySelector('.text-pink-500').innerText = articles;
    }
}

window.addEventListener('load', function() {
    var btn = document.getElementById('menuToggleBtn');
    if (btn) {
        btn.style.top = '80px';
        btn.style.right = '15px';
        btn.style.position = 'fixed';
    }
});

function switchSource(key, name) {
	
    resetAdsCache();
    currentFilterTag = null;
    profileMode = null; 
    sessionStorage.setItem('selectedSource', key);
    sessionStorage.setItem('selectedSourceName', name);
    currentSourceKey = key;
    currentSourceName = name;
    dummyData = shuffleArray(allDBs[key]);
    if (key === 'life') {
        currentType = 'all';
        currentCat = 'all content';
    } else if (key === 'liveGirls' || key === 'liveChannels') {
        currentType = 'live';
        currentCat = 'all content';
    } else {
        currentType = 'video';
        currentCat = 'all content';
        updateImageModeClass(); 
    }
    const catTitle = document.getElementById('catTitle');
    if (catTitle) {
        catTitle.innerText = t('sectionTitles')[key] || t('sectionNames')[key] || key;
    }
    updateSubTitle();
    saveState();
    window.history.pushState({view: 'section', section: key}, "");
    window.scrollTo({ top: 0, behavior: 'smooth' });
    renderMenu(); 
    renderAll();
    updateFooterStats();
    setTimeout(() => {
        renderAdsBelowPagination();
    }, 300);
    
    // إغلاق القائمة الجانبية إذا كانت مفتوحة (على الكمبيوتر)
    const sidebar = document.getElementById('mainSidebar');
    if (sidebar && sidebar.classList.contains('translate-x-0')) {
        toggleSidebar();
    }
}


function saveState() {
    sessionStorage.setItem('selectedType', currentType);
    sessionStorage.setItem('selectedCat', currentCat);
    sessionStorage.setItem('activeParent', activeParent || ""); 
}


function renderMenu() {
    var box = document.getElementById('menuBox'); 
    if (!box) return;
    box.innerHTML = "";
    const mainSections = [
        { key: 'zaj', name: 'مستقيم ⚤', nameEn: 'Straight', icon: 'https://b.top4top.io/p_3690o1nhv1.png', hasSubmenu: true },
        { key: 'nesa', name: 'سحاقيات ⚢', nameEn: 'Lesbian', icon: 'https://h.top4top.io/p_3690et83z1.png', hasSubmenu: true },
        { key: 'haywan', name: 'متحولين جنسياً ⚧', nameEn: 'Trans', icon: 'https://l.top4top.io/p_36909qyxa1.png', hasSubmenu: true },
     { key: 'ime', name: '  Anime ', nameEn: 'Trans', icon: 'https://l.top4top.io/p_36909qyxa1.png', hasSubmenu: true },   
        { key: 'liveGirls', name: 'Live Girls', nameEn: 'Live Girls', icon: '🔴', hasSubmenu: true, isExternal: false },
        { key: 'liveChannels', name: 'Live Channels Porn', nameEn: 'Live Channels Porn', icon: '📺', hasSubmenu: true, isExternal: false },
        { key: 'life', name: 'وضعيات جنسية', nameEn: 'Sex Positions', icon: 'https://b.top4top.io/p_3690o1nhv1.png', hasSubmenu: false }
    ];
    const submenuItems = [
        { type: 'video', cat: 'all content', name: '🎬 فيديوهات', icon: '🎬' },
        { type: 'movie', cat: 'all content', name: '🎞️ أفلام', icon: '🎞️' },
        { type: 'image', cat: 'ثابتة', name: '🖼️ صور ثابتة', icon: '🖼️' },
        { type: 'image', cat: 'متحركة', name: '🎞️ صور متحركة', icon: '🎞️' }
    ];
    const lifeSubmenuItems = [
        { type: 'all', cat: 'all content', name: '📄 مقالات', icon: '📄' }
    ];
    mainSections.forEach(section => {
        const sectionContainer = document.createElement('div');
        sectionContainer.className = 'main-section-container';
        sectionContainer.style.marginBottom = '8px';
        sectionContainer.style.borderRadius = '12px';
        sectionContainer.style.overflow = 'hidden';
        const isActive = (currentSourceKey === section.key);
        if (isActive) {
            sectionContainer.style.background = 'rgba(190, 24, 93, 0.15)';
            sectionContainer.style.borderRight = '3px solid #be185d';
        }
        const sectionHeader = document.createElement('div');
        sectionHeader.className = 'main-section-header';
        sectionHeader.style.cssText = `
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 12px 16px;
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 600;
            font-size: 0.9rem;
            color: ${isActive ? '#fff' : '#ddd'};
        `;
        const leftContent = document.createElement('div');
        leftContent.style.display = 'flex';
        leftContent.style.alignItems = 'center';
        leftContent.style.gap = '12px';
        leftContent.innerHTML = `
            <img src="${section.icon}" style="width: 24px; height: 24px; object-fit: contain;">
            <span>${currentLang === 'ar' ? section.name : section.nameEn}</span>
        `;
        let arrowSpan = null;
        if (section.hasSubmenu) {
            arrowSpan = document.createElement('span');
            arrowSpan.className = `section-arrow-${section.key}`;
            arrowSpan.style.cssText = `
                transition: transform 0.2s ease;
                font-size: 0.8rem;
                color: #be185d;
            `;
            arrowSpan.innerHTML = isActive ? '▲' : '▼';
        }
        sectionHeader.appendChild(leftContent);
        if (arrowSpan) sectionHeader.appendChild(arrowSpan);
        const subMenu = document.createElement('div');
        subMenu.className = `section-submenu-${section.key}`;
        subMenu.style.cssText = `
            display: ${isActive ? 'block' : 'none'};
            padding: 5px 0 10px 35px;
            border-right: 2px solid rgba(190, 24, 93, 0.3);
            margin-right: 12px;
        `;
        if (section.isExternal && section.externalHandler) {
            sectionHeader.onclick = (e) => {
                e.stopPropagation();
                if (typeof window[section.externalHandler] === 'function') {
                    window[section.externalHandler]();
                }
            };
        } else {
            const itemsToShow = section.hasSubmenu ? submenuItems : lifeSubmenuItems;
            itemsToShow.forEach(sub => {
                const subItem = document.createElement('div');
                subItem.className = 'submenu-item';
                subItem.style.cssText = `
                    padding: 8px 12px;
                    margin: 2px 0;
                    cursor: pointer;
                    border-radius: 8px;
                    transition: all 0.2s ease;
                    font-size: 0.8rem;
                    display: flex;
                    align-items: center;
                    gap: 8px;
                    color: #bbb;
                `;
                let isSubActive = false;
                if (section.hasSubmenu) {
                    isSubActive = (isActive && currentType === sub.type && currentCat === sub.cat);
                } else {
                    isSubActive = (isActive && currentType === 'all');
                }
                if (isSubActive) {
                    subItem.style.background = 'rgba(190, 24, 93, 0.2)';
                    subItem.style.color = '#fff';
                    subItem.style.borderRight = '2px solid #be185d';
                }
                subItem.innerHTML = `
                    <span style="font-size: 1rem;">${sub.icon}</span>
                    <span>${sub.name}</span>
                `;
                subItem.onclick = (e) => {
                    e.stopPropagation();
                    const sectionNames = {
                        zaj: currentLang === 'ar' ? 'مستقيم ⚤' : 'Straight',
                        nesa: currentLang === 'ar' ? 'سحاقيات ⚢' : 'Lesbian',
                        haywan: currentLang === 'ar' ? 'متحولين جنسياً ⚧' : 'Trans',
                        life: currentLang === 'ar' ? 'وضعيات جنسية' : 'Sex Positions'
                    };
                    if (typeof switchSource === 'function') {
                        switchSource(section.key, sectionNames[section.key]);
                    }
                    setTimeout(() => {
                        if (section.hasSubmenu) {
                            currentType = sub.type;
                            currentCat = sub.cat;
                        } else {
                            currentType = 'all';
                            currentCat = 'all content';
                        }
                        activeParent = null;
                        profileMode = null;
                        currentPage = 1;
                        saveState();
                        renderMenu();
                        renderAll();
                        const mobileMenu = document.getElementById('mobileSidebarMenu');
                        if (mobileMenu && mobileMenu.classList.contains('show')) {
                            mobileMenu.classList.remove('show');
                            const overlay = document.getElementById('sidebarOverlay');
                            if (overlay) overlay.classList.remove('show');
                        }
                    }, 50);
                };
                subMenu.appendChild(subItem);
            });
            sectionHeader.onclick = (e) => {
                e.stopPropagation();
                const isCurrentlyOpen = subMenu.style.display === 'block';
                if (isActive) {
                    subMenu.style.display = isCurrentlyOpen ? 'none' : 'block';
                    if (arrowSpan) {
                        arrowSpan.innerHTML = isCurrentlyOpen ? '▼' : '▲';
                    }
                } else {
                    const sectionNames = {
                        zaj: currentLang === 'ar' ? 'مستقيم ⚤' : 'Straight',
                        nesa: currentLang === 'ar' ? 'سحاقيات ⚢' : 'Lesbian',
                        haywan: currentLang === 'ar' ? 'متحولين جنسياً ⚧' : 'Trans',
                        life: currentLang === 'ar' ? 'وضعيات جنسية' : 'Sex Positions'
                    };
                    if (typeof switchSource === 'function') {
                        switchSource(section.key, sectionNames[section.key]);
                    }
                    setTimeout(() => {
                        if (section.hasSubmenu) {
                            currentType = 'video';
                            currentCat = 'all content';
                        } else {
                            currentType = 'all';
                            currentCat = 'all content';
                        }
                        activeParent = null;
                        profileMode = null;
                        currentPage = 1;
                        saveState();
                        renderMenu();
                        renderAll();
                    }, 50);
                }
            };
        }
        sectionContainer.appendChild(sectionHeader);
        if (!section.isExternal) {
            sectionContainer.appendChild(subMenu);
        }
        box.appendChild(sectionContainer);
    });
    updateAllTexts();
}

function updateMobileSidebarMenuContent() {
    const mobileMenu = document.getElementById('mobileSidebarMenu');
    if (!mobileMenu) return;
    const activeSectionSpan = document.querySelector('.db-switch-btn-mobile span:first-child');
    if (activeSectionSpan) {
        const activeName = document.getElementById('activeName')?.innerText || 'قسم الزواج';
        activeSectionSpan.innerHTML = activeName;
    }
    updateMobileSidebarButtons();
    const statsPreview = document.querySelector('#mobileSidebarMenu .quick-stats, #mobileSidebarMenu > div:last-child');
    if (statsPreview && dummyData) {
        const videosCount = dummyData.filter(item => item.type === 'video').length || 0;
        const imagesCount = dummyData.filter(item => item.type === 'image').length || 0;
        const articlesCount = dummyData.filter(item => item.type === 'article').length || 0;
        statsPreview.innerHTML = `
            <div style="text-align: center;">
                <div style="font-size: 18px; font-weight: bold; color: #be185d;">${videosCount}</div>
                <div style="font-size: 10px; color: #999;">فيديو</div>
            </div>
            <div style="text-align: center;">
                <div style="font-size: 18px; font-weight: bold; color: #be185d;">${imagesCount}</div>
                <div style="font-size: 10px; color: #999;">صورة</div>
            </div>
            <div style="text-align: center;">
                <div style="font-size: 18px; font-weight: bold; color: #be185d;">${articlesCount}</div>
                <div style="font-size: 10px; color: #999;">مقال</div>
            </div>
        `;
    }
}

function updateMobileSidebarButtons() {
    const favBtn = document.querySelector('#mobileSidebarMenu #btnFav');
    const likeBtn = document.querySelector('#mobileSidebarMenu #btnLike');
    const watchLaterBtn = document.querySelector('#mobileSidebarMenu #btnWatchLater');
    if (favBtn) {
        favBtn.style.background = profileMode === 'fav' ? '#be185d' : 'rgba(255,255,255,0.1)';
    }
    if (likeBtn) {
        likeBtn.style.background = profileMode === 'like' ? '#be185d' : 'rgba(255,255,255,0.1)';
    }
    if (watchLaterBtn) {
        watchLaterBtn.style.background = profileMode === 'watchlater' ? '#be185d' : 'rgba(255,255,255,0.1)';
    }
}

// ================ دوال البطاقات والمحتوى ================
function createCard(p) {
    if (p.type === 'article') {
        return `
            <div class="mag-item cursor-pointer relative" onclick="openArticle(${p.id})">
                <img src="${p.image}" class="w-full h-40 object-cover" loading="lazy"
                    onload="if(this.naturalHeight > this.naturalWidth * 1.2) { 
                        this.classList.add('portrait-thumb');
                        this.closest('.thumb-wrapper')?.classList.add('portrait-content');
                    }">
                <div class="mag-overlay">
                    <h3 class="font-bold text-sm">${p.title}</h3>
                </div>
                <div class="absolute top-2 left-2 z-30">
                    <button class="card-menu-btn w-7 h-7 text-lg" onclick="toggleCardMenu(event, ${p.id})">⁝</button>
                    <div id="menu-${p.id}" class="card-menu-dropdown">
                        <div class="card-menu-item ${favorites.includes(p.id) ? 'active' : ''}" 
                             data-id="${p.id}" data-action="fav"
                             onclick="toggleFavFromMenu(event, ${p.id})">
                            <span>❤️</span> ${favorites.includes(p.id) ? 'إزالة من المفضلة' : 'إضافة للمفضلة'}
                        </div>
                        <div class="card-menu-item ${watchLater.includes(p.id) ? 'active' : ''}" 
                             data-id="${p.id}" data-action="watchlater"
                             onclick="toggleWatchLaterFromMenu(event, ${p.id})">
                            <span>⏱️</span> ${watchLater.includes(p.id) ? 'إزالة من المشاهدة لاحقاً' : 'مشاهدة لاحقاً'}
                        </div>
                    </div>
                </div>
            </div>
        `;
    }
    let defaultDuration = '--:--';
    if (p.type === 'video') {
        if (p.contentURL.includes('drive.google.com')) {
            defaultDuration = '05:30';
        } else if (p.contentURL.includes('top4top')) {
            defaultDuration = '03:45';
        }
    }
    return `
        <div class="video-card group mb-4 relative" data-video-id="${p.id}" data-video-url="${p.contentURL}" data-video-type="${p.type}">
            <div class="thumb-wrapper relative overflow-hidden rounded-lg bg-zinc-900/40" 
                 style="aspect-ratio: 3/2; border: 1px solid rgba(255,255,255,0.08);"
                 onclick='playVideo(${JSON.stringify(p).replace(/"/g, "&quot;")})'>
                
                <img src="${p.image}" 
                     class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                     alt="${p.title}"
                     loading="lazy"
                     onload="if(this.naturalHeight > this.naturalWidth * 1.2) { 
                         this.classList.add('portrait-thumb');
                         this.closest('.thumb-wrapper')?.classList.add('portrait-content');
                     }"
                     onerror="this.src='https://via.placeholder.com/300x200/333/fff?text=Image'">
            ${p.type === 'live' ? 
    `<span class="video-duration-badge bg-red-600 animate-pulse text-[10px] px-2 py-1">
        <i>🔴</i> LIVE
    </span>` : 
    (p.type === 'video' ? 
        `<span class="video-duration-badge text-[10px] px-2 py-1">
            <i>⏱️</i> ${p.duration || defaultDuration}
        </span>` : 
        (p.type === 'gif' ?
            `<span class="video-duration-badge text-[10px] px-2 py-1" style="background: #8b5cf6;">
                <i>🎞️</i> GIF
            </span>` :
            `<span class="video-duration-badge text-[10px] px-2 py-1">
                <i>🖼️</i> صورة
            </span>`
        )
    )
}
            </div>
            
            <div class="flex items-center justify-between mt-2 px-1">
                <div class="video-title group-hover:text-pink-500 transition-colors line-clamp-2 text-sm font-bold flex-1" 
                     style="cursor: pointer; line-height: 1.3; text-align: right;"
                     onclick='playVideo(${JSON.stringify(p).replace(/"/g, "&quot;")})'>
                    ${p.title}
                </div>
                
                <div class="relative mr-1">
                    <button class="card-menu-btn w-7 h-7 bg-zinc-800 hover:bg-pink-600 rounded-full flex items-center justify-center text-white text-lg"
                            onclick="toggleCardMenu(event, ${p.id})">
                        ⁝
                    </button>
                    <div id="menu-${p.id}" class="card-menu-dropdown absolute bottom-full mb-2 left-0 w-40 bg-zinc-900 border border-zinc-700 rounded-lg shadow-2xl overflow-hidden z-50 hidden">
                        <div class="card-menu-item ${favorites.includes(p.id) ? 'active' : ''} flex items-center gap-2 p-2 hover:bg-pink-600/30 cursor-pointer transition-colors border-b border-zinc-800 text-xs"
                             data-id="${p.id}" data-action="fav"
                             onclick="toggleFavFromMenu(event, ${p.id})">
                            <span class="text-base">❤️</span>
                            <span class="text-xs font-medium">${favorites.includes(p.id) ? 'إزالة' : 'إضافة للمفضلة'}</span>
                        </div>
                        <div class="card-menu-item ${watchLater.includes(p.id) ? 'active' : ''} flex items-center gap-2 p-2 hover:bg-pink-600/30 cursor-pointer transition-colors text-xs"
                             data-id="${p.id}" data-action="watchlater"
                             onclick="toggleWatchLaterFromMenu(event, ${p.id})">
                            <span class="text-base">⏱️</span>
                            <span class="text-xs font-medium">${watchLater.includes(p.id) ? 'إزالة' : 'مشاهدة لاحقاً'}</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="flex items-center justify-start mt-1 px-1">
                <span class="text-[8px] text-gray-400 bg-zinc-800/60 px-2 py-0.5 rounded-full">${p.labels || (p.type === 'video' ? 'فيديو' : 'صورة')}</span>
            </div>
        </div>
    `;
}
function playVideo(p) {
    // إذا كان العنصر يحتوي على رابط إعادة توجيه خارجي
    if (p.redirectUrl) {
        window.open(p.redirectUrl, '_blank');
        return;
    }

    window.history.pushState({view: 'player', videoId: p.id}, "");
    currentPlayingId = p.id;
    sessionStorage.setItem('currentPlayingId', p.id);
    sessionStorage.setItem('lastPlayingSource', currentSourceKey);
    sessionStorage.setItem('lastPlayingSourceName', currentSourceName);
    sessionStorage.setItem('scrollBeforePlayer', window.pageYOffset || document.documentElement.scrollTop);

    const win = document.getElementById('playWindow');
    const vArea = document.getElementById('vArea');
    win.classList.remove('hidden');
    win.style.display = 'block';
    setTimeout(() => {
        const playerTop = document.getElementById('playWindow');
        if (playerTop) playerTop.scrollTo({ top: 0, behavior: 'smooth' });
    }, 100);
    document.body.style.overflow = 'hidden';
    win.setAttribute('dir', 'rtl');

    const titleElement = document.getElementById('vTitle');
    if (titleElement) titleElement.innerText = p.title;

    // التصنيفات
    let tagsContainer = document.getElementById('playerTagsContainer');
    if (!tagsContainer) {
        tagsContainer = document.createElement('div');
        tagsContainer.id = 'playerTagsContainer';
        tagsContainer.style.cssText = `display:flex; flex-wrap:wrap; gap:8px; margin:12px 0 8px; padding:8px 0; border-bottom:1px solid rgba(255,255,255,0.1);`;
        if (titleElement) titleElement.parentNode.insertBefore(tagsContainer, titleElement.nextSibling);
    }
    const itemTags = getItemTags(p);
    tagsContainer.innerHTML = itemTags.length ? `<span style="color:#be185d;font-size:0.75rem;font-weight:bold;">التصنيفات:</span> ${itemTags.map(tag => `<button onclick="filterByTag('${tag.replace(/'/g, "\\'")}')" style="background:rgba(190,24,93,0.2); border:1px solid rgba(190,24,93,0.5); color:#ffb3d9; padding:4px 12px; border-radius:20px; font-size:0.7rem; cursor:pointer;">#${tag}</button>`).join('')}` : '';

    // أزرار التفاعل
    let actionsContainer = document.querySelector('.action-buttons-container');
    if (!actionsContainer) {
        actionsContainer = document.createElement('div');
        actionsContainer.className = 'action-buttons-container';
        actionsContainer.style.cssText = `display:flex; flex-wrap:wrap; gap:12px; margin:10px 0; padding:10px 0; border-top:1px solid rgba(255,255,255,0.1); border-bottom:1px solid rgba(255,255,255,0.1);`;
        tagsContainer.parentNode.insertBefore(actionsContainer, tagsContainer.nextSibling);
    }
    actionsContainer.innerHTML = `
        <button class="action-btn" onclick="toggleFavFromPlayer(event, ${p.id})">❤️ مفضلة</button>
        <button class="action-btn" onclick="toggleLike(event, ${p.id})">👍 إعجاب</button>
        <button class="action-btn" onclick="toggleDislike(event, ${p.id})">👎 عدم إعجاب</button>
        <button class="action-btn" onclick="toggleWatchLater(event, ${p.id})">⏱️ لاحقاً</button>
        <button class="action-btn" onclick="shareVideo(event, ${p.id}, '${p.title.replace(/'/g, "\\'")}')">📤 مشاركة</button>
    `;

    vArea.innerHTML = '';
    vArea.classList.remove('image-mode');
    vArea.style.paddingBottom = '56.25%';
    vArea.style.position = 'relative';

    // ========== معالجة أنواع المحتوى ==========
    if (p.type === 'live') {
        let embedURL = p.contentURL;
        if (embedURL.includes('youtube.com/watch')) {
            const videoId = embedURL.split('v=')[1]?.split('&')[0];
            embedURL = `https://www.youtube.com/embed/${videoId}?autoplay=1`;
        } else if (embedURL.includes('youtu.be')) {
            const videoId = embedURL.split('/').pop();
            embedURL = `https://www.youtube.com/embed/${videoId}?autoplay=1`;
        } else if (embedURL.includes('twitch.tv')) {
            const channel = embedURL.split('twitch.tv/')[1];
            embedURL = `https://player.twitch.tv/?channel=${channel}&parent=${location.hostname}`;
        }
        const blockedDomains = ['oxax.tv', 'example.com'];
        if (blockedDomains.some(domain => embedURL.includes(domain))) {
            window.open(p.contentURL, '_blank');
            setTimeout(() => { updateRecSuggestions(); updateSideSuggestions(); updateFooterStats(); }, 100);
            return;
        }
        vArea.innerHTML = `<iframe style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" src="${embedURL}" allow="autoplay; fullscreen; encrypted-media" allowfullscreen></iframe>`;
    }
    else if (p.type === 'video') {
        let videoUrl = p.contentURL;
        let isGoogleDrive = videoUrl.includes('drive.google.com');

        // ===== حالة Google Drive: استخدم iframe التضميني (لا عناصر تحكم مكررة) =====
        if (isGoogleDrive) {
            // استخرج معرف الملف
            let fileId = null;
            let match = videoUrl.match(/\/d\/(.+?)\/view/);
            if (!match) match = videoUrl.match(/id=(.+?)(&|$)/);
            if (match) fileId = match[1];

            if (fileId) {
                const embedUrl = `https://drive.google.com/file/d/${fileId}/preview`;
                vArea.innerHTML = `<iframe style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" src="${embedUrl}" allow="autoplay; fullscreen" allowfullscreen></iframe>`;
            } else {
                vArea.innerHTML = `<div style="text-align:center;padding:20px;color:red;">❌ رابط Google Drive غير صالح</div>`;
            }
        }
        // ===== حالة روابط MP4 المباشرة: استخدم عنصر video مع controls فقط (بدون initAdvancedPlayer) =====
        else if (videoUrl.match(/\.mp4$/i) || videoUrl.includes('.mp4?')) {
            vArea.innerHTML = `
                <div style="position:absolute;top:0;left:0;width:100%;height:100%;background:black;">
                    <video id="mainVideo" style="width:100%;height:100%;object-fit:contain;" controls autoplay>
                        <source src="${videoUrl}" type="video/mp4">
                        المتصفح لا يدعم الفيديو.
                    </video>
                </div>
            `;
        }
        // ===== أي رابط آخر: حاول iframe عادي =====
        else {
            vArea.innerHTML = `<iframe style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" src="${videoUrl}" allow="autoplay; fullscreen" allowfullscreen></iframe>`;
        }
    }
    else if (p.type === 'image' || p.type === 'gif') {
        vArea.style.paddingBottom = '0';
        vArea.classList.add('image-mode');
        const mediaUrl = (p.type === 'gif' && p.image) ? p.image : p.contentURL;
        vArea.innerHTML = `
            <div style="display:flex; align-items:center; justify-content:center; width:100%; height:100%; background:black;">
                <img src="${mediaUrl}" style="max-width:100%; max-height:85vh; width:auto; height:auto; object-fit:contain; display:block; margin:0 auto; border-radius:12px;" alt="${p.title}">
            </div>
        `;
    }

    // تحديث الاقتراحات
    setTimeout(() => {
        updateRecSuggestions();
        updateSideSuggestions();
        updateFooterStats();
    }, 150);
}
function initAdvancedPlayer(videoElement, containerId) {
    const video = videoElement;
    const container = document.getElementById(containerId);
    if (!video || !container) return;

    let controls = container.querySelector('.custom-controls');
    if (!controls) {
        controls = document.createElement('div');
        controls.className = 'custom-controls';
        controls.style.cssText = `
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            background: linear-gradient(to top, rgba(0,0,0,0.8), rgba(0,0,0,0));
            padding: 12px 16px;
            display: flex;
            align-items: center;
            gap: 12px;
            opacity: 0;
            transition: opacity 0.3s ease;
            z-index: 20;
            box-sizing: border-box;
        `;
        controls.innerHTML = `
            <button class="ctrl-play-pause" style="background:none; border:none; color:white; font-size:20px; cursor:pointer;">⏸️</button>
            <span class="ctrl-current-time" style="color:white; font-size:12px;">00:00</span>
            <input type="range" class="ctrl-progress" value="0" min="0" max="100" step="0.1" style="flex:1; height:4px; -webkit-appearance:none; background:rgba(255,255,255,0.3); border-radius:2px;">
            <span class="ctrl-duration" style="color:white; font-size:12px;">00:00</span>
            <button class="ctrl-mute" style="background:none; border:none; color:white; font-size:18px; cursor:pointer;">🔊</button>
            <button class="ctrl-fullscreen" style="background:none; border:none; color:white; font-size:18px; cursor:pointer;">⛶</button>
        `;
        container.appendChild(controls);
    }

    const playPauseBtn = controls.querySelector('.ctrl-play-pause');
    const currentTimeSpan = controls.querySelector('.ctrl-current-time');
    const progressBar = controls.querySelector('.ctrl-progress');
    const durationSpan = controls.querySelector('.ctrl-duration');
    const muteBtn = controls.querySelector('.ctrl-mute');
    const fullscreenBtn = controls.querySelector('.ctrl-fullscreen');
    let controlsTimeout;

    function formatTime(seconds) {
        if (isNaN(seconds)) return '00:00';
        const mins = Math.floor(seconds / 60);
        const secs = Math.floor(seconds % 60);
        return `${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
    }

    function updateProgress() {
        if (video.duration) {
            const percent = (video.currentTime / video.duration) * 100;
            progressBar.value = percent;
            progressBar.style.background = `linear-gradient(to right, #be185d ${percent}%, rgba(255,255,255,0.3) ${percent}%)`;
            currentTimeSpan.innerText = formatTime(video.currentTime);
        }
    }

    function showControls() {
        controls.style.opacity = '1';
        clearTimeout(controlsTimeout);
        controlsTimeout = setTimeout(() => {
            if (!container.matches(':hover') && !controls.matches(':hover')) controls.style.opacity = '0';
        }, 2000);
    }

    video.addEventListener('loadedmetadata', () => {
        durationSpan.innerText = formatTime(video.duration);
        progressBar.max = 100;
        updateProgress();
    });
    video.addEventListener('timeupdate', updateProgress);
    video.addEventListener('play', () => { playPauseBtn.innerText = '⏸️'; showControls(); });
    video.addEventListener('pause', () => { playPauseBtn.innerText = '▶️'; showControls(); });
    video.addEventListener('click', () => { video.paused ? video.play() : video.pause(); showControls(); });

    playPauseBtn.addEventListener('click', (e) => { e.stopPropagation(); video.paused ? video.play() : video.pause(); showControls(); });
    progressBar.addEventListener('input', (e) => { video.currentTime = (e.target.value / 100) * video.duration; updateProgress(); showControls(); });
    muteBtn.addEventListener('click', (e) => { e.stopPropagation(); video.muted = !video.muted; muteBtn.innerText = video.muted ? '🔇' : '🔊'; showControls(); });
    fullscreenBtn.addEventListener('click', (e) => { e.stopPropagation(); if (!document.fullscreenElement) container.requestFullscreen(); else document.exitFullscreen(); showControls(); });

    let lastTap = 0;
    video.addEventListener('click', (e) => {
        const now = Date.now();
        if (now - lastTap < 300) {
            e.preventDefault();
            const rect = video.getBoundingClientRect();
            const isLeft = (e.clientX - rect.left) < rect.width / 2;
            let newTime = video.currentTime + (isLeft ? -10 : 10);
            newTime = Math.min(Math.max(newTime, 0), video.duration);
            video.currentTime = newTime;
            let msg = container.querySelector('.seek-message') || (() => { let d = document.createElement('div'); d.className = 'seek-message'; d.style.cssText = 'position:absolute;bottom:20%;left:50%;transform:translateX(-50%);background:rgba(0,0,0,0.8);color:white;padding:8px 16px;border-radius:30px;font-size:16px;z-index:30;pointer-events:none;opacity:0;white-space:nowrap;'; container.appendChild(d); return d; })();
            msg.innerText = (isLeft ? '-10' : '+10') + ' ثانية';
            msg.style.opacity = '1';
            setTimeout(() => msg.style.opacity = '0', 800);
        }
        lastTap = now;
    });

    container.addEventListener('mousemove', showControls);
    container.addEventListener('touchstart', showControls);
    container.addEventListener('mouseleave', () => { clearTimeout(controlsTimeout); controls.style.opacity = '0'; });
    video.play().catch(e => console.log('autoplay blocked'));
    showControls();
}
function restoreFinalState() {
    try {
        const saved = localStorage.getItem('porniq_state');
        if (!saved) return false;
        const state = JSON.parse(saved);
        if (state.section && allDBs[state.section]) {
            currentSourceKey = state.section;
            currentSourceName = state.sectionName || 'الزواج';
            dummyData = [...allDBs[currentSourceKey]];
        }
        if (state.page) currentPage = state.page;
        if (state.type) currentType = state.type;
        if (state.cat) currentCat = state.cat;
        if (state.profile !== null) profileMode = state.profile;
        if (state.video) {
            currentPlayingId = state.video;
            localStorage.setItem('currentPlayingId', state.video);
        }
        renderMenu();
        renderAll();
        updateSubTitle();
        return true;
    } catch(e) {
        return false;
    }
}

function toggleFavFromPlayer(event, id) {
    event.stopPropagation();
    const idx = favorites.indexOf(id);
    idx > -1 ? favorites.splice(idx, 1) : favorites.push(id);
    localStorage.setItem('myFavs', JSON.stringify(favorites));
    updatePlayerButtons(id);
}

function toggleLike(event, id) {
    event.stopPropagation();
    const idx = likes.indexOf(id);
    if (idx > -1) {
        likes.splice(idx, 1);
        if (likesCount[id]) {
            likesCount[id] = Math.max(0, (likesCount[id] || 1) - 1);
        }
    } else {
        likes.push(id);
        likesCount[id] = (likesCount[id] || 0) + 1;
        const dislikeIdx = dislikes.indexOf(id);
        if (dislikeIdx > -1) {
            dislikes.splice(dislikeIdx, 1);
            localStorage.setItem('myDislikes', JSON.stringify(dislikes));
        }
    }
    localStorage.setItem('myLikes', JSON.stringify(likes));
    localStorage.setItem('likesCount', JSON.stringify(likesCount));
    updatePlayerButtons(id);
}

function toggleDislike(event, id) {
    event.stopPropagation();
    const likeIdx = likes.indexOf(id);
    if (likeIdx > -1) {
        likes.splice(likeIdx, 1);
        if (likesCount[id]) {
            likesCount[id] = Math.max(0, (likesCount[id] || 1) - 1);
        }
        localStorage.setItem('myLikes', JSON.stringify(likes));
        localStorage.setItem('likesCount', JSON.stringify(likesCount));
    }
    const idx = dislikes.indexOf(id);
    idx > -1 ? dislikes.splice(idx, 1) : dislikes.push(id);
    localStorage.setItem('myDislikes', JSON.stringify(dislikes));
    updatePlayerButtons(id);
}

function toggleWatchLater(event, id) {
    event.stopPropagation();
    const idx = watchLater.indexOf(id);
    idx > -1 ? watchLater.splice(idx, 1) : watchLater.push(id);
    localStorage.setItem('myWatchLater', JSON.stringify(watchLater));
    updatePlayerButtons(id);
}

function shareVideo(event, id, title) {
    event.stopPropagation();
    const dummyLink = `https://myplatform.com/video/${id}`;
    navigator.clipboard.writeText(dummyLink).then(() => {
        const btn = event.currentTarget;
        const originalText = btn.innerHTML;
        btn.innerHTML = '<i>✓</i> تم النسخ';
        btn.style.background = '#10b981';
        setTimeout(() => {
            btn.innerHTML = originalText;
            btn.style.background = '#2563eb';
        }, 2000);
    });
}

function updatePlayerButtons(id) {
    const container = document.querySelector('.action-buttons-container');
    if (!container) return;
    const favBtn = container.querySelector('.fav-action-btn');
    if (favBtn) {
        const isFav = favorites.includes(id);
        favBtn.classList.toggle('active', isFav);
        favBtn.innerHTML = isFav ? '<i>❤️</i> تم الحفظ' : '<i>❤️</i> إضافة للمفضلة';
    }
    const likeBtn = container.querySelector('.like-action-btn');
    if (likeBtn) {
        const isLike = likes.includes(id);
        likeBtn.classList.toggle('active', isLike);
        likeBtn.innerHTML = isLike ? `<i>👍</i> معجب <span class="count">${likesCount[id] || 1}</span>` : '<i>👍</i> إعجاب';
    }
    const dislikeBtn = container.querySelector('.dislike-action-btn');
    if (dislikeBtn) {
        const isDislike = dislikes.includes(id);
        dislikeBtn.classList.toggle('active', isDislike);
        dislikeBtn.innerHTML = isDislike ? '<i>👎</i> غير معجب' : '<i>👎</i> عدم إعجاب';
    }
    const watchLaterBtn = container.querySelector('.watch-later-btn');
    if (watchLaterBtn) {
        const isWatchLater = watchLater.includes(id);
        watchLaterBtn.classList.toggle('active', isWatchLater);
        watchLaterBtn.innerHTML = isWatchLater ? '<i>⏱️</i> تم الحفظ' : '<i>⏱️</i> مشاهدة لاحقاً';
    }
}

function closePlayer() {
    var savedPosition = scrollPosition || sessionStorage.getItem('scrollBeforePlayer') || window.pageYOffset;
    
    // إزالة بيانات الفيديو من sessionStorage فقط
    sessionStorage.removeItem('currentPlayingId');
    sessionStorage.removeItem('lastPlayingSource');
    sessionStorage.removeItem('lastPlayingSourceName');
    sessionStorage.removeItem('scrollBeforePlayer');
    
    currentPlayingId = null;
    const win = document.getElementById('playWindow');
    win.classList.add('hidden');
    win.style.display = 'none';
    document.getElementById('vArea').innerHTML = '';
    const actionsContainer = document.querySelector('.action-buttons-container');
    if (actionsContainer) actionsContainer.remove();
    document.body.style.overflow = 'auto';
    
    if(window.history.state && window.history.state.view === 'player') {
        window.history.back();
    } else {
        setTimeout(() => {
            window.scrollTo({
                top: parseInt(savedPosition) || 0,
                behavior: 'smooth'
            });
        }, 100);
    }
    sessionStorage.removeItem('savedScrollY');
}

// ================ دوال الاقتراحات ================
function updateRecSuggestions() {
    const recContainer = document.getElementById('recBox');
    if (!recContainer) return;
    if (typeof dummyData === 'undefined' || !dummyData) return;
    const currentItem = dummyData.find(i => i.id == currentPlayingId);
    if (!currentItem) {
        recContainer.innerHTML = '<div class="col-span-full text-center text-gray-500 py-4 text-sm">لا توجد اقتراحات متاحة</div>';
        return;
    }
    const currentTags = getItemTags(currentItem);
    let dataSource = [...dummyData];
    if (currentSourceKey === 'life') {
        dataSource = dataSource.filter(item => item.type === 'article');
    } else {
        dataSource = dataSource.filter(item => item.type === currentItem.type);
    }
    dataSource = dataSource.filter(item => item.id != currentPlayingId);
    dataSource.sort((a, b) => {
        const aTags = getItemTags(a);
        const bTags = getItemTags(b);
        let aMatch = 0, bMatch = 0;
        currentTags.forEach(tag => {
            if (aTags.some(t => t.toLowerCase().includes(tag.toLowerCase()))) aMatch++;
            if (bTags.some(t => t.toLowerCase().includes(tag.toLowerCase()))) bMatch++;
        });
        return bMatch - aMatch;
    });
    const selected = dataSource.slice(0, 6);
    if (selected.length === 0) {
        recContainer.innerHTML = '<div class="col-span-full text-center text-gray-500 py-4 text-sm">لا توجد اقتراحات متاحة</div>';
        return;
    }
    recContainer.innerHTML = selected.map(item => {
        const thumbImage = item.image || 'https://via.placeholder.com/200x120/333/fff?text=Video';
        return `
        <div class="suggestion-item cursor-pointer group" onclick='playVideo(${JSON.stringify(item).replace(/"/g, "&quot;")})'>
            <div class="relative rounded-xl overflow-hidden bg-zinc-900 mb-1 border border-zinc-800 group-hover:border-pink-500 transition-all" 
                 style="aspect-ratio: 16/9; width: 100%;">
                <img src="${thumbImage}" 
                     alt="${item.title}"
                     class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                     loading="lazy"
                     onload="if(this.naturalHeight > this.naturalWidth * 1.2) { 
                         this.classList.add('portrait-thumb');
                         this.closest('.relative.rounded-xl').style.aspectRatio = '9/16';
                         this.closest('.relative.rounded-xl').style.minHeight = '320px';
                     }"
                     onerror="this.src='https://via.placeholder.com/200x120/333/fff?text=Video'">
                ${item.type === 'video' ? 
                    `<span class="absolute bottom-1 right-1 bg-black/90 text-white text-[10px] px-2 py-0.5 rounded-md border border-white/20">
                        ⏱️ ${item.duration || '03:45'}
                    </span>` : 
                    `<span class="absolute bottom-1 right-1 bg-black/90 text-white text-[10px] px-2 py-0.5 rounded-md border border-white/20">
                        🖼️ صورة
                    </span>`
                }
            </div>
            <h4 class="text-xs font-medium text-gray-300 line-clamp-2 group-hover:text-pink-400 transition-colors">
                ${item.title}
            </h4>
        </div>
    `}).join('');
}

function updateSideSuggestions() {
    const sideContainer = document.getElementById('sideVerticalList');
    if (!sideContainer || typeof dummyData === 'undefined') return;
    let dataSource = [...dummyData];
    if (currentSourceKey === 'life') {
        dataSource = dataSource.filter(item => item.type === 'article');
    } else {
        const currentItem = dummyData.find(i => i.id === currentPlayingId);
        if (currentItem) {
            dataSource = dataSource.filter(item => item.type === currentItem.type);
        }
    }
    const shuffled = dataSource.sort(() => 0.5 - Math.random());
    const selected = shuffled.slice(0, 4);
    sideContainer.innerHTML = selected.map(item => {
        const thumbImage = item.image || 'https://via.placeholder.com/120x80/333/fff?text=Video';
        return `
        <div class="flex items-center cursor-pointer group hover:bg-zinc-800/70 transition-all border border-transparent hover:border-pink-500/30"
             onclick='playVideo(${JSON.stringify(item).replace(/"/g, "&quot;")})'>
            <div class="w-16 h-12 bg-zinc-900 rounded-md overflow-hidden flex-shrink-0 border border-zinc-800 group-hover:border-pink-500 transition-all">
                <img src="${thumbImage}" 
                     alt="${item.title}"
                     class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
                     loading="lazy"
                     onload="if(this.naturalHeight > this.naturalWidth * 1.2) this.classList.add('portrait-thumb');"
                     onerror="this.src='https://via.placeholder.com/120x80/333/fff?text=Video';">
            </div>
            <div class="flex-1 min-w-0">
                <h4 class="text-xs font-medium text-gray-200 line-clamp-2 group-hover:text-pink-500">
                    ${item.title}
                </h4>
                <span class="text-[9px] text-pink-500">${item.labels || (item.type === 'video' ? 'فيديو' : 'صورة')}</span>
            </div>
        </div>
    `}).join('');
}

// ================ دوال العرض الرئيسية ================
function renderAll() {
    updateImageModeClass(); 
	
    const grid = document.getElementById('gridBox');
    if (!grid) return;
    const q = document.getElementById('searchBox')?.value.toLowerCase() || '';
    let filtered = dummyData.filter(p => {
       // إذا كان المستخدم يطلب صور (image) فتشمل أيضاً gif
let itemType = p.type;
if (currentType === 'image' && itemType === 'gif') itemType = 'image';
if (currentType !== 'all' && itemType !== currentType) return false;
        if (currentType === 'image' && currentCat !== 'all content') {
            if (currentCat === 'ثابتة' && p.labels?.includes('متحركة')) return false;
            if (currentCat === 'متحركة' && !p.labels?.includes('متحركة')) return false;
            
        }
        if (q && !p.title.toLowerCase().includes(q)) return false;
        if (currentFilterTag) {
            const tags = getItemTags(p).map(t => t.toLowerCase());
            if (!tags.includes(currentFilterTag.toLowerCase())) return false;
        }
        if (profileMode === 'fav' && !favorites.includes(p.id)) return false;
        if (profileMode === 'like' && !likes.includes(p.id)) return false;
        if (profileMode === 'watchlater' && !watchLater.includes(p.id)) return false;
        return true;
    });
    if (filtered.length === 0) {
        grid.innerHTML = `<div class="col-span-full text-center py-20 opacity-50">لا يوجد محتوى حالياً</div>`;
        renderPagination(0);
        return;
    }
    
    const paginated = filtered.slice((currentPage - 1) * itemsPerPage, currentPage * itemsPerPage);
    grid.innerHTML = paginated.map((item, index) => {
        let html = createCard(item);
      // if ((index + 1) % 4 === 0) {
   // const ad = getRandomAd();
   // html += `
      //  <div class="ad-card bg-zinc-900/30 rounded-2xl border border-zinc-800 overflow-hidden">
           // <a href="${ad.link}" target="_blank" class="block">
              //  <div class="relative bg-black flex items-center justify-center" style="min-height: unset; height: auto;">
                //    ${ad.type === 'video' ? 
                   //     `<video autoplay muted loop playsinline style="display: block; width: auto; height: auto; max-width: 100%; margin: 0 auto;" src="${ad.url}"></video>` :
                     //   `<img src="${ad.url || ad.image}" style="display: block; width: auto; height: auto; max-width: 100%; margin: 0 auto;" loading="lazy">`
                 //   }
               // </div>
           //   //  <div class="text-[10px] text-center py-2 text-zinc-500 uppercase">AD - إعلان</div>
          //  </a>
        //</div>`;
//}
        return html;
    }).join('');
    renderPagination(filtered.length);
    updateStats();
}

function changeCategory(event, sourceKey, displayName, contentType, categoryType) {
    resetAdsCache();
    if (event) {
        event.preventDefault();
        event.stopPropagation();
    }
    // ... الكود الموجود ...
    
    window.scrollTo({ top: 0, behavior: 'smooth' });
    
    // إغلاق القائمة الجانبية إذا كانت مفتوحة (على الكمبيوتر)
    const sidebar = document.getElementById('mainSidebar');
    if (sidebar && sidebar.classList.contains('translate-x-0')) {
        toggleSidebar(); // تستخدم الدالة الموجودة لإغلاق القائمة
        saveState();
    }
}

function shuffleArray(array) {
    let shuffled = [...array];
    for (let i = shuffled.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
    }
    return shuffled;
}

function toggleProfileMode(mode) { 
    profileMode = (profileMode === mode) ? null : mode; 
    currentPage = 1;
updateImageModeClass(); 
    renderAll(); 
    renderMenu(); 
    const catTitle = document.getElementById('catTitle');
    if (profileMode === 'fav') catTitle.innerText = t('favTitle');
    else if (profileMode === 'like') catTitle.innerText = t('likeTitle');
    else if (profileMode === 'watchlater') catTitle.innerText = t('watchLaterTitle');
    else catTitle.innerText = t('sectionTitles')[currentSourceKey] || t('sectionNames')[currentSourceKey];
    setTimeout(() => {
        renderAdsBelowPagination();
    }, 200);
}

function renderPagination(totalItems) {
    const totalPages = Math.ceil(totalItems / itemsPerPage);
    const container = document.getElementById('paginationBox');
    if (!container) return;
    container.innerHTML = '';
    if (totalPages <= 1) return;
    let html = "";
    if (currentPage > 1) {
        html += `<button onclick="goToPage(${currentPage - 1})" class="pagination-btn">السابق</button>`;
    }
    for (let i = 1; i <= totalPages; i++) {
        if (i === 1 || i === totalPages || (i >= currentPage - 1 && i <= currentPage + 1)) {
            const activeClass = i === currentPage ? 'active' : '';
            html += `<button onclick="goToPage(${i})" class="pagination-btn ${activeClass}">${i}</button>`;
        } else if (i === currentPage - 2 || i === currentPage + 2) {
            html += `<span class="text-zinc-600 px-1">...</span>`;
        }
    }
    if (currentPage < totalPages) {
        html += `<button onclick="goToPage(${currentPage + 1})" class="pagination-btn">التالي</button>`;
    }
    container.innerHTML = html;
}

function goToPage(page) {
    currentPage = page;
    renderAll();
    window.scrollTo({ top: 0, behavior: 'smooth' });
    sessionStorage.setItem('currentPage', currentPage);
    saveState();  // <-- أضف هذا السطر إذا لم يكن موجوداً
    setTimeout(() => {
        renderAdsBelowPagination();
    }, 200);
}

// ================ دوال المقالات ================
function openArticle(articleId) {
    scrollPosition = window.pageYOffset || document.documentElement.scrollTop;
    const article = dummyData.find(item => item.id === articleId);
    if (!article) return;
    window.history.pushState({view: 'article', articleId: articleId}, "");
    document.getElementById('artTitle').innerText = article.title;
    let contentHtml = article.blocks.map(b => `<div class="mb-6">${b.content}</div>`).join('');
    let relatedArticles = dummyData
        .filter(item => item.type === 'article' && item.id !== articleId)
        .slice(0, 4);
    if (relatedArticles.length > 0) {
        contentHtml += `
            <div class="mt-12 pt-8 border-t border-zinc-800">
                <h3 class="text-2xl font-bold text-pink-500 mb-6">${t('relatedTopics')}</h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    ${relatedArticles.map(ra => `
                        <div class="flex gap-4 p-3 bg-zinc-900 rounded-xl cursor-pointer hover:bg-zinc-800 transition-all" onclick="openArticle(${ra.id})">
                            <img src="${ra.image}" class="w-24 h-24 object-cover rounded-lg" onerror="this.src='https://via.placeholder.com/96x96/333/fff?text=Article'">
                            <div class="font-bold text-sm">${ra.title}</div>
                        </div>
                    `).join('')}
                </div>
            </div>`;
    }
    document.getElementById('artBody').innerHTML = contentHtml;
    document.getElementById('articleView').style.display = 'block';
    window.scrollTo(0, 0);
}

function closeArticle() { 
    document.getElementById('articleView').style.display = 'none'; 
    setTimeout(() => {
        window.scrollTo({
            top: scrollPosition || 0,
            behavior: 'smooth'
        });
    }, 100);
    if(window.history.state && window.history.state.view === 'article') {
        window.history.back();
    }
}

// ================ دوال القائمة المنبثقة ================
function toggleCardMenu(event, itemId) {
    event.stopPropagation();
    document.querySelectorAll('.card-menu-dropdown').forEach(menu => {
        if (menu.id !== `menu-${itemId}`) {
            menu.classList.remove('show');
        }
    });
    const menu = document.getElementById(`menu-${itemId}`);
    if (menu) menu.classList.toggle('show');
}

document.addEventListener('click', function(e) {
    if (!e.target.closest('.card-menu-btn') && !e.target.closest('.card-menu-dropdown')) {
        document.querySelectorAll('.card-menu-dropdown').forEach(menu => {
            menu.classList.remove('show');
        });
    }
});

window.addEventListener('scroll', function() {
    document.querySelectorAll('.card-menu-dropdown.show').forEach(menu => {
        menu.classList.remove('show');
    });
}, { passive: true });

function toggleFavFromMenu(event, id) {
    event.stopPropagation();
    const idx = favorites.indexOf(id);
    idx > -1 ? favorites.splice(idx, 1) : favorites.push(id);
    localStorage.setItem('myFavs', JSON.stringify(favorites));
    const menuItem = document.querySelector(`.card-menu-item[data-id="${id}"][data-action="fav"]`);
    if (menuItem) {
        menuItem.classList.toggle('active', favorites.includes(id));
        const spans = menuItem.querySelectorAll('span');
        if (spans.length >= 2) {
            spans[1].innerHTML = favorites.includes(id) ? 'إزالة من المفضلة' : 'إضافة للمفضلة';
        }
    }
    const menu = document.getElementById(`menu-${id}`);
    if (menu) menu.classList.remove('show');
}

function toggleWatchLaterFromMenu(event, id) {
    event.stopPropagation();
    const idx = watchLater.indexOf(id);
    idx > -1 ? watchLater.splice(idx, 1) : watchLater.push(id);
    localStorage.setItem('myWatchLater', JSON.stringify(watchLater));
    const menuItem = document.querySelector(`.card-menu-item[data-id="${id}"][data-action="watchlater"]`);
    if (menuItem) {
        menuItem.classList.toggle('active', watchLater.includes(id));
        const spans = menuItem.querySelectorAll('span');
        if (spans.length >= 2) {
            spans[1].innerHTML = watchLater.includes(id) ? 'إزالة من المشاهدة لاحقاً' : 'مشاهدة لاحقاً';
        }
    }
    const menu = document.getElementById(`menu-${id}`);
    if (menu) menu.classList.remove('show');
}

// ================ دوال الإحصائيات ================
function updateStats() {
    if (!dummyData) return;
    const videos = dummyData.filter(item => item.type === 'video').length;
    const images = dummyData.filter(item => item.type === 'image').length;
    const articles = dummyData.filter(item => item.type === 'article').length;
    const videosEl = document.getElementById('videosCount');
    const imagesEl = document.getElementById('imagesCount');
    const articlesEl = document.getElementById('articlesCount');
    if (videosEl) videosEl.innerHTML = `📹 ${videos} ${t('videos')}`;
    if (imagesEl) imagesEl.innerHTML = `🖼️ ${images} ${t('images')}`;
    if (articlesEl) articlesEl.innerHTML = `📄 ${articles} ${t('articles')}`;
}

function updateFooterStats() {
    const videos = dummyData.filter(item => item.type === 'video').length;
    const images = dummyData.filter(item => item.type === 'image').length;
    const articles = dummyData.filter(item => item.type === 'article').length;
    document.querySelectorAll('.footer-videos-count').forEach(el => el.innerText = videos);
    document.querySelectorAll('.footer-images-count').forEach(el => el.innerText = images);
    document.querySelectorAll('.footer-articles-count').forEach(el => el.innerText = articles);
}

function updatePlayerStats() { }

function updatePlayerFooterStats() { }

// ================ دوال البحث ================
function doSearch(query = '') {
    let data = [...allDBs[currentSourceKey]];
    data = data.filter(item => item.type === currentType);
    if (query) {
        query = query.toLowerCase();
        data = data.filter(item => item.title.toLowerCase().includes(query));
    }
    dummyData = data;
    currentPage = 1;
    renderAll();
    saveState();  // <-- أضف هذا السطر
}

// ================ دوال مدة الفيديو ================
function getVideoDuration(url) {
    return new Promise((resolve) => {
        if (url.includes('drive.google.com')) {
            resolve("05:30");
            return;
        }
        const video = document.createElement('video');
        video.preload = 'metadata';
        video.crossOrigin = 'anonymous';
        const timeout = setTimeout(() => {
            resolve("03:45");
        }, 3000);
        video.onloadedmetadata = function() {
            clearTimeout(timeout);
            const seconds = video.duration;
            if (isFinite(seconds) && seconds > 0) {
                const minutes = Math.floor(seconds / 60);
                const remainingSeconds = Math.floor(seconds % 60);
                const duration = `${minutes.toString().padStart(2, '0')}:${remainingSeconds.toString().padStart(2, '0')}`;
                URL.revokeObjectURL(video.src);
                resolve(duration);
            } else {
                resolve("03:45");
            }
        };
        video.onerror = function() {
            clearTimeout(timeout);
            resolve("03:45");
        };
        video.src = url;
    });
}

async function updateVideoDurations() {
    console.log('جاري تحديث مدة الفيديوهات...');
    const videoCards = document.querySelectorAll('.video-card[data-video-type="video"]');
    for (const card of videoCards) {
        const videoId = card.dataset.videoId;
        const videoUrl = card.dataset.videoUrl;
        const durationBadge = document.getElementById(`duration-${videoId}`);
        if (durationBadge && durationBadge.innerHTML.includes('--:--')) {
            try {
                const duration = await getVideoDuration(videoUrl);
                durationBadge.innerHTML = `<i>⏱️</i> ${duration}`;
                console.log(`تم تحديث مدة الفيديو ${videoId}: ${duration}`);
            } catch (e) {
                console.log('خطأ في تحديث مدة الفيديو', videoId);
            }
        }
    }
}

// ================ دوال التنقل في الفوتر ================
function goToVideos() {
    if (currentSourceKey === 'life') {
        switchSource('zaj', 'الزواج');
    }
    currentType = 'video';
    currentCat = 'all content';
    profileMode = null;
    currentPage = 1;
    saveState();
    renderMenu();
    renderAll();
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

function goToImages() {
    if (currentSourceKey === 'life') {
        switchSource('zaj', 'الزواج');
    }
    currentType = 'image';
    currentCat = 'all content';
    profileMode = null;
    currentPage = 1;
    saveState();
    renderMenu();
    renderAll();
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

function goToArticles() {
    if (currentSourceKey !== 'life') {
        switchSource('life', 'الحياة الزوجية');
    }
    currentType = 'all';
    currentCat = 'all content';
    profileMode = null;
    currentPage = 1;
    saveState();
    renderMenu();
    renderAll();
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

function goToPopular() {
    if (currentSourceKey === 'life') {
        switchSource('zaj', 'الزواج');
    }
    currentType = 'video';
    currentCat = 'all content';
    profileMode = null;
    currentPage = 1;
    saveState();
    renderMenu();
    renderAll();
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

// ================ دوال الإعلانات تحت الترقيم ================
// دالة لعرض الإعلانات تحت الترقيم مع دعم الإعلان الواحد المتغير للهواتف
// دالة لعرض الإعلانات تحت الترقيم مع دعم الفيديو MP4 في الإعلانات المربعة والأفقية
// استبدل دالة renderAdsBelowPagination بالكامل بهذا الكود
function renderAdsBelowPagination() {
    const paginationBox = document.getElementById('paginationBox');
    if (!paginationBox) return;
    let adsContainer = document.getElementById('adsBelowPagination');
    if (!adsContainer) {
        adsContainer = document.createElement('div');
        adsContainer.id = 'adsBelowPagination';
        adsContainer.className = 'mt-6 mb-4';
        paginationBox.parentNode.insertBefore(adsContainer, paginationBox.nextSibling);
    }

    const isMobile = window.innerWidth < 768;

    function renderMediaItem(ad) {
        const videoSrc = ad.video ? ad.video.trim() : '';
        const imageSrc = ad.image || 'https://via.placeholder.com/400x225/333/fff?text=AD';
        if (videoSrc) {
            return `<video autoplay muted loop playsinline style="display: block; width: auto; height: auto; max-width: 100%; margin: 0 auto;" src="${videoSrc}"></video>`;
        } else {
            return `<img src="${imageSrc}" style="display: block; width: auto; height: auto; max-width: 100%; margin: 0 auto;" alt="${ad.title || 'إعلان'}">`;
        }
    }

    if (isMobile) {
        let adHtml = '';
        if (!adsCache.horizontalRectAd) {
            const useSquare = Math.random() < 0.7;
            if (useSquare && siteAds.paginationSquareAds && siteAds.paginationSquareAds.length) {
                const adsList = siteAds.paginationSquareAds;
                const randomAd = adsList[Math.floor(Math.random() * adsList.length)];
                adsCache.horizontalRectAd = { type: 'square', data: randomAd };
            } else if (siteAds.horizontalRectAds && siteAds.horizontalRectAds.length) {
                const horizontalAd = siteAds.horizontalRectAds[Math.floor(Math.random() * siteAds.horizontalRectAds.length)];
                adsCache.horizontalRectAd = { type: 'horizontal', data: horizontalAd };
            } else {
                adsCache.horizontalRectAd = { type: 'square', data: siteAds.paginationSquareAds[0] };
            }
        }

        const cached = adsCache.horizontalRectAd;
        if (cached.type === 'square') {
            const ad = cached.data;
            adHtml = `
                <div class="ad-square bg-zinc-900/50 rounded-lg overflow-hidden border border-zinc-800 hover:border-pink-500 transition-all group">
                    <a href="${ad.link}" target="_blank" class="block">
                        <div class="relative bg-black flex items-center justify-center" style="min-height: unset; height: auto;">
                            ${renderMediaItem(ad)}
                        </div>
                    </a>
                    <div class="text-center text-[10px] text-gray-500 py-2 mt-2">📢 إعلان</div>
                </div>
            `;
        } else {
            const ad = cached.data;
            adHtml = `
                <div class="ad-horizontal bg-zinc-900/50 rounded-xl overflow-hidden border border-zinc-800 hover:border-pink-500 transition-all group">
                    <a href="${ad.link}" target="_blank" class="block w-full">
                        <div class="relative bg-black flex items-center justify-center" style="min-height: unset; height: auto;">
                            ${renderMediaItem(ad)}
                        </div>
                        <div class="text-center text-[10px] text-gray-500 py-2">📢 إعلان</div>
                    </a>
                </div>
            `;
        }
        adsContainer.innerHTML = adHtml;
    } 
    else {
        const showSquares = Math.random() < 0.5;
        let html = '';
        if (showSquares) {
            let squareAds = [];
            if (siteAds.paginationSquareAds && siteAds.paginationSquareAds.length) {
                const shuffled = [...siteAds.paginationSquareAds].sort(() => 0.5 - Math.random());
                squareAds = shuffled.slice(0, 3);
            } else {
                squareAds = [
                    { link: '#', title: 'إعلان', image: '' },
                    { link: '#', title: 'إعلان', image: '' },
                    { link: '#', title: 'إعلان', image: '' }
                ];
            }
            html += `<div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">`;
            squareAds.forEach(ad => {
                html += `
                    <div class="ad-square bg-zinc-900/50 rounded-lg overflow-hidden border border-zinc-800 hover:border-pink-500 transition-all group">
                        <a href="${ad.link}" target="_blank" class="block">
                            <div class="relative bg-black flex items-center justify-center" style="min-height: unset; height: auto;">
                                ${renderMediaItem(ad)}
                            </div>
                        </a>
                    </div>
                `;
            });
            html += `</div>`;
        } else {
            let rectAd = null;
            if (siteAds.horizontalRectAds && siteAds.horizontalRectAds.length) {
                rectAd = siteAds.horizontalRectAds[Math.floor(Math.random() * siteAds.horizontalRectAds.length)];
            }
            if (rectAd) {
                html += `
                    <div class="ad-horizontal bg-zinc-900/50 rounded-xl overflow-hidden border border-zinc-800 hover:border-pink-500 transition-all group mt-4">
                        <a href="${rectAd.link}" target="_blank" class="block w-full">
                            <div class="relative bg-black flex items-center justify-center" style="min-height: unset; height: auto;">
                                ${renderMediaItem(rectAd)}
                            </div>
                            <div class="text-center text-[10px] text-gray-500 py-2">📢 إعلان مستطيل</div>
                        </a>
                    </div>
                `;
            }
        }
        html += `<div class="text-center text-[10px] text-gray-500 py-2 mt-2">📢 إعلانات</div>`;
        adsContainer.innerHTML = html;
    }
}
function optimizeImageDisplay() {
    const vArea = document.getElementById('vArea');
    if (vArea) {
        const images = vArea.querySelectorAll('img');
        images.forEach(img => {
            if (img.complete) {
                const isPortrait = img.naturalHeight > img.naturalWidth;
                vArea.classList.toggle('portrait-media', isPortrait);
                vArea.classList.toggle('landscape-media', !isPortrait);
            } else {
                img.onload = function() {
                    const isPortrait = this.naturalHeight > this.naturalWidth;
                    vArea.classList.toggle('portrait-media', isPortrait);
                    vArea.classList.toggle('landscape-media', !isPortrait);
                };
            }
        });
    }
}

// ================ أحداث التاريخ ================
window.onpopstate = function(event) {
    const playWindow = document.getElementById('playWindow');
    const articleView = document.getElementById('articleView');
    if (playWindow && (playWindow.style.display === 'block' || playWindow.style.display === 'flex' || !playWindow.classList.contains('hidden'))) {
        playWindow.classList.add('hidden');
        playWindow.style.display = 'none';
        document.getElementById('vArea').innerHTML = '';
        document.body.style.overflow = 'auto';
        setTimeout(() => {
            window.scrollTo({
                top: scrollPosition || 0,
                behavior: 'smooth'
            });
        }, 50);
        return false;
    }
    if (articleView && articleView.style.display === 'block') {
        articleView.style.display = 'none';
        setTimeout(() => {
            window.scrollTo({
                top: scrollPosition || 0,
                behavior: 'smooth'
            });
        }, 50);
        return false;
    }
};

// ================ أحداث تحميل الصفحة ================
window.onload = () => {
	loadData();
    updateUserUI();
    if (currentUser) {
        ageVerificationEnabled = false;
        const modal = document.getElementById('ageVerifyModal');
        if (modal) modal.style.display = 'none';
    } else {
        ageVerificationEnabled = true;
    }
    updateFooterStats();
    updateUserActivity();
    checkAgeVerification();
    

    // محاولة استعادة الفيديو قيد التشغيل أولاً (من sessionStorage فقط)
    const playingId = sessionStorage.getItem('currentPlayingId');
    const playingSource = sessionStorage.getItem('lastPlayingSource');
    if (playingId && playingSource && allDBs[playingSource]) {
        currentSourceKey = playingSource;
        currentSourceName = sessionStorage.getItem('lastPlayingSourceName') || (playingSource === 'zaj' ? 'الزواج' : playingSource);
        dummyData = shuffleArray(allDBs[playingSource]);
        const videoItem = dummyData.find(item => item.id == playingId);
        if (videoItem) {
            const savedScroll = sessionStorage.getItem('scrollBeforePlayer');
            if (savedScroll) scrollPosition = parseInt(savedScroll);
            setTimeout(() => {
                playVideo(videoItem);
            }, 100);
            return;
        } else {
            sessionStorage.removeItem('currentPlayingId');
            sessionStorage.removeItem('lastPlayingSource');
            sessionStorage.removeItem('lastPlayingSourceName');
        }
    }

    // إذا لم يكن هناك فيديو قيد التشغيل، نستعيد حالة التصفح من sessionStorage
    const savedSource = sessionStorage.getItem('selectedSource');
    const savedPage = sessionStorage.getItem('currentPage');
    const savedProfileMode = sessionStorage.getItem('profileMode');
    
    if (savedSource && allDBs[savedSource]) {
        currentSourceKey = savedSource;
        currentSourceName = sessionStorage.getItem('selectedSourceName') || 'الزواج';
        dummyData = shuffleArray(allDBs[savedSource]);
        currentType = sessionStorage.getItem('selectedType') || 'video';
        currentCat = sessionStorage.getItem('selectedCat') || 'all content';
        activeParent = sessionStorage.getItem('activeParent') || null;
        if (activeParent === "null") activeParent = null;
        currentPage = savedPage ? parseInt(savedPage) : 1;
        if (savedProfileMode && (savedProfileMode === 'fav' || savedProfileMode === 'like' || savedProfileMode === 'watchlater')) {
            profileMode = savedProfileMode;
        } else {
            profileMode = null;
        }
  } else {
    // لا توجد جلسة سابقة: نستخدم القسم الافتراضي Straight مع الفيديوهات
    currentSourceKey = 'zaj';
    currentSourceName = 'الزواج';
    dummyData = shuffleArray(allDBs['zaj']);
    
        currentType = 'video';
        currentCat = 'all content';
        activeParent = null;
        profileMode = null;
        currentPage = 1;
        sessionStorage.setItem('selectedSource', 'zaj');
        sessionStorage.setItem('selectedSourceName', 'الزواج');
        sessionStorage.setItem('selectedType', 'video');
        sessionStorage.setItem('selectedCat', 'all content');
        sessionStorage.setItem('activeParent', '');
        sessionStorage.setItem('currentPage', '1');
        sessionStorage.removeItem('profileMode');
    }

    const activeNameEl = document.getElementById('activeName');
    if(activeNameEl && currentSourceKey) {
        activeNameEl.innerHTML = t('sectionNames')[currentSourceKey];
    }
    const catTitle = document.getElementById('catTitle');
    if (catTitle) {
        catTitle.innerText = t('sectionTitles')[currentSourceKey] || t('sectionNames')[currentSourceKey];
    }
    renderMenu(); 
    renderAll();
    updateSubTitle();
    if(localStorage.getItem('darkMode') === 'true'){
        document.body.classList.add('dark-mode');
    }
    setTimeout(updateVideoDurations, 1000);
    setInterval(checkUserActivity, 10000);
    setInterval(checkAgeVerification, 60000);
    setTimeout(() => {
        renderAdsBelowPagination();
        renderSideAds(); 
    }, 500);
    history.replaceState({view: 'home'}, "");
    document.getElementById('langBtn').innerHTML = currentLang === 'ar' ? '🌐 AR' : '🌐 EN';
    
    restoreScrollPosition();
};

document.addEventListener('DOMContentLoaded', function() {
    updateAllTexts();
});

function handleBackAction() {
    document.getElementById('playWindow').style.display = 'none';
    document.getElementById('playWindow').classList.add('hidden');
    document.getElementById('vArea').innerHTML = "";
    document.getElementById('recBox').innerHTML = "";
    setTimeout(() => {
        window.scrollTo({
            top: scrollPosition || 0,
            behavior: 'smooth'
        });
    }, 100);
}

function toggleAction(event, type, id) {
    event.stopPropagation();
    let list = (type === 'fav') ? favorites : likes;
    let key = (type === 'fav') ? 'myFavs' : 'myLikes';
    const idx = list.indexOf(id);
    idx > -1 ? list.splice(idx, 1) : list.push(id);
    localStorage.setItem(key, JSON.stringify(list));
    renderAll();
}

function enhanceMobileMenu() {
    if (window.innerWidth <= 768) {
        document.querySelectorAll('.card-menu-btn').forEach(btn => {
            btn.addEventListener('touchstart', function(e) {
                e.stopPropagation();
            });
        });
        document.addEventListener('touchstart', function(e) {
            if (!e.target.closest('.card-menu-btn') && !e.target.closest('.card-menu-dropdown')) {
                document.querySelectorAll('.card-menu-dropdown.show').forEach(menu => {
                    menu.classList.remove('show');
                });
            }
        });
    }
}

function initMobileSidebar() {
    if (window.innerWidth <= 768) {
        if (!document.getElementById('sidebarMenuBtn')) {
            const menuBtn = document.createElement('button');
            menuBtn.id = 'sidebarMenuBtn';
            menuBtn.setAttribute('aria-label', 'القائمة الرئيسية');
            document.body.appendChild(menuBtn);
            const overlay = document.createElement('div');
            overlay.id = 'sidebarOverlay';
            document.body.appendChild(overlay);
            const sidebarMenu = document.createElement('div');
            sidebarMenu.id = 'mobileSidebarMenu';
            const closeBtn = document.createElement('div');
            closeBtn.id = 'closeSidebarMenu';
            closeBtn.innerHTML = '✕';
            sidebarMenu.appendChild(closeBtn);
            const profileTitle = document.createElement('h3');
            profileTitle.className = 'text-center text-pink-500 font-bold text-lg mb-3 mt-2';
            profileTitle.innerText = 'ملفي الشخصي';
            sidebarMenu.appendChild(profileTitle);
            const originalSidebar = document.querySelector('aside.w-full.lg\\:w-64');
            if (originalSidebar) {
                const profileCard = originalSidebar.querySelector('.profile-card');
                if (profileCard) {
                    const clonedProfile = profileCard.cloneNode(true);
                    clonedProfile.style.margin = '0 0 20px 0';
                    clonedProfile.style.background = 'linear-gradient(135deg, #be185d, #6d28d9)';
                    sidebarMenu.appendChild(clonedProfile);
                }
            }
            const browseTitle = document.createElement('h3');
            browseTitle.className = 'text-center text-pink-500 font-bold text-lg mb-3 mt-4';
            browseTitle.innerText = 'تصفح المحتوى';
            sidebarMenu.appendChild(browseTitle);
            const dbSwitchContainer = document.createElement('div');
            dbSwitchContainer.className = 'mb-4';
            const activeSection = document.getElementById('activeName')?.innerText || 'قسم الزواج';
            dbSwitchContainer.innerHTML = `
                <div class="db-switch-btn-mobile" style="background: linear-gradient(135deg, #917ee6, #6d28d9); color: white; padding: 15px; border-radius: 50px; display: flex; justify-content: space-between; align-items: center; font-weight: bold; cursor: pointer; margin-bottom: 10px; border: 2px solid rgba(255,255,255,0.2);">
                    <span style="font-size: 16px;">${activeSection}</span>
                    <span style="background: rgba(255,255,255,0.2); width: 30px; height: 30px; border-radius: 50%; display: flex; align-items: center; justify-content: center;">▼</span>
                </div>
                <div class="db-dropdown-mobile" style="display: none; background: #1f1f1f; border: 2px solid #be185d; border-radius: 20px; overflow: hidden; margin-bottom: 15px;">
                    <div class="section-option" data-section="zaj" style="padding: 15px 20px; border-bottom: 1px solid #333; cursor: pointer; color: white; font-size: 15px; transition: all 0.2s;">مستقيم ⚤</div>
                    <div class="section-option" data-section="nesa" style="padding: 15px 20px; border-bottom: 1px solid #333; cursor: pointer; color: white; font-size: 15px;">سحاقيات ⚢</div>
                    <div class="section-option" data-section="haywan" style="padding: 15px 20px; border-bottom: 1px solid #333; cursor: pointer; color: white; font-size: 15px;">متحولين جنسياً ⚧</div>
                             <div class="section-option" data-section="haywan" style="padding: 15px 20px; border-bottom: 1px solid #333; cursor: pointer; color: white; font-size: 15px;">  Anime</div>
                    <div class="section-option" data-section="life" style="padding: 15px 20px; cursor: pointer; color: white; font-size: 15px;">وضعيات جنسية</div>
                </div>
            `;
            sidebarMenu.appendChild(dbSwitchContainer);
            const menuContainer = document.createElement('div');
            menuContainer.id = 'mobileMenuBox';
            menuContainer.style.display = 'flex';
            menuContainer.style.flexDirection = 'row';
            menuContainer.style.flexWrap = 'wrap';
            menuContainer.style.gap = '8px';
            menuContainer.style.marginTop = '10px';
            let menuItems;
            if (currentSourceKey === 'life') {
                menuItems = [
                    { name: 'كل المحتوى', type: 'all', cat: 'all content', icon: 'https://b.top4top.io/p_3690o1nhv1.png' }
                ];
            } else {
                menuItems = [
                    { name: 'الفيديوهات', type: 'video', cat: 'all content', icon: 'https://b.top4top.io/p_3690o1nhv1.png' },
                    { name: 'أفلام', type: 'movie', cat: 'all content', icon: 'https://b.top4top.io/p_3690o1nhv1.png' },
                    { name: 'صور ثابتة', type: 'image', cat: 'ثابتة', icon: 'https://h.top4top.io/p_3690et83z1.png' },
                    { name: 'صور متحركة', type: 'image', cat: 'متحركة', icon: 'https://l.top4top.io/p_36909qyxa1.png' }
                ];
            }
            menuItems.forEach(item => {
                const isActive = (currentSourceKey === 'life' && item.type === 'all') || 
                                (currentType === item.type && currentCat === item.cat);
                const menuItem = document.createElement('div');
                menuItem.className = 'mobile-menu-item';
                menuItem.style.flex = '1 1 auto';
                menuItem.style.minWidth = 'calc(33.33% - 8px)';
                menuItem.style.background = isActive ? 'linear-gradient(135deg, #be185d, #6d28d9)' : 'rgba(39, 39, 42, 0.8)';
                menuItem.style.padding = '12px 5px';
                menuItem.style.borderRadius = '30px';
                menuItem.style.display = 'flex';
                menuItem.style.flexDirection = 'column';
                menuItem.style.alignItems = 'center';
                menuItem.style.gap = '5px';
                menuItem.style.cursor = 'pointer';
                menuItem.style.border = '1px solid rgba(255,255,255,0.1)';
                menuItem.style.transition = 'all 0.3s ease';
                menuItem.setAttribute('data-type', item.type);
                menuItem.setAttribute('data-cat', item.cat);
                menuItem.innerHTML = `
                    <img src="${item.icon}" style="width: 24px; height: 24px; object-fit: contain;">
                    <span style="font-size: 11px; font-weight: 600; color: white; text-align: center;">${item.name}</span>
                `;
                menuItem.addEventListener('click', function() {
                    if (currentSourceKey === 'life' && item.type === 'all') {
                        currentType = 'all';
                        currentCat = 'all content';
                    } else {
                        currentType = item.type;
                        currentCat = item.cat;
                    }
                    activeParent = null;
                    profileMode = null;
                    currentPage = 1;
                    saveState();
                    if (typeof renderMenu === 'function') renderMenu();
                    if (typeof renderAll === 'function') renderAll();
                    sidebarMenu.classList.remove('show');
                    overlay.classList.remove('show');
                    updateMobileMenuActiveState();
                });
                menuContainer.appendChild(menuItem);
            });
            sidebarMenu.appendChild(menuContainer);
            const statsPreview = document.createElement('div');
            statsPreview.style.display = 'flex';
            statsPreview.style.justifyContent = 'space-around';
            statsPreview.style.marginTop = '20px';
            statsPreview.style.padding = '15px 10px';
            statsPreview.style.background = 'rgba(0,0,0,0.3)';
            statsPreview.style.borderRadius = '50px';
            const videosCount = dummyData?.filter(item => item.type === 'video').length || 0;
            const imagesCount = dummyData?.filter(item => item.type === 'image').length || 0;
            const articlesCount = dummyData?.filter(item => item.type === 'article').length || 0;
            statsPreview.innerHTML = `
                <div style="text-align: center;">
                    <div style="font-size: 18px; font-weight: bold; color: #be185d;">${videosCount}</div>
                    <div style="font-size: 10px; color: #999;">فيديو</div>
                </div>
                <div style="text-align: center;">
                    <div style="font-size: 18px; font-weight: bold; color: #be185d;">${imagesCount}</div>
                    <div style="font-size: 10px; color: #999;">صورة</div>
                </div>
                <div style="text-align: center;">
                    <div style="font-size: 18px; font-weight: bold; color: #be185d;">${articlesCount}</div>
                    <div style="font-size: 10px; color: #999;">مقال</div>
                </div>
            `;
            sidebarMenu.appendChild(statsPreview);
            document.body.appendChild(sidebarMenu);
            menuBtn.addEventListener('click', function() {
                sidebarMenu.classList.add('show');
                overlay.classList.add('show');
                updateMobileMenuContent();
            });
            closeBtn.addEventListener('click', function() {
                sidebarMenu.classList.remove('show');
                overlay.classList.remove('show');
            });
            overlay.addEventListener('click', function() {
                sidebarMenu.classList.remove('show');
                overlay.classList.remove('show');
            });
            const dbSwitch = document.querySelector('.db-switch-btn-mobile');
            const dbDropdown = document.querySelector('.db-dropdown-mobile');
            if (dbSwitch) {
                dbSwitch.addEventListener('click', function() {
                    dbDropdown.style.display = dbDropdown.style.display === 'block' ? 'none' : 'block';
                });
            }
            document.querySelectorAll('.section-option').forEach(option => {
                option.addEventListener('click', function() {
                    const section = this.dataset.section;
                    const sectionNames = {
    zaj: currentLang === 'ar' ? 'مستقيم ⚤' : 'Straight',
    nesa: currentLang === 'ar' ? 'سحاقيات ⚢' : 'Lesbian',
    haywan: currentLang === 'ar' ? 'متحولين جنسياً ⚧' : 'Trans',
    ime: currentLang === 'ar' ? 'أنمي 🎌' : 'Anime 🎌',
    life: currentLang === 'ar' ? 'وضعيات جنسية' : 'Sex Positions'
};
                    document.querySelector('.db-switch-btn-mobile span:first-child').innerHTML = sectionNames[section];
                    dbDropdown.style.display = 'none';
                    if (typeof switchSource === 'function') {
                        switchSource(section, sectionNames[section]);
                    }
                    setTimeout(updateMobileMenuContent, 300);
                    setTimeout(() => {
                        sidebarMenu.classList.remove('show');
                        overlay.classList.remove('show');
                    }, 500);
                });
            });
            setTimeout(() => {
                const favBtn = document.querySelector('#mobileSidebarMenu #btnFav');
                const likeBtn = document.querySelector('#mobileSidebarMenu #btnLike');
                const watchLaterBtn = document.querySelector('#mobileSidebarMenu #btnWatchLater');
                if (favBtn) {
                    favBtn.onclick = function(e) {
                        e.stopPropagation();
                        toggleProfileMode('fav');
                        updateMobileSidebarButtons();
                        setTimeout(() => {
                            sidebarMenu.classList.remove('show');
                            overlay.classList.remove('show');
                        }, 300);
                    };
                }
                if (likeBtn) {
                    likeBtn.onclick = function(e) {
                        e.stopPropagation();
                        toggleProfileMode('like');
                        updateMobileSidebarButtons();
                        setTimeout(() => {
                            sidebarMenu.classList.remove('show');
                            overlay.classList.remove('show');
                        }, 300);
                    };
                }
                if (watchLaterBtn) {
                    watchLaterBtn.onclick = function(e) {
                        e.stopPropagation();
                        toggleProfileMode('watchlater');
                        updateMobileSidebarButtons();
                        setTimeout(() => {
                            sidebarMenu.classList.remove('show');
                            overlay.classList.remove('show');
                        }, 300);
                    };
                }
            }, 100);
        }
    } else {
        const elements = ['sidebarMenuBtn', 'mobileSidebarMenu', 'sidebarOverlay'];
        elements.forEach(id => {
            const el = document.getElementById(id);
            if (el) el.remove();
        });
    }
}

function updateMobileMenuContent() {
    const activeSection = document.getElementById('activeName')?.innerText || 'قسم الزواج';
    const switchBtnSpan = document.querySelector('.db-switch-btn-mobile span:first-child');
    if (switchBtnSpan) switchBtnSpan.innerHTML = activeSection;
    updateMobileSidebarButtons();
    const statsPreview = document.querySelector('#mobileSidebarMenu > div:last-child');
    if (statsPreview) {
        const videosCount = dummyData?.filter(item => item.type === 'video').length || 0;
        const imagesCount = dummyData?.filter(item => item.type === 'image').length || 0;
        const articlesCount = dummyData?.filter(item => item.type === 'article').length || 0;
        statsPreview.innerHTML = `
            <div style="text-align: center;">
                <div style="font-size: 18px; font-weight: bold; color: #be185d;">${videosCount}</div>
                <div style="font-size: 10px; color: #999;">فيديو</div>
            </div>
            <div style="text-align: center;">
                <div style="font-size: 18px; font-weight: bold; color: #be185d;">${imagesCount}</div>
                <div style="font-size: 10px; color: #999;">صورة</div>
            </div>
            <div style="text-align: center;">
                <div style="font-size: 18px; font-weight: bold; color: #be185d;">${articlesCount}</div>
                <div style="font-size: 10px; color: #999;">مقال</div>
            </div>
        `;
    }
}
// دالة لتغيير القسم والفئة من القائمة الجانبية الثابتة
function changeCategory(event, sourceKey, displayName, contentType, categoryType) {
    if (event) {
        event.preventDefault();
        event.stopPropagation();
    }
    resetAdsCache();
    
    // تعيين المصدر أولاً
    currentSourceKey = sourceKey;
    currentSourceName = displayName;
    sessionStorage.setItem('selectedSource', sourceKey);
    sessionStorage.setItem('selectedSourceName', displayName);
    
    // تحديث البيانات
    dummyData = shuffleArray(allDBs[sourceKey]);
    currentFilterTag = null;
    profileMode = null;
    currentPage = 1;
    
    // تعيين نوع المحتوى والفئة حسب المعطيات
    if (contentType === 'video') {
        currentType = 'video';
        currentCat = 'all content';
    } else if (contentType === 'movie') {
        currentType = 'movie';
        currentCat = 'all content';
    } else if (contentType === 'image') {
        currentType = 'image';
        currentCat = (categoryType === 'static') ? 'ثابتة' : (categoryType === 'animated' ? 'متحركة' : 'all content');
    } else if (contentType === 'article') {
        currentType = 'article';
        currentCat = 'all content';
    } else {
        currentType = 'all';
        currentCat = 'all content';
    }
    updateImageModeClass(); 
    
    // تحديث واجهة المستخدم
    const catTitle = document.getElementById('catTitle');
    if (catTitle) catTitle.innerText = t('sectionTitles')[sourceKey] || t('sectionNames')[sourceKey] || sourceKey;
    updateSubTitle();
    saveState();
    
    window.history.pushState({view: 'section', section: sourceKey}, "");
    window.scrollTo({ top: 0, behavior: 'smooth' });
    
    renderMenu(); 
    renderAll();
    updateFooterStats();
    setTimeout(() => renderAdsBelowPagination(), 300);
    
    // إغلاق القائمة الجانبية إذا كانت مفتوحة
    const sidebar = document.getElementById('mainSidebar');
    if (sidebar && sidebar.classList.contains('translate-x-0')) {
        toggleSidebar();
    }
}

function updateMobileMenuActiveState() {
    const menuItems = document.querySelectorAll('#mobileMenuBox .mobile-menu-item');
    const currentSource = currentSourceKey;
    menuItems.forEach(item => {
        const type = item.dataset.type;
        const cat = item.dataset.cat;
        let isActive = false;
        if (currentSource === 'life' && type === 'all') {
            isActive = true;
        } else {
            isActive = (currentType === type && currentCat === cat);
        }
        item.style.background = isActive ? 'linear-gradient(135deg, #be185d, #6d28d9)' : 'rgba(39, 39, 42, 0.8)';
    });
}

// دالة دمج لـ renderAll (واحدة فقط)
const originalRenderAll = renderAll;
renderAll = function() {
    originalRenderAll();
    updateSidebarStats();
    if (window.innerWidth <= 768) {
        setTimeout(updateMobileMenuContent, 200);
    }
};

function updateMainMenuPopup() {
    const popupMenus = document.querySelectorAll('.card-menu-dropdown');
    popupMenus.forEach(menu => {
        menu.innerHTML = '';
        const closeBtn = document.createElement('div');
        closeBtn.className = 'close-menu';
        closeBtn.innerHTML = '✕';
        closeBtn.onclick = function(e) {
            e.stopPropagation();
            menu.classList.remove('show');
        };
        menu.appendChild(closeBtn);
        const savedSection = document.createElement('div');
        savedSection.className = 'saved-section';
        savedSection.innerHTML = `
            <div class="saved-title">📁 المحفوظات</div>
            <div class="saved-buttons">
                <div class="saved-row">
                    <div class="saved-btn ${profileMode === 'fav' ? 'active' : ''}" onclick="toggleProfileMode('fav'); this.closest('.card-menu-dropdown').classList.remove('show');">❤️ المفضل</div>
                    <div class="saved-btn ${profileMode === 'like' ? 'active' : ''}" onclick="toggleProfileMode('like'); this.closest('.card-menu-dropdown').classList.remove('show');">👍 الإعجاب</div>
                </div>
                <div class="saved-row">
                    <div class="saved-btn ${profileMode === 'watchlater' ? 'active' : ''}" onclick="toggleProfileMode('watchlater'); this.closest('.card-menu-dropdown').classList.remove('show');">⏱️ المشاهدة لاحقاً</div>
                </div>
            </div>
            <div class="saved-stats">
                <span>📹 ${dummyData?.filter(item => item.type === 'video').length || 0}</span>
                <span>🖼️ ${dummyData?.filter(item => item.type === 'image').length || 0}</span>
                <span>📄 ${dummyData?.filter(item => item.type === 'article').length || 0}</span>
            </div>
        `;
        menu.appendChild(savedSection);
        const sectionsTitle = document.createElement('div');
        sectionsTitle.className = 'sections-title';
        sectionsTitle.innerText = '📋 الأقسام';
        menu.appendChild(sectionsTitle);
        const activeSection = document.getElementById('activeName')?.innerText || 'قسم الزواج';
        const dbSwitch = document.createElement('div');
        dbSwitch.className = 'db-switch-mobile';
        dbSwitch.innerHTML = `<span>${activeSection}</span> <span>▼</span>`;
        const dbDropdown = document.createElement('div');
        dbDropdown.className = 'db-dropdown-mobile';
        dbDropdown.innerHTML = `
            <div class="db-option" data-section="zaj">مستقيم ⚤</div>
            <div class="db-option" data-section="nesa">سحاقيات ⚢</div>
            <div class="db-option" data-section="haywan">متحولين جنسياً ⚧</div>
                     <div class="db-option" data-section="haywan">  Anime</div>
            <div class="db-option" data-section="life">وضعيات جنسية</div>
        `;
        dbSwitch.onclick = () => {
            dbDropdown.classList.toggle('show');
        };
        dbDropdown.querySelectorAll('.db-option').forEach(opt => {
            opt.onclick = function() {
                const section = this.dataset.section;
                const names = {
                    zaj: 'مستقيم ⚤',
                    nesa: 'سحاقيات ⚢',
                    haywan: 'متحولين جنسياً ⚧',
                    ime: '  Anime',
                    life: 'وضعيات جنسية'
                };
                dbSwitch.querySelector('span:first-child').innerText = names[section];
                dbDropdown.classList.remove('show');
                if (typeof switchSource === 'function') {
                    switchSource(section, names[section]);
                }
                menu.classList.remove('show');
            };
        });
        menu.appendChild(dbSwitch);
        menu.appendChild(dbDropdown);
        const mainSections = document.createElement('div');
        mainSections.className = 'main-sections';
        const currentSource = currentSourceKey || 'zaj';
        let sections = [];
        if (currentSource === 'life') {
            sections = [
                { name: 'كل المحتوى', type: 'all', cat: 'all content', icon: 'https://b.top4top.io/p_3690o1nhv1.png' }
            ];
        } else {
            sections = [
                { name: 'فيديوهات', type: 'video', cat: 'all content', icon: 'https://b.top4top.io/p_3690o1nhv1.png' },
                { name: 'صور ثابتة', type: 'image', cat: 'ثابتة', icon: 'https://h.top4top.io/p_3690et83z1.png' },
                { name: 'صور متحركة', type: 'image', cat: 'متحركة', icon: 'https://l.top4top.io/p_36909qyxa1.png' }
            ];
        }
        sections.forEach(section => {
            const isActive = (currentSource === 'life' && section.type === 'all') || 
                            (currentType === section.type && currentCat === section.cat);
            const sectionItem = document.createElement('div');
            sectionItem.className = 'section-item';
            sectionItem.style.background = isActive ? 'linear-gradient(135deg, #be185d, #6d28d9)' : '';
            sectionItem.innerHTML = `
                <img src="${section.icon}">
                <span>${section.name}</span>
            `;
            sectionItem.onclick = function() {
                if (currentSource === 'life' && section.type === 'all') {
                    currentType = 'all';
                    currentCat = 'all content';
                } else {
                    currentType = section.type;
                    currentCat = section.cat;
                }
                activeParent = null;
                profileMode = null;
                currentPage = 1;
                saveState();
                renderMenu();
                renderAll();
                menu.classList.remove('show');
            };
            mainSections.appendChild(sectionItem);
        });
        menu.appendChild(mainSections);
        const quickStats = document.createElement('div');
        quickStats.className = 'quick-stats';
        quickStats.innerHTML = `
            <div>
                <div class="number">${dummyData?.filter(item => item.type === 'video').length || 0}</div>
                <div class="label">فيديو</div>
            </div>
            <div>
                <div class="number">${dummyData?.filter(item => item.type === 'image').length || 0}</div>
                <div class="label">صورة</div>
            </div>
            <div>
                <div class="number">${dummyData?.filter(item => item.type === 'article').length || 0}</div>
                <div class="label">مقال</div>
            </div>
        `;
        menu.appendChild(quickStats);
    });
}

const originalCreateCard = createCard;
createCard = function(p) {
    let html = originalCreateCard(p);
    setTimeout(() => {
        if (window.innerWidth <= 768) {
            updateMainMenuPopup();
        }
    }, 100);
    return html;
};

// ===== إصلاح قائمة الثلاث نقاط للهواتف =====
(function fixMobileMenuFinal() {
    window.toggleCardMenu = function(event, itemId) {
        event.stopPropagation();
        event.preventDefault();
        document.querySelectorAll('.card-menu-dropdown').forEach(menu => {
            if (menu.id !== `menu-${itemId}`) {
                menu.classList.remove('show');
            }
        });
        const existingOverlay = document.querySelector('.menu-overlay');
        if (existingOverlay) existingOverlay.remove();
        const menu = document.getElementById(`menu-${itemId}`);
        if (!menu) return;
        if (menu.classList.contains('show')) {
            menu.classList.remove('show');
            return;
        }
        const item = dummyData.find(i => i.id == itemId);
        if (!item) return;
        menu.innerHTML = '';
        const favDiv = document.createElement('div');
        favDiv.className = `card-menu-item ${favorites.includes(itemId) ? 'active' : ''}`;
        favDiv.innerHTML = `<span>❤️</span><span>${favorites.includes(itemId) ? 'إزالة من المفضلة' : 'إضافة للمفضلة'}</span>`;
        favDiv.onclick = function(e) {
            e.stopPropagation();
            const idx = favorites.indexOf(itemId);
            if (idx > -1) {
                favorites.splice(idx, 1);
            } else {
                favorites.push(itemId);
            }
            localStorage.setItem('myFavs', JSON.stringify(favorites));
            menu.innerHTML = '';
            const newFavDiv = document.createElement('div');
            newFavDiv.className = `card-menu-item ${favorites.includes(itemId) ? 'active' : ''}`;
            newFavDiv.innerHTML = `<span>❤️</span><span>${favorites.includes(itemId) ? 'إزالة من المفضلة' : 'إضافة للمفضلة'}</span>`;
            newFavDiv.onclick = arguments.callee;
            const newWatchDiv = document.createElement('div');
            newWatchDiv.className = `card-menu-item ${watchLater.includes(itemId) ? 'active' : ''}`;
            newWatchDiv.innerHTML = `<span>⏱️</span><span>${watchLater.includes(itemId) ? 'إزالة من المشاهدة لاحقاً' : 'مشاهدة لاحقاً'}</span>`;
            newWatchDiv.onclick = function(ev) {
                ev.stopPropagation();
                const wIdx = watchLater.indexOf(itemId);
                if (wIdx > -1) {
                    watchLater.splice(wIdx, 1);
                } else {
                    watchLater.push(itemId);
                }
                localStorage.setItem('myWatchLater', JSON.stringify(watchLater));
                menu.innerHTML = '';
                const ffDiv = document.createElement('div');
                ffDiv.className = `card-menu-item ${favorites.includes(itemId) ? 'active' : ''}`;
                ffDiv.innerHTML = `<span>❤️</span><span>${favorites.includes(itemId) ? 'إزالة من المفضلة' : 'إضافة للمفضلة'}</span>`;
                ffDiv.onclick = newFavDiv.onclick;
                const wwDiv = document.createElement('div');
                wwDiv.className = `card-menu-item ${watchLater.includes(itemId) ? 'active' : ''}`;
                wwDiv.innerHTML = `<span>⏱️</span><span>${watchLater.includes(itemId) ? 'إزالة من المشاهدة لاحقاً' : 'مشاهدة لاحقاً'}</span>`;
                wwDiv.onclick = arguments.callee;
                menu.appendChild(ffDiv);
                menu.appendChild(wwDiv);
                updateStats();
                renderAll();
            };
            menu.appendChild(newFavDiv);
            menu.appendChild(newWatchDiv);
            updateStats();
            renderAll();
        };
        const watchDiv = document.createElement('div');
        watchDiv.className = `card-menu-item ${watchLater.includes(itemId) ? 'active' : ''}`;
        watchDiv.innerHTML = `<span>⏱️</span><span>${watchLater.includes(itemId) ? 'إزالة من المشاهدة لاحقاً' : 'مشاهدة لاحقاً'}</span>`;
        watchDiv.onclick = function(e) {
            e.stopPropagation();
            const idx = watchLater.indexOf(itemId);
            if (idx > -1) {
                watchLater.splice(idx, 1);
            } else {
                watchLater.push(itemId);
            }
            localStorage.setItem('myWatchLater', JSON.stringify(watchLater));
            menu.innerHTML = '';
            const newFavDiv2 = document.createElement('div');
            newFavDiv2.className = `card-menu-item ${favorites.includes(itemId) ? 'active' : ''}`;
            newFavDiv2.innerHTML = `<span>❤️</span><span>${favorites.includes(itemId) ? 'إزالة من المفضلة' : 'إضافة للمفضلة'}</span>`;
            newFavDiv2.onclick = favDiv.onclick;
            const newWatchDiv2 = document.createElement('div');
            newWatchDiv2.className = `card-menu-item ${watchLater.includes(itemId) ? 'active' : ''}`;
            newWatchDiv2.innerHTML = `<span>⏱️</span><span>${watchLater.includes(itemId) ? 'إزالة من المشاهدة لاحقاً' : 'مشاهدة لاحقاً'}</span>`;
            newWatchDiv2.onclick = arguments.callee;
            menu.appendChild(newFavDiv2);
            menu.appendChild(newWatchDiv2);
            updateStats();
            renderAll();
        };
        menu.appendChild(favDiv);
        menu.appendChild(watchDiv);
        const btn = event.currentTarget;
        const rect = btn.getBoundingClientRect();
        if (window.innerWidth <= 768) {
            menu.style.position = 'fixed';
            menu.style.top = '50%';
            menu.style.left = '50%';
            menu.style.transform = 'translate(-50%, -50%)';
            menu.style.bottom = 'auto';
            menu.style.right = 'auto';
            menu.style.margin = '0';
        } else {
            menu.style.position = 'absolute';
            menu.style.bottom = '100%';
            menu.style.left = '0';
            menu.style.transform = 'none';
            menu.style.marginBottom = '8px';
        }
        menu.classList.add('show');
        const closeMenu = function(e) {
            if (!menu.contains(e.target) && !e.target.closest('.card-menu-btn')) {
                menu.classList.remove('show');
                document.removeEventListener('click', closeMenu);
                document.removeEventListener('touchstart', closeMenu);
            }
        };
        setTimeout(() => {
            document.addEventListener('click', closeMenu);
            document.addEventListener('touchstart', closeMenu);
        }, 100);
    };
    console.log('✅ تم إصلاح قائمة الثلاث نقاط - بدون شاشة سوداء');
})();

function renderSideAds() {
    const container = document.getElementById('sideAdsContainer');
    if (!container) return;
    
    // تهيئة الإعلانات الجانبية إذا لم تكن موجودة
    const ads = initSidebarAdsForCurrentSection();
    
    let html = '<div class="text-[10px] uppercase tracking-widest opacity-30 font-bold mb-3 text-center text-gray-400">Sponsored</div>';
    for(let i = 0; i < ads.length; i++) {
        let ad = ads[i];
        html += `
            <a href="${ad.link}" target="_blank" class="block w-full aspect-square rounded-2xl overflow-hidden border border-zinc-800 hover:border-pink-600 transition-all transform hover:scale-[1.02] shadow-xl mb-3 group relative">
                <div class="w-full h-full bg-black flex items-center justify-center">
                    ${ad.type === 'video' ? `
                        <video class="w-full h-full object-cover" autoplay muted loop playsinline>
                            <source src="${ad.url}" type="video/mp4">
                        </video>
                    ` : `
                        <img src="${ad.url}" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" alt="إعلان">
                    `}
                </div>
                <div class="absolute bottom-2 left-2 right-2 text-white text-xs font-bold opacity-0 group-hover:opacity-100 transition-opacity text-center bg-black/60 backdrop-blur-sm py-1 rounded-full">
                    اضغط للمزيد
                </div>
            </a>
            <div class="text-center text-[8px] text-pink-500/70 -mt-2 mb-2">📢 إعلان</div>
        `;
    }
    container.innerHTML = html;
}

function fixSidebarOrder() {
    const sidebar = document.querySelector('aside.w-full.lg\\:w-64');
    if (!sidebar) return;
    const profileCard = sidebar.querySelector('.profile-card');
    const dbSwitch = sidebar.querySelector('.relative');
    const menuBox = sidebar.querySelector('#menuBox');
    const verticalAd = sidebar.querySelector('.vertical-ad-left');
    const elements = [profileCard, dbSwitch, menuBox, verticalAd];
    elements.forEach(el => {
        if (el && el.parentNode === sidebar) {
            sidebar.removeChild(el);
        }
    });
    if (profileCard) sidebar.appendChild(profileCard);
    if (dbSwitch) sidebar.appendChild(dbSwitch);
    if (menuBox) sidebar.appendChild(menuBox);
    if (verticalAd) sidebar.appendChild(verticalAd);
}
setTimeout(fixSidebarOrder, 100);
const originalRenderMenu = window.renderMenu;
if (typeof originalRenderMenu === 'function') {
    window.renderMenu = function() {
        originalRenderMenu();
        setTimeout(fixSidebarOrder, 50);
    };
}

function getRandomHorizontalRectAd() {
    const ads = siteAds.horizontalRectAds || [];
    return ads[Math.floor(Math.random() * ads.length)];
}

function initRightSlideMenu() {
    const existingBtn = document.getElementById('rightSlideBtn');
    if (existingBtn) existingBtn.remove();
    const existingMenu = document.getElementById('rightSlideMenu');
    if (existingMenu) existingMenu.remove();
    const existingOverlay = document.getElementById('rightSlideOverlay');
    if (existingOverlay) existingOverlay.remove();
    const menuBtn = document.createElement('button');
    menuBtn.id = 'rightSlideBtn';
    menuBtn.setAttribute('aria-label', 'القائمة');
    menuBtn.style.cssText = `
        position: fixed;
        top: 50%;
        right: 15px;
        transform: translateY(-50%);
        width: 48px;
        height: 48px;
        background: linear-gradient(135deg, #be185d, #6d28d9);
        border: 2px solid rgba(255,255,255,0.3);
        border-radius: 50%;
        color: white;
        font-size: 24px;
        font-weight: bold;
        display: flex;
        align-items: center;
        justify-content: center;
        z-index: 10000;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(0,0,0,0.3);
    `;
    menuBtn.onmouseenter = () => {
        menuBtn.style.transform = 'translateY(-50%) scale(1.05)';
        menuBtn.style.boxShadow = '0 6px 20px rgba(190,24,93,0.4)';
    };
    menuBtn.onmouseleave = () => {
        menuBtn.style.transform = 'translateY(-50%) scale(1)';
        menuBtn.style.boxShadow = '0 4px 15px rgba(0,0,0,0.3)';
    };
    document.body.appendChild(menuBtn);
    const overlay = document.createElement('div');
    overlay.id = 'rightSlideOverlay';
    overlay.style.cssText = `
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.7);
        backdrop-filter: blur(5px);
        z-index: 9998;
        opacity: 0;
        visibility: hidden;
        transition: all 0.3s ease;
    `;
    document.body.appendChild(overlay);
    const slideMenu = document.createElement('div');
    slideMenu.id = 'rightSlideMenu';
    slideMenu.style.cssText = `
        position: fixed;
        top: 0;
        right: 0;
        width: 320px;
        max-width: 85%;
        height: 100vh;
        background: linear-gradient(135deg, #0f0f0f, #050505);
        border-left: 2px solid #be185d;
        z-index: 10001;
        transform: translateX(100%);
        transition: transform 0.3s ease;
        overflow-y: auto;
        overflow-x: hidden;
        box-shadow: -5px 0 30px rgba(0,0,0,0.5);
    `;
    const closeBtn = document.createElement('div');
    closeBtn.innerHTML = '✕';
    closeBtn.style.cssText = `
        position: absolute;
        top: 15px;
        left: 15px;
        width: 36px;
        height: 36px;
        background: rgba(255,255,255,0.1);
        border: 1px solid #be185d;
        border-radius: 50%;
        color: white;
        font-size: 18px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.2s ease;
        z-index: 10002;
    `;
    closeBtn.onmouseenter = () => {
        closeBtn.style.background = '#be185d';
        closeBtn.style.transform = 'rotate(90deg)';
    };
    closeBtn.onmouseleave = () => {
        closeBtn.style.background = 'rgba(255,255,255,0.1)';
        closeBtn.style.transform = 'rotate(0deg)';
    };
    slideMenu.appendChild(closeBtn);
    const menuContent = document.createElement('div');
    menuContent.style.padding = '20px 16px 20px 20px';
    menuContent.style.direction = 'rtl';
    const profileSection = document.createElement('div');
    profileSection.style.cssText = `
        background: linear-gradient(135deg, #be185d, #6d28d9);
        border-radius: 16px;
        padding: 16px;
        margin-bottom: 25px;
    `;
    profileSection.innerHTML = `
        <div class="text-[10px] uppercase font-bold opacity-80 mb-2" style="color:white;">📁 ملفي الشخصي</div>
        <div class="flex flex-col gap-2">
            <div class="flex gap-2">
                <div id="slideBtnFav" class="slide-tab-btn flex-1 text-center py-2 px-1 rounded-lg bg-white/10 text-white text-sm font-bold cursor-pointer transition-all">❤️ المفضل</div>
                <div id="slideBtnLike" class="slide-tab-btn flex-1 text-center py-2 px-1 rounded-lg bg-white/10 text-white text-sm font-bold cursor-pointer transition-all">👍 الإعجاب</div>
            </div>
            <div class="flex gap-2 mt-1">
                <div id="slideBtnWatchLater" class="slide-tab-btn flex-1 text-center py-2 px-1 rounded-lg bg-white/10 text-white text-sm font-bold cursor-pointer transition-all">⏱️ المشاهدة لاحقاً</div>
            </div>
        </div>
        <div id="slideStats" class="mt-3 pt-3 border-t border-white/20 flex justify-around text-xs text-white/80">
            <span>📹 ${dummyData?.filter(item => item.type === 'video').length || 0}</span>
            <span>🖼️ ${dummyData?.filter(item => item.type === 'image').length || 0}</span>
            <span>📄 ${dummyData?.filter(item => item.type === 'article').length || 0}</span>
        </div>
    `;
    menuContent.appendChild(profileSection);
    const sectionsTitle = document.createElement('h3');
    sectionsTitle.innerText = '📋 الأقسام';
    sectionsTitle.style.cssText = `
        color: #be185d;
        font-size: 1.1rem;
        font-weight: bold;
        margin: 0 0 15px 0;
        padding-bottom: 8px;
        border-bottom: 2px solid rgba(190,24,93,0.3);
    `;
    menuContent.appendChild(sectionsTitle);
    const mainSections = [
    { key: 'zaj', name: 'مستقيم ⚤', nameEn: 'Straight', icon: 'https://b.top4top.io/p_3690o1nhv1.png', hasSubmenu: true },
    { key: 'nesa', name: 'سحاقيات ⚢', nameEn: 'Lesbian', icon: 'https://h.top4top.io/p_3690et83z1.png', hasSubmenu: true },
    { key: 'haywan', name: 'متحولين جنسياً ⚧', nameEn: 'Trans', icon: 'https://l.top4top.io/p_36909qyxa1.png', hasSubmenu: true },
    { key: 'ime', name: '  Anime', nameEn: 'Trans', icon: 'https://l.top4top.io/p_36909qyxa1.png', hasSubmenu: true },
    { key: 'liveGirls', name: 'Live Girls', nameEn: 'Live Girls', icon: '🔴', hasSubmenu: false },
    { key: 'liveChannels', name: 'Live Channels Porn', nameEn: 'Live Channels Porn', icon: '📺', hasSubmenu: false },
    { key: 'life', name: 'وضعيات جنسية', nameEn: 'Sex Positions', icon: 'https://b.top4top.io/p_3690o1nhv1.png', hasSubmenu: false }
];
    mainSections.forEach(section => {
        const sectionContainer = document.createElement('div');
        sectionContainer.style.cssText = `
            margin-bottom: 8px;
            border-radius: 12px;
            background: ${currentSourceKey === section.key ? 'rgba(190, 24, 93, 0.2)' : 'rgba(39, 39, 42, 0.5)'};
            overflow: hidden;
            transition: all 0.2s ease;
        `;
        const sectionHeader = document.createElement('div');
        sectionHeader.style.cssText = `
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 14px 16px;
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 600;
            font-size: 1rem;
            color: ${currentSourceKey === section.key ? '#fff' : '#ddd'};
        `;
        sectionHeader.innerHTML = `
            <div class="flex items-center gap-3">
                <span style="font-size: 1.3rem;">${section.icon}</span>
                <span>${section.name}</span>
            </div>
            <span class="slide-arrow-${section.key}" style="font-size: 0.9rem; transition: transform 0.2s ease; color: #be185d;">▼</span>
        `;
        const subMenu = document.createElement('div');
        subMenu.className = `slide-submenu-${section.key}`;
        subMenu.style.cssText = `
            display: ${currentSourceKey === section.key ? 'block' : 'none'};
            background: rgba(0, 0, 0, 0.4);
            padding: 8px 0;
        `;
        if (section.key !== 'life') {
            const subItems = [
                { type: 'video', cat: 'all content', name: '🎬 الفيديوهات', icon: '🎬' },
                { type: 'movie', cat: 'all content', name: '🎬 افلام', icon: '🎬' },
                { type: 'image', cat: 'ثابتة', name: '🖼️ صور ثابتة', icon: '🖼️' },
                { type: 'image', cat: 'متحركة', name: '🎞️ صور متحركة', icon: '🎞️' }
            ];
            subItems.forEach(sub => {
                const subItem = document.createElement('div');
                subItem.style.cssText = `
                    padding: 10px 20px 10px 45px;
                    cursor: pointer;
                    transition: all 0.2s ease;
                    font-size: 0.85rem;
                    display: flex;
                    align-items: center;
                    gap: 10px;
                    color: #ccc;
                `;
                subItem.innerHTML = `<span style="font-size: 1rem;">${sub.icon}</span><span>${sub.name}</span>`;
                const isActive = (currentSourceKey === section.key && currentType === sub.type && currentCat === sub.cat);
                if (isActive) {
                    subItem.style.background = 'rgba(190, 24, 93, 0.3)';
                    subItem.style.color = '#fff';
                }
                subItem.onmouseenter = () => {
                    subItem.style.background = 'rgba(190, 24, 93, 0.2)';
                };
                subItem.onmouseleave = () => {
                    if (!isActive) subItem.style.background = 'transparent';
                };
                subItem.onclick = (e) => {
                    e.stopPropagation();
                    const sectionNames = {
                        zaj: 'مستقيم ⚤',
                        nesa: 'سحاقيات ⚢',
                        haywan: 'متحولين جنسياً ⚧',
                       ime: '  Anime',                       
                        life: 'وضعيات جنسية'
                    };
                    if (typeof switchSource === 'function') {
                        switchSource(section.key, sectionNames[section.key]);
                    }
                    setTimeout(() => {
                        currentType = sub.type;
                        currentCat = sub.cat;
                        activeParent = null;
                        profileMode = null;
                        currentPage = 1;
                        saveState();
                        renderMenu();
                        renderAll();
                    }, 50);
                    slideMenu.style.transform = 'translateX(100%)';
                    overlay.style.opacity = '0';
                    overlay.style.visibility = 'hidden';
                };
                subMenu.appendChild(subItem);
            });
        } else {
            const subItem = document.createElement('div');
            subItem.style.cssText = `
                padding: 10px 20px 10px 45px;
                cursor: pointer;
                transition: all 0.2s ease;
                font-size: 0.85rem;
                display: flex;
                align-items: center;
                gap: 10px;
                color: #ccc;
            `;
            subItem.innerHTML = `<span style="font-size: 1rem;">📄</span><span>المقالات</span>`;
            const isActive = (currentSourceKey === 'life');
            if (isActive) {
                subItem.style.background = 'rgba(190, 24, 93, 0.3)';
                subItem.style.color = '#fff';
            }
            subItem.onmouseenter = () => {
                subItem.style.background = 'rgba(190, 24, 93, 0.2)';
            };
            subItem.onmouseleave = () => {
                if (!isActive) subItem.style.background = 'transparent';
            };
            subItem.onclick = (e) => {
                e.stopPropagation();
                if (typeof switchSource === 'function') {
                    switchSource('life', 'وضعيات جنسية');
                }
                setTimeout(() => {
                    currentType = 'all';
                    currentCat = 'all content';
                    activeParent = null;
                    profileMode = null;
                    currentPage = 1;
                    saveState();
                    renderMenu();
                    renderAll();
                }, 50);
                slideMenu.style.transform = 'translateX(100%)';
                overlay.style.opacity = '0';
                overlay.style.visibility = 'hidden';
            };
            subMenu.appendChild(subItem);
        }
        const arrowSpan = sectionHeader.querySelector(`.slide-arrow-${section.key}`);
        sectionHeader.onclick = (e) => {
            e.stopPropagation();
            const isCurrentlyOpen = subMenu.style.display === 'block';
            if (currentSourceKey === section.key) {
                subMenu.style.display = isCurrentlyOpen ? 'none' : 'block';
                arrowSpan.style.transform = isCurrentlyOpen ? 'rotate(0deg)' : 'rotate(180deg)';
            } else {
                const sectionNames = {
                    zaj: 'مستقيم ⚤',
                    nesa: 'سحاقيات ⚢',
                    haywan: 'متحولين جنسياً ⚧',
                    ime: '  Anime',                  
                    live: "Live Girls", 
                    life: 'وضعيات جنسية'
                };
                if (typeof switchSource === 'function') {
                    switchSource(section.key, sectionNames[section.key]);
                }
                setTimeout(() => {
                    if (section.key !== 'life') {
                        currentType = 'video';
                        currentCat = 'all content';
                    } else {
                        currentType = 'all';
                        currentCat = 'all content';
                    }
                    activeParent = null;
                    profileMode = null;
                    currentPage = 1;
                    saveState();
                    renderMenu();
                    renderAll();
                    subMenu.style.display = 'block';
                    arrowSpan.style.transform = 'rotate(180deg)';
                    document.querySelectorAll('[class^="slide-submenu-"]').forEach(menu => {
                        if (menu !== subMenu) {
                            menu.style.display = 'none';
                        }
                    });
                    document.querySelectorAll('[class^="slide-arrow-"]').forEach(arrow => {
                        if (arrow !== arrowSpan) {
                            arrow.style.transform = 'rotate(0deg)';
                        }
                    });
                }, 50);
                document.querySelectorAll('#rightSlideMenu > div > div').forEach(container => {
                    if (container !== sectionContainer) {
                        container.style.background = 'rgba(39, 39, 42, 0.5)';
                    }
                });
                sectionContainer.style.background = 'rgba(190, 24, 93, 0.2)';
            }
        };
        sectionContainer.appendChild(sectionHeader);
        sectionContainer.appendChild(subMenu);
        menuContent.appendChild(sectionContainer);
        if (currentSourceKey === section.key) {
            subMenu.style.display = 'block';
            arrowSpan.style.transform = 'rotate(180deg)';
        }
    });
    const quickStats = document.createElement('div');
    quickStats.style.cssText = `
        margin-top: 20px;
        padding: 15px 10px;
        background: rgba(0,0,0,0.3);
        border-radius: 12px;
        display: flex;
        justify-content: space-around;
        text-align: center;
    `;
    quickStats.innerHTML = `
        <div>
            <div style="font-size: 18px; font-weight: bold; color: #be185d;">${dummyData?.filter(item => item.type === 'video').length || 0}</div>
            <div style="font-size: 10px; color: #999;">فيديو</div>
        </div>
        <div>
            <div style="font-size: 18px; font-weight: bold; color: #be185d;">${dummyData?.filter(item => item.type === 'image').length || 0}</div>
            <div style="font-size: 10px; color: #999;">صورة</div>
        </div>
        <div>
            <div style="font-size: 18px; font-weight: bold; color: #be185d;">${dummyData?.filter(item => item.type === 'article').length || 0}</div>
            <div style="font-size: 10px; color: #999;">مقال</div>
        </div>
    `;
    menuContent.appendChild(quickStats);
    slideMenu.appendChild(menuContent);
    document.body.appendChild(slideMenu);
    menuBtn.onclick = () => {
        slideMenu.style.transform = 'translateX(0)';
        overlay.style.opacity = '1';
        overlay.style.visibility = 'visible';
        updateSlideStats();
        updateSlideButtons();
    };
    closeBtn.onclick = () => {
        slideMenu.style.transform = 'translateX(100%)';
        overlay.style.opacity = '0';
        overlay.style.visibility = 'hidden';
    };
    overlay.onclick = () => {
        slideMenu.style.transform = 'translateX(100%)';
        overlay.style.opacity = '0';
        overlay.style.visibility = 'hidden';
    };
    document.getElementById('slideBtnFav')?.addEventListener('click', () => {
        toggleProfileMode('fav');
        updateSlideButtons();
        setTimeout(() => {
            slideMenu.style.transform = 'translateX(100%)';
            overlay.style.opacity = '0';
            overlay.style.visibility = 'hidden';
        }, 300);
    });
    document.getElementById('slideBtnLike')?.addEventListener('click', () => {
        toggleProfileMode('like');
        updateSlideButtons();
        setTimeout(() => {
            slideMenu.style.transform = 'translateX(100%)';
            overlay.style.opacity = '0';
            overlay.style.visibility = 'hidden';
        }, 300);
    });
    document.getElementById('slideBtnWatchLater')?.addEventListener('click', () => {
        toggleProfileMode('watchlater');
        updateSlideButtons();
        setTimeout(() => {
            slideMenu.style.transform = 'translateX(100%)';
            overlay.style.opacity = '0';
            overlay.style.visibility = 'hidden';
        }, 300);
    });
}

function updateSlideStats() {
    const statsDiv = document.getElementById('slideStats');
    if (statsDiv) {
        const videosCount = dummyData?.filter(item => item.type === 'video').length || 0;
        const imagesCount = dummyData?.filter(item => item.type === 'image').length || 0;
        const articlesCount = dummyData?.filter(item => item.type === 'article').length || 0;
        statsDiv.innerHTML = `
            <span>📹 ${videosCount}</span>
            <span>🖼️ ${imagesCount}</span>
            <span>📄 ${articlesCount}</span>
        `;
    }
}

function updateSlideButtons() {
    const favBtn = document.getElementById('slideBtnFav');
    const likeBtn = document.getElementById('slideBtnLike');
    const watchLaterBtn = document.getElementById('slideBtnWatchLater');
    if (favBtn) {
        favBtn.style.background = profileMode === 'fav' ? 'rgba(255,255,255,0.3)' : 'rgba(255,255,255,0.1)';
    }
    if (likeBtn) {
        likeBtn.style.background = profileMode === 'like' ? 'rgba(255,255,255,0.3)' : 'rgba(255,255,255,0.1)';
    }
    if (watchLaterBtn) {
        watchLaterBtn.style.background = profileMode === 'watchlater' ? 'rgba(255,255,255,0.3)' : 'rgba(255,255,255,0.1)';
    }
}

function addResponsiveStyles() {
    const style = document.createElement('style');
    style.textContent = `
        @media (max-width: 768px) {
            #rightSlideMenu {
                width: 280px !important;
            }
            .slide-tab-btn {
                font-size: 12px !important;
                padding: 8px 4px !important;
            }
        }
        @media (max-width: 480px) {
            #rightSlideMenu {
                width: 260px !important;
            }
        }
        #rightSlideMenu::-webkit-scrollbar {
            width: 4px;
        }
        #rightSlideMenu::-webkit-scrollbar-track {
            background: #1a1a1a;
        }
        #rightSlideMenu::-webkit-scrollbar-thumb {
            background: #be185d;
            border-radius: 4px;
        }
    `;
    document.head.appendChild(style);
}

function activateCard(element) {
    document.querySelectorAll('.candy-card').forEach(card => {
        card.classList.remove('active');
    });
    element.classList.add('active');
    if (element.classList.contains('confirm-card')) {
        console.log("تم تأكيد الفهم");
    }
}

function fixPortraitSuggestions() {
    setTimeout(() => {
        document.querySelectorAll('#recBox .relative.rounded-xl').forEach(container => {
            const img = container.querySelector('img');
            if (img) {
                if (img.classList.contains('portrait-thumb') || 
                    (img.complete && img.naturalHeight > img.naturalWidth)) {
                    container.style.aspectRatio = '9 / 16';
                    container.style.minHeight = '400px';
                    img.style.objectFit = 'contain';
                    img.style.background = '#000';
                } else {
                    container.style.aspectRatio = '16 / 9';
                    img.style.objectFit = 'cover';
                }
            }
        });
    }, 200);
}


function openLivePlayer() {
    const container = document.getElementById("livePlayerContainer");
    const frame = document.getElementById("liveFrame");
    frame.src = "http://oxax.tv/oh-ah.html";
    container.style.display = "block";
    document.getElementById("mainContent").style.display = "none";
}

function closeLivePlayer() {
    const container = document.getElementById("livePlayerContainer");
    const frame = document.getElementById("liveFrame");
    frame.src = "";
    container.style.display = "none";
    document.getElementById("mainContent").style.display = "block";
}

function checkLive() {
    const params = new URLSearchParams(window.location.search);
    const isLive = params.get("live");
    if (isLive === "true") {
        window.location.replace("http://oxax.tv/oh-ah.html");
    }
}

const originalUpdateRecSuggestions = updateRecSuggestions;
updateRecSuggestions = function() {
    originalUpdateRecSuggestions();
    fixPortraitSuggestions();
};
function updateImageModeClass() {
    if (currentType === 'image') {
        document.body.classList.add('image-mode-active');
    } else {
        document.body.classList.remove('image-mode-active');
    }
}

function initPlayer() {
    const video = document.getElementById('mainVideo');
    const progress = document.getElementById('progressBar');
    const playPauseBtn = document.getElementById('playPauseBtn');
    const currentTimeSpan = document.getElementById('currentTime');
    const durationSpan = document.getElementById('durationTime');
    const fullscreenBtn = document.getElementById('fullscreenBtn');
    const centerPlayBtn = document.getElementById('centerPlayBtn');
    if (!video) return;
    function formatTime(seconds) {
        if (isNaN(seconds)) return '00:00';
        const mins = Math.floor(seconds / 60);
        const secs = Math.floor(seconds % 60);
        return `${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`;
    }
    function hideCenterBtn() {
        if (centerPlayBtn) centerPlayBtn.style.display = 'none';
    }
    function showCenterBtnIfPaused() {
        if (video.paused && video.currentTime < video.duration) {
            centerPlayBtn.style.display = 'flex';
        } else {
            centerPlayBtn.style.display = 'none';
        }
    }
    function showSeekMessage(delta) {
        let msgDiv = document.getElementById('seekMessage');
        if (!msgDiv) {
            msgDiv = document.createElement('div');
            msgDiv.id = 'seekMessage';
            msgDiv.style.cssText = `
                position: absolute;
                bottom: 20%;
                left: 50%;
                transform: translateX(-50%);
                background: rgba(0,0,0,0.7);
                color: white;
                padding: 8px 16px;
                border-radius: 30px;
                font-size: 16px;
                font-weight: bold;
                z-index: 20;
                pointer-events: none;
                backdrop-filter: blur(4px);
                transition: opacity 0.3s;
                opacity: 0;
            `;
            document.getElementById('playerContainer').appendChild(msgDiv);
        }
        const text = delta > 0 ? `+${delta} ثانية` : `${delta} ثانية`;
        msgDiv.innerText = text;
        msgDiv.style.opacity = '1';
        setTimeout(() => {
            msgDiv.style.opacity = '0';
        }, 800);
    }
    video.addEventListener('dblclick', (e) => {
        e.preventDefault();
        e.stopPropagation();
        video.classList.add('video-double-tap');
        setTimeout(() => video.classList.remove('video-double-tap'), 150);
        const rect = video.getBoundingClientRect();
        const clickX = e.clientX - rect.left;
        const videoWidth = rect.width;
        const isLeftHalf = clickX < videoWidth / 2;
        const seekAmount = 10;
        let newTime = video.currentTime + (isLeftHalf ? -seekAmount : seekAmount);
        newTime = Math.min(Math.max(newTime, 0), video.duration);
        video.currentTime = newTime;
        showSeekMessage(isLeftHalf ? -seekAmount : seekAmount);
    });
    video.onloadedmetadata = () => {
        durationSpan.innerText = formatTime(video.duration);
        progress.max = video.duration;
        showCenterBtnIfPaused();
    };
    video.ontimeupdate = () => {
        progress.value = video.currentTime;
        currentTimeSpan.innerText = formatTime(video.currentTime);
        const percent = (video.currentTime / video.duration) * 100;
        progress.style.background = `linear-gradient(to right, #be185d ${percent}%, rgba(255,255,255,0.3) ${percent}%)`;
        showCenterBtnIfPaused();
    };
    video.onended = () => {
        playPauseBtn.innerText = '▶️';
        showCenterBtnIfPaused();
    };
    playPauseBtn.onclick = () => {
        if (video.paused) {
            video.play();
            playPauseBtn.innerText = '⏸️';
            hideCenterBtn();
        } else {
            video.pause();
            playPauseBtn.innerText = '▶️';
            showCenterBtnIfPaused();
        }
    };
    if (centerPlayBtn) {
        centerPlayBtn.onclick = () => {
            video.play();
            playPauseBtn.innerText = '⏸️';
            hideCenterBtn();
        };
    }
    progress.oninput = () => {
        video.currentTime = progress.value;
        if (video.paused) showCenterBtnIfPaused();
    };
    const toggleFullscreen = () => {
        const target = video;
        if (target.webkitEnterFullscreen) {
            target.webkitEnterFullscreen();
            return;
        }
        if (!document.fullscreenElement) {
            const requestFullscreen = target.requestFullscreen || target.webkitRequestFullscreen || target.mozRequestFullScreen || target.msRequestFullscreen;
            if (requestFullscreen) {
                requestFullscreen.call(target).catch(err => {
                    const container = document.getElementById('playerContainer');
                    if (container && container !== target) {
                        const containerRequest = container.requestFullscreen || container.webkitRequestFullscreen || container.mozRequestFullScreen || container.msRequestFullscreen;
                        if (containerRequest) containerRequest.call(container);
                    }
                });
            }
        } else {
            const exitFullscreen = document.exitFullscreen || document.webkitExitFullscreen || document.mozCancelFullScreen || document.msExitFullscreen;
            if (exitFullscreen) exitFullscreen.call(document);
        }
    };
    fullscreenBtn.onclick = toggleFullscreen;
    const handleFullscreenChange = () => {
        if (document.fullscreenElement) {
            fullscreenBtn.innerText = '✕';
        } else {
            fullscreenBtn.innerText = '⛶';
        }
    };
    document.addEventListener('fullscreenchange', handleFullscreenChange);
    document.addEventListener('webkitfullscreenchange', handleFullscreenChange);
    document.addEventListener('mozfullscreenchange', handleFullscreenChange);
    document.addEventListener('MSFullscreenChange', handleFullscreenChange);
    const playerContainer = document.getElementById('playerContainer');
    let touchTimeout;
    playerContainer.addEventListener('touchstart', () => {
        const controls = document.getElementById('customControls');
        controls.style.opacity = '1';
        clearTimeout(touchTimeout);
        touchTimeout = setTimeout(() => {
            if (!playerContainer.matches(':hover') && !controls.matches(':hover')) {
                controls.style.opacity = '0';
            }
        }, 2000);
    });
    
    


    
    function handleClick(id) {
    const item = dummyData.find(x => x.id === id);
    if (!item) return;

    // إذا لايف → افتح موقع خارجي
    if (item.type === 'live') {
        window.open(item.contentURL, '_blank');
        return;
    }




    // غير هيج → نفس شغلك القديم
    openPlayer(id);
}
    progress.style.background = 'rgba(255,255,255,0.3)';
}

window.addEventListener('load', enhanceMobileMenu);
window.addEventListener('resize', enhanceMobileMenu);

window.toggleLanguage = toggleLanguage;
window.verifyAge = verifyAge;
window.toggleDBList = toggleDBList;
window.switchSource = switchSource;
window.toggleProfileMode = toggleProfileMode;
window.doSearch = doSearch;
window.goToPage = goToPage;
window.playVideo = playVideo;
window.closePlayer = closePlayer;
window.openArticle = openArticle;
window.closeArticle = closeArticle;
window.toggleCardMenu = toggleCardMenu;
window.toggleFavFromMenu = toggleFavFromMenu;
window.toggleWatchLaterFromMenu = toggleWatchLaterFromMenu;
window.toggleFavFromPlayer = toggleFavFromPlayer;
window.toggleLike = toggleLike;
window.toggleDislike = toggleDislike;
window.toggleWatchLater = toggleWatchLater;
window.shareVideo = shareVideo;
window.goToVideos = goToVideos;
window.goToImages = goToImages;
window.goToArticles = goToArticles;
window.goToPopular = goToPopular;
window.showAuthModal = showAuthModal;
window.closeAuthModal = closeAuthModal;
window.handleLogin = handleLogin;
window.handleRegister = handleRegister;
window.verifyCode = verifyCode;
window.resetToRegister = resetToRegister;
window.toggleAuthMode = toggleAuthMode;
window.googleSignIn = googleSignIn;
window.logout = logout;
// ================ إصلاح مشكلة التمرير إلى أعلى المشغل عند اختيار اقتراح ================

// حفظ الدالة الأصلية مؤقتاً
const originalPlayVideo = window.playVideo;

// دالة مساعدة للتمرير إلى أعلى نافذة المشغل
function scrollToTopOfPlayer() {
    const playerWindow = document.getElementById('playWindow');
    if (playerWindow && playerWindow.style.display === 'block') {
        // التمرير إلى أعلى نافذة المشغل
        playerWindow.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
        // أيضاً نمرر منطقة الفيديو نفسها للضمان
        const vArea = document.getElementById('vArea');
        if (vArea) {
            vArea.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
    }
}

// إعادة تعريف playVideo مع إضافة التمرير
window.playVideo = function(p) {
    // استدعاء الدالة الأصلية
    originalPlayVideo(p);
    
    // بعد تشغيل الفيديو، انتظر قليلاً ثم تمرير إلى الأعلى
    setTimeout(() => {
        scrollToTopOfPlayer();
    }, 150);
};

// التأكد من أن playVideoById تستخدم الدالة المعدلة
const originalPlayVideoById = window.playVideoById;
if (typeof originalPlayVideoById === 'function') {
    window.playVideoById = function(id) {
        const item = dummyData.find(i => i.id == id);
        if (item) {
            playVideo(item); // هذه تستخدم الدالة المعدلة
        } else {
            console.warn('العنصر غير موجود:', id);
        }
    };
} else {
    // في حال لم تكن موجودة، نضيفها
    window.playVideoById = function(id) {
        const item = dummyData.find(i => i.id == id);
        if (item) {
            playVideo(item);
        } else {
            console.warn('العنصر غير موجود:', id);
        }
    };
}

// إضافة مستمع إضافي لضمان التمرير عند تغيير عنوان الفيديو (في حالة التحميل البطيء)
const observer = new MutationObserver(function(mutations) {
    mutations.forEach(function(mutation) {
        if (mutation.type === 'childList' && mutation.target.id === 'vArea') {
            // تغير محتوى منطقة الفيديو -> قد يكون فيديو جديد
            scrollToTopOfPlayer();
        }
    });
});
const vAreaObserver = document.getElementById('vArea');
if (vAreaObserver) {
    observer.observe(vAreaObserver, { childList: true, subtree: true });
}
// ================ دوال مساعدة عامة للتبديل إلى الأقسام ================
function switchToStraightVideo() {
    switchSource('zaj', 'Straight');
    setTimeout(() => {
        currentType = 'video';
        currentCat = 'all content';
        profileMode = null;
        currentPage = 1;
        saveState();
        renderAll();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 50);
}

function switchToLesbianVideo() {
    switchSource('nesa', 'Lesbian');
    setTimeout(() => {
        currentType = 'video';
        currentCat = 'all content';
        profileMode = null;
        currentPage = 1;
        saveState();
        renderAll();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 50);
}

function switchToTransVideo() {
    switchSource('haywan', 'Trans');
    setTimeout(() => {
        currentType = 'video';
        currentCat = 'all content';
        profileMode = null;
        currentPage = 1;
        saveState();
        renderAll();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 50);
}

function switchToAnimeVideo() {
    switchSource('ime', 'Anime');
    setTimeout(() => {
        currentType = 'video';
        currentCat = 'all content';
        profileMode = null;
        currentPage = 1;
        saveState();
        renderAll();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 50);
}

function switchToImages() {
    if (currentSourceKey === 'life') switchSource('zaj', 'Straight');
    setTimeout(() => {
        currentType = 'image';
        currentCat = 'all content';
        profileMode = null;
        currentPage = 1;
        saveState();
        renderAll();
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }, 50);
}

function playRandomVideo() {
    let sourceData = dummyData.length ? dummyData : allDBs['zaj'];
    const videos = sourceData.filter(item => item.type === 'video');
    if (videos.length) {
        const randomVideo = videos[Math.floor(Math.random() * videos.length)];
        playVideo(randomVideo);
    } else {
        alert('لا توجد فيديوهات متاحة حالياً');
    }
}

// اجعلها متاحة عالميًا
window.switchToStraightVideo = switchToStraightVideo;
window.switchToLesbianVideo = switchToLesbianVideo;
window.switchToTransVideo = switchToTransVideo;
window.switchToAnimeVideo = switchToAnimeVideo;
window.switchToImages = switchToImages;
window.playRandomVideo = playRandomVideo;

// ================ الشريط العلوي الجديد (محسّن بالكامل) ================
function initTopNavbar() {
    const container = document.querySelector('.top-navbar ul');
    if (!container) return;

    // روابط قابلة للتخصيص
   // ملف script.js - تم التعديل في دالة initTopNavbar
const TIKTOK_URL = localStorage.getItem('tiktokRedirectUrl') || 'https://www.xfree.com/';
    const PORNSTARS_URL = '#';
    const COMMUNITY_URL = '#';

    const randomCategories = [...allCategories].sort(() => 0.5 - Math.random()).slice(0, 8);

    // تحديد السهم حسب اللغة
    const arrowIcon = currentLang === 'ar' ? '◀' : '▶';

    const navItems = [
        {
            label: 'pornxiq TikTok',
            type: 'link',
            action: () => window.open(TIKTOK_URL, '_blank'),
        },
        {
            label: 'PORN VIDEOS',
            type: 'dropdown',
            items: [
                { name: 'Straight video', action: switchToStraightVideo },
                { name: 'Lesbian video', action: switchToLesbianVideo },
                { name: 'Trans video', action: switchToTransVideo },
                { name: 'Anime video', action: switchToAnimeVideo }
            ]
        },
        {
            label: 'CATEGORIES',
            type: 'dropdown',
            items: randomCategories.map(cat => ({
                name: cat,
                action: () => filterByTag(cat)
            }))
        },
        {
            label: 'LIVE CAMS',
            type: 'link',
            action: () => switchSource('liveGirls', 'Live Girls'),
        },
        {
            label: 'LIVE CHANNELS',
            type: 'link',
            action: () => switchSource('liveChannels', 'Live Channels Porn'),
        },
        {
            label: 'PORNSTARS',
            type: 'link',
            action: () => window.open(PORNSTARS_URL, '_blank'),
        },
        {
            label: 'FUCK NOW',
            type: 'button',
            action: playRandomVideo,
            class: 'fuck-now'
        },
        {
            label: 'COMMUNITY',
            type: 'dropdown',
            items: [
                { name: 'Forum', action: () => window.open(COMMUNITY_URL, '_blank') },
                { name: 'Discord', action: () => window.open('https://discord.gg/example', '_blank') },
                { name: 'Telegram', action: () => window.open('https://t.me/example', '_blank') }
            ]
        },
        {
            label: 'PHOTOS & GIFTS',
            type: 'link',
            action: switchToImages,
        }
    ];

    // بناء HTML الشريط
    container.innerHTML = navItems.map(item => {
        if (item.type === 'link' || item.type === 'button') {
            return `<li><a class="nav-link ${item.class || ''}" href="javascript:void(0)">${item.label}</a></li>`;
        } else if (item.type === 'dropdown') {
            return `
                <li class="dropdown-wrapper">
                    <a class="nav-link dropdown-toggle" href="javascript:void(0)">
                        ${item.label} <span class="dropdown-arrow">${arrowIcon}</span>
                    </a>
                    <div class="dropdown-menu">
                        ${item.items.map(sub => `<a href="javascript:void(0)" class="dropdown-item">${sub.name}</a>`).join('')}
                    </div>
                </li>
            `;
        }
        return '';
    }).join('');

    // ربط الأحداث
    const dropdownToggles = container.querySelectorAll('.dropdown-toggle');
    const dropdownMenus = container.querySelectorAll('.dropdown-menu');

    // إغلاق جميع القوائم المنسدلة
    function closeAllDropdowns(except = null) {
        dropdownMenus.forEach(menu => {
            if (menu !== except) {
                menu.classList.remove('show');
            }
        });
    }

    // فتح/إغلاق القائمة عند النقر على الزر (يدعم اللمس)
    dropdownToggles.forEach((toggle) => {
        const parentLi = toggle.closest('li');
        const menu = parentLi.querySelector('.dropdown-menu');

        toggle.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();

            const isShown = menu.classList.contains('show');
            closeAllDropdowns(menu);

            if (!isShown) {
                menu.classList.add('show');
            }
        });
    });

    // ربط أحداث العناصر داخل القائمة
    container.querySelectorAll('.nav-link:not(.dropdown-toggle)').forEach(link => {
        link.addEventListener('click', (e) => {
            e.preventDefault();
            const label = link.innerText.trim();
            const item = navItems.find(i => i.label === label);
            if (item && (item.type === 'link' || item.type === 'button')) {
                item.action();
                closeAllDropdowns();
            }
        });
    });

    // ربط أحداث عناصر القوائم المنسدلة
    container.querySelectorAll('.dropdown-item').forEach(item => {
        item.addEventListener('click', (e) => {
            e.preventDefault();
            const parentDropdown = item.closest('.dropdown-wrapper');
            const toggle = parentDropdown.querySelector('.dropdown-toggle');
            const label = toggle.innerText.replace(arrowIcon, '').trim();
            const navItem = navItems.find(i => i.label === label);

            if (navItem && navItem.type === 'dropdown') {
                const subItemName = item.innerText.trim();
                const subItem = navItem.items.find(s => s.name === subItemName);
                if (subItem && typeof subItem.action === 'function') {
                    subItem.action();
                }
            }
            closeAllDropdowns();
        });
    });

    // إغلاق القوائم عند النقر خارجها
    document.addEventListener('click', (e) => {
        if (!e.target.closest('.top-navbar')) {
            closeAllDropdowns();
        }
    });
}
// إخفاء الحاويات الفارغة على الهواتف
function hideEmptyContainersOnMobile() {
    if (window.innerWidth <= 768) {
        const emptySelectors = [
            '#topCategoryBar',
            '#adsBelowPagination',
            '#sideAdsContainer',
            '.vertical-ad-left',
            'aside.xl\\:block'
        ];
        emptySelectors.forEach(selector => {
            const el = document.querySelector(selector);
            if (el && el.innerHTML.trim() === '') {
                el.style.display = 'none';
            }
        });
    }
}

// تنفيذ عند تحميل الصفحة وعند كل تغيير
window.addEventListener('load', hideEmptyContainersOnMobile);
window.addEventListener('resize', hideEmptyContainersOnMobile);
// استدعاء الدالة عند تحميل الصفحة
window.addEventListener('DOMContentLoaded', () => {
    initTopNavbar();
});

// استدعاء الدالة عند تحميل الصفحة
window.addEventListener('DOMContentLoaded', () => {
    initTopNavbar();
});


console.log('✅ تم إضافة التمرير التلقائي إلى أعلى المشغل عند اختيار اقتراح');


